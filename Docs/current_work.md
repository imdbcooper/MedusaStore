# Current Work

> Cleanup status: legacy provider/runtime routes have been removed from the master template. Delivery Hub/direct Yandex is the selected delivery contour; first real direct Yandex runtime dispatch now exists only behind a strict gate and only on a fully ready supported contour; backend startup must not require new delivery env secrets. Existing old databases may still contain obsolete delivery rows/provider ids and require separate operator-approved cleanup if relevant.

> Статус документа: текущий операционный фокус проекта по состоянию на `2026-04-22`
>
> Назначение: дать агенту с пустым контекстом быстрый и однозначный ответ на три вопроса:
>
> 1. что делаем прямо сейчас;
> 2. где именно в репозитории идет работа;
> 3. что еще рано начинать.
>
> Последний spec reconciliation: `2026-04-22` — [`delivery_hub_spec.md`](./delivery_hub_spec.md) v1.3 truthfully reconciled с materialized tranche-1 state после admin/store response-boundary hardening, no-network route coverage, focused admin page seam coverage через [`page-state.ts`](../medusa-agency-boilerplate/src/admin/routes/settings/delivery/page-state.ts), admin-only execution-plan observability contour поверх existing shipment planning preview, backend/admin-only normalized shipment-result preview seam, backend/admin-only fulfillment application mutation preview seam, backend/admin-only failure-handling / compensation preview seam и нового backend-only controlled shipment execution contract scaffold через [`shipment-execution-contract.ts`](../medusa-agency-boilerplate/src/modules/delivery-hub/shipment-execution-contract.ts), который связывает normalized execution plan, deterministic execution identity, idempotency reservation drift semantics, explicit neutral state machine и future persistence-intent drafts без live dispatch или persistence writes. Standard shipping method commit is generic/provider-neutral; removed provider routes/helpers are not used; Delivery Hub neutral save/clear/summary is present; live shipment dispatch remains gated/not enabled. Подробности — §16.14, §16.19, §16.20 spec.

---

## 1. Роль документа

Этот файл является **каноническим оперативным статусом проекта**.

Именно здесь должно быть явно написано:

- какая фаза активна сейчас;
- что является текущей задачей разработки;
- какие файлы и директории являются рабочей поверхностью;
- в каком порядке агент должен продолжать работу.

Если этот документ расходится с кодом, важнее код и проверенное состояние репозитория. После проверки этот документ нужно обновить.

---

## 2. Что делаем сейчас

### Активная фаза

**Фаза 2** из [master_repo_plan_v2.md](./master_repo_plan_v2.md) завершена и подтверждена проверками.

`Фаза 6 storefront customization` теперь truthfully закрыта только после reopen/remediation cycle: прежний closure verdict был overstated, затем reopening был признан валидным по трём gap'ам, а post-remediation regression/readiness checkpoint `2026-04-19` зафиксировал финальный verdict **PASS**.

`Фаза 7: шаблонизация и ускорение запуска нового клиента` теперь тоже truthfully закрыта: tranche 1 init contract baseline и tranche 2 packaging/handoff narrative уже были materialized, а `2026-04-20` closure-check дополнительно подтвердил tranche 3 через повторный RC smoke pass на каноническом runtime path `bootstrap → preflight → dev` и baseline integrity runtime gates.

Текущий operational context больше не находится внутри открытого workstream `Фазы 7`: активный roadmap step теперь truthfully смещён в **Фазу 8: автоматические проверки, staging и production readiness**, где `tranche 1` baseline integrity contour уже materialized и подтверждён локальным static + runtime pass, а remaining open scope лежит уже в staging/deploy/backup/restore/rollback/monitoring contour.

Первые интеграционные шаги Фазы 3 и последующие foundational steps уже зафиксированы в репозитории:

- **notification slice v1** реализован и подтвержден как первый integration slice;
- **notification hardening v1** теперь тоже реализован и проверен как закрытый delivery result для notification-track;
- **order lifecycle notifications hardening v1.1** реализован поверх `order lifecycle notifications v1` как source-of-truth update для первого production-like customer-facing notification slice: trigger = `order.placed`, path = `subscriber → workflow → Notification Module`, canonical recipient в runtime по-прежнему = `order.email`, а anti-duplicate contract теперь опирается на existing notification storage, query-before-create dedupe и controlled skip с diagnostics при duplicate match;
- **payment track v1** реализован как **YooKassa-first** путь и ранее подтвержден end-to-end для текущего payment scope;
- **shipping track v1** реализован как backend-first **legacy provider** provider-aware multi-quote slice; **`provider_aware_v2` подтверждён storefront remediation step `2026-04-21` поверх уже существующего contour**: текущий checkout path по-прежнему использует [`/store/delivery/rates`](../medusa-agency-boilerplate/src/api/store/delivery/rates/route.ts) и [`/store/delivery/points`](../medusa-agency-boilerplate/src/api/store/delivery/points/route.ts), но storefront теперь truthfully refetch-ит rates и pickup points при изменении address fingerprint внутри той же корзины, не держит stale quote / stale point как silently selected, сохраняет recoverable UI state вместо исчезновения shipping option и отделяет canonical shopper mode key от реального `shipping_option.id` в persisted selection data;
- **checkout end-to-end validation v1** закрыт: подтвержден полный flow `shipping → hosted YooKassa payment → automatic return → review → order placement → confirmed order page`; ложный blocker вокруг `payment_collection` снят; targeted fix в [`payment-button/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/checkout/components/payment-button/index.tsx) больше не допускает вызов `placeOrder()` до hosted authorization, а targeted fix в [`cookies.ts`](../medusa-agency-boilerplate-storefront/src/lib/data/cookies.ts) меняет policy для cart cookie c `sameSite: "strict"` на `sameSite: "lax"` для корректного cross-site return.

Новый truthfully confirmed update `2026-04-21` для shipping roadmap:

- исторический legacy provider slice остается подтвержденным промежуточным результатом и не должен переписываться как будто его не было;
- но долгосрочный shipping direction больше не читается как `historical provider-aware`, потому что direct Yandex investigation показал расхождение между работающим `Yandex Delivery` API и непрозрачным legacy provider runtime path для `PVZ` / `dropoff` сценариев;
- текущий concrete workstream для shipping теперь truthfully смещён в проектирование и подготовку **собственного delivery-layer `delivery-hub`**, где первым adapter будет **`Yandex Delivery`**, а merchant-facing настройка должна жить в админке магазина без форка official admin;
- source-of-truth спецификация этого workstream зафиксирована в [delivery_hub_spec.md](./delivery_hub_spec.md).
- backend/admin/tests tranche-1 для `delivery-hub` уже materialized в `medusa-agency-boilerplate`: добавлены foundation module scaffold, encrypted connection storage, event log storage, provider registry, direct `Yandex Delivery` adapter, admin API routes `/admin/delivery/*`, middleware wiring, admin page `Settings -> Delivery`, minimal warehouse/domain surface, neutral store/public contract routes `/store/delivery/catalog`, `/store/delivery/quotes`, `/store/delivery/pickup-points`, `/store/delivery/pickup-windows`, минимальный cart delivery selection contract через `/store/delivery/selection`, следующий tranche-safe readiness bridge через `/store/delivery/readiness`, canonical backend shipping-option metadata scaffold для `deliveryhub` через [`src/modules/delivery-hub/shipping-option-contract.ts`](../medusa-agency-boilerplate/src/modules/delivery-hub/shipping-option-contract.ts), [`src/modules/delivery-hub/provider-surface.ts`](../medusa-agency-boilerplate/src/modules/delivery-hub/provider-surface.ts), [`src/modules/deliveryhub.ts`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts), registration в [`medusa-config.ts`](../medusa-agency-boilerplate/medusa-config.ts), а теперь и backend-only fulfillment-provider bridge scaffold через [`src/modules/delivery-hub/fulfillment-provider-bridge.ts`](../medusa-agency-boilerplate/src/modules/delivery-hub/fulfillment-provider-bridge.ts), который materializes явный Medusa-facing seam для validated `deliveryhub` selection/option payloads и future create-fulfillment handoff без live network, shipment automation, storefront wiring или checkout cutover; поверх него теперь materialized ещё и explicit backend-only order-side diagnostic seam для persisted/create-fulfillment input contract: helper [`buildDeliveryHubCreateFulfillmentBridgeDiagnostic()`](../medusa-agency-boilerplate/src/modules/delivery-hub/fulfillment-provider-bridge.ts) нормализует order/fulfillment/items alongside persisted `deliveryhub` fulfillment data, диагностирует missing/invalid fragments, provider drift и nested shape drift, возвращает blocked/ready diagnostic-only report и по-прежнему не инициирует shipment execution или network calls; следующим tranche-safe alignment шагом поверх того же pipeline теперь materialized explicit shared verdict helper [`buildDeliveryHubFulfillmentContractVerdict()`](../medusa-agency-boilerplate/src/modules/delivery-hub/fulfillment-provider-bridge.ts), который нормализует тот же provider-facing fulfillment contract без item/order scaffolding, а [`validateFulfillmentData()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:62) и [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:119) теперь опираются на один и тот же normalized contract verdict для valid/missing/drifted input без contract drift и без расширения execution surface; поверх intentional block в [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:119) теперь materialized ещё и узкий backend/admin-only execution-plan preview seam: helper [`buildDeliveryHubShipmentExecutionPlanPreview()`](../medusa-agency-boilerplate/src/modules/delivery-hub/fulfillment-provider-bridge.ts) поверх existing diagnostic verdict строит normalized provider execution plan для будущего `create_shipment`, добавляет redacted outbound request preview, отдельно фиксирует readiness verdict и blocked reasons, deterministic `execution_identity` preview (`provider_operation_label`, `provider_operation_reference`, `plan_fingerprint`, `execution_fingerprint`, `idempotency_key_preview`) строго из normalized execution plan без live calls, raw hash inputs или secret-bearing fragments, а теперь ещё и deterministic `shipment_result_preview`, который dry-run-only проецирует будущую neutral shipment-result normalization semantics (`current_mode`, `result_decision`, normalization target, identity/idempotency linkage, sanitized artifact presence summary, blocked materialization actions и explicit disabled confirmations) без provider response fetch, без shipment creation и без persistence; следующим узким tranche-safe шагом поверх этого же contour теперь materialized preview-only persistence/audit seam: тот же [`buildDeliveryHubShipmentExecutionPlanPreview()`](../medusa-agency-boilerplate/src/modules/delivery-hub/fulfillment-provider-bridge.ts) теперь dry-run-only materializes admin-safe `persistence_audit_preview` c redacted `metadata_patch`, shadow `execution_record`, idempotency/dedupe reservation field mapping, planned execution-status transitions, previewed audit/event-log entries, а также truthfully разделёнными `blocked` и `deferred` persistence actions без БД write-path, без shipment execution и без provider calls; поверх уже materialized execution-plan / persistence-audit / provider-dispatch / shipment-result contours теперь materialized ещё и deterministic backend/admin-only `fulfillment_application_preview`, который dry-run-only проецирует будущую neutral Medusa/domain application semantics (`current_mode=preview_only`, `application_decision`, `medusa_fulfillment_mutation_plan`, mutation target presence, identity/idempotency linkage, persistence linkage, blocked application actions и explicit confirmations, что order mutation / fulfillment persistence / shipment persistence / label persistence / event persistence / checkout cutover остаются disabled) без order writes, fulfillment writes, shipment writes, event writes или provider execution; следующим tranche-safe шагом поверх уже materialized execution-plan / dispatch / shipment-result / fulfillment-application contours теперь materialized ещё и admin-only `failure_handling_preview`, который deterministic dry-run-only проецирует future neutral failure classification / retry / compensation / manual-intervention semantics (`current_mode=preview_only`, `projected_retry_policy|no_live_failure_path`, narrow failure classes/reason buckets, identity/idempotency linkage, retry/compensation/manual-intervention posture, blocked failure actions и explicit confirmations что retry scheduling / rollback / compensation writes / provider re-dispatch / event persistence / order-fulfillment mutation / checkout cutover остаются disabled) без scheduler, без rollback logic, без compensation persistence и без provider calls; поверх уже существующего observability contour теперь materialized ещё и deterministic admin-only `execution_lifecycle_preview`, который собирает existing preview seams в neutral ordered phase timeline `preflight_eligibility → provider_dispatch → shipment_result_normalization → fulfillment_application → failure_handling`, фиксирует `current_mode=preview_only`, stable lifecycle status/readiness posture, concise identity/idempotency correlation summary, per-phase blocked reasons / disabled live actions / linked preview artifacts и explicit confirmations что orchestration scheduling / shipment execution / provider calls / persistence writes / retry scheduling / compensation writes / order-fulfillment mutation / checkout cutover остаются disabled; текущая [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:119) по-прежнему не делает live shipment path, checkout cutover, shopper/storefront changes или вмешательство в legacy provider flow; поверх уже существующего planner/preview contour теперь materialized расширенный admin-only execution-plan observability read model: service helper [`buildExecutionPlanObservabilityPreview()`](../medusa-agency-boilerplate/src/modules/delivery-hub/service.ts) поднимает operator-safe redacted shipment-planning snapshot поверх desired/deferred mode picture, helper [`buildDeliveryHubExecutionPlanObservabilityPreview()`](../medusa-agency-boilerplate/src/modules/delivery-hub/fulfillment-provider-bridge.ts) materializes diagnostic `execution_plan + execution_identity + outbound_payload_preview + persistence_audit_preview + provider_dispatch_preview + shipment_result_preview + failure_handling_preview + fulfillment_application_preview + execution_lifecycle_preview + blocked reasons + issues + shipment_execution` without live path, admin route [`GET /admin/delivery/execution-plan/preview`](../medusa-agency-boilerplate/src/api/admin/delivery/execution-plan/preview/route.ts) остаётся строго admin-only и продолжает route-level sanitation через [`shared.ts`](../medusa-agency-boilerplate/src/api/admin/delivery/shared.ts), а admin page [`page.tsx`](../medusa-agency-boilerplate/src/admin/routes/settings/delivery/page.tsx) теперь показывает ещё и read-only lifecycle status / phase sequence / identity summary / per-phase disabled actions рядом с read-only failure-handling verdict, shipment-result verdict, fulfillment application verdict, redacted deterministic identity snapshot, persistence/audit preview, preflight gate, dispatch preview, redacted outbound payload preview, readiness/blocked reasons и preserved shipment-execution block; read-only readiness preview seam для bridge assembly остаётся прежним: service helper [`buildFulfillmentBridgeReadinessPreview()`](../medusa-agency-boilerplate/src/modules/delivery-hub/service.ts) собирает diagnostic-only snapshot поверх existing planner/preview contour, admin route [`GET /admin/delivery/fulfillment-bridge/preview`](../medusa-agency-boilerplate/src/api/admin/delivery/fulfillment-bridge/preview/route.ts) возвращает sanitized structured bridge preview без order/cart mutations и без shipment execution, а admin page [`page.tsx`](../medusa-agency-boilerplate/src/admin/routes/settings/delivery/page.tsx) truthfully показывает operator-facing read-only readiness state для каждого mode_code; tranche-safe desired shipping-option planner/read-model через [`src/modules/delivery-hub/shipping-option-planner.ts`](../medusa-agency-boilerplate/src/modules/delivery-hub/shipping-option-planner.ts) + internal service bridge [`planDesiredShippingOptions()`](../medusa-agency-boilerplate/src/modules/delivery-hub/service.ts:177), read-only reconciliation/diff scaffold между desired `deliveryhub` projections и current Medusa shipping-option snapshots через [`src/modules/delivery-hub/shipping-option-reconciliation.ts`](../medusa-agency-boilerplate/src/modules/delivery-hub/shipping-option-reconciliation.ts) + internal helper [`reconcileShippingOptions()`](../medusa-agency-boilerplate/src/modules/delivery-hub/service.ts:257), materialized pure sync-operation plan builder [`src/modules/delivery-hub/shipping-option-sync-operation-plan.ts`](../medusa-agency-boilerplate/src/modules/delivery-hub/shipping-option-sync-operation-plan.ts) + service bridge [`buildShippingOptionSyncOperationPlan()`](../medusa-agency-boilerplate/src/modules/delivery-hub/service.ts:264), manual internal executor scaffold [`src/modules/delivery-hub/shipping-option-sync-executor.ts`](../medusa-agency-boilerplate/src/modules/delivery-hub/shipping-option-sync-executor.ts), internal Medusa mutation-port bridge [`src/modules/delivery-hub/shipping-option-medusa-mutation-port.ts`](../medusa-agency-boilerplate/src/modules/delivery-hub/shipping-option-medusa-mutation-port.ts), explicit admin/manual sync scaffold [`src/modules/delivery-hub/shipping-option-manual-sync.ts`](../medusa-agency-boilerplate/src/modules/delivery-hub/shipping-option-manual-sync.ts) + admin-only route [`POST /admin/delivery/shipping-options/sync`](../medusa-agency-boilerplate/src/api/admin/delivery/shipping-options/sync/route.ts), отдельный audit helper [`src/modules/delivery-hub/shipping-option-manual-sync-audit.ts`](../medusa-agency-boilerplate/src/modules/delivery-hub/shipping-option-manual-sync-audit.ts), который materializes truthful manual sync audit trail через existing [`delivery_event_logs`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/event-log-repository.ts), а теперь и operator-facing admin UI surface на [`Settings -> Delivery`](../medusa-agency-boilerplate/src/admin/routes/settings/delivery/page.tsx) с helper [`manual-sync.ts`](../medusa-agency-boilerplate/src/admin/routes/settings/delivery/manual-sync.ts): primary action запускает safe `dry_run`, secondary execute-path остаётся disabled без exact guard `deliveryhub:execute_shipping_option_sync` и explicit `service_zone_id` / `shipping_profile_id`, UI truthfully показывает `execution.mode`, reconciliation / operation-plan / execution summaries, shipping-option preview, fulfillment-bridge readiness preview и execution-plan observability preview вместе с persistence/audit dry-run snapshot, normalized shipment-result projection, fulfillment application mutation projection, failure-handling/compensation projection и новым lifecycle timeline projection без auto-refresh loop и без public/store leakage; при этом storefront cut-in, automatic shipping option sync/cutover, shipments и зрелая multi-provider orchestration всё ещё остаются deferred по [delivery_hub_spec.md](./delivery_hub_spec.md) §16.

#### Truthful tranche-1 status for `delivery-hub`

- `2026-04-23` first cutover package materialized **Delivery Hub default contract + legacy provider deprecation fence**: explicit default/new fulfillment contour now lives in [`fulfillment-contour-contract.ts`](../medusa-agency-boilerplate/src/modules/fulfillment-contour-contract.ts), [`medusa-config.ts`](../medusa-agency-boilerplate/medusa-config.ts) always registers `deliveryhub` with that default contour metadata, direct `Yandex Delivery` remains the Delivery Hub/Yandex adapter authority, and legacy provider is marked deprecated legacy compatibility only through code constants/comments plus env/docs contract. This package intentionally does not delete the removed legacy provider module, does not unblock [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:119), does not activate checkout writes/cutover, does not wire storefront save/clear/setShippingMethod for Delivery Hub, and does not touch the execution-ledger readiness contour.
- `2026-04-23` second safe cutover package materialized **Delivery Hub store selection write activation behind backend contract**: [`POST /store/delivery/selection`](../medusa-agency-boilerplate/src/api/store/delivery/selection/route.ts) now accepts only a strict neutral customer-safe selection contract (`provider_code`, `connection_id`, neutral `quote_type`, opaque `quote_reference`, quote summary, pickup point/window summary and optional `correlation_id`) and persists it through the existing cart metadata update seam under `metadata.delivery_hub.selection`; [`GET /store/delivery/selection`](../medusa-agency-boilerplate/src/api/store/delivery/selection/route.ts) and [`DELETE /store/delivery/selection`](../medusa-agency-boilerplate/src/api/store/delivery/selection/route.ts) remain the matching read/clear contract. Store schemas are strict against raw provider/Yandex fragments, nested metadata, credentials and secret-like fields; persisted/read selection now carries explicit customer-safe `provider_code` and `correlation_id` while still excluding raw provider responses, credentials, secret headers, `raw_reference`, `quote_key`, `metadata` and other provider internals. This backend package did not wire checkout UI to [`saveDeliveryHubSelection()`](../medusa-agency-boilerplate-storefront/src/lib/data/delivery-hub.ts:151), did not call/wire [`setShippingMethod()`](../medusa-agency-boilerplate-storefront/src/lib/data/cart.ts:226), did not unblock [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:119), did not remove legacy provider, did not require new env and did not change the execution-ledger activation contour.
- `2026-04-23` third safe storefront cut-in package started/enabled **Delivery Hub neutral selection save/clear behind the backend contract**: checkout shipping now exposes a controlled Delivery Hub selection cut-in block in [`shipping/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/checkout/components/shipping/index.tsx) that calls existing [`saveDeliveryHubSelection()`](../medusa-agency-boilerplate-storefront/src/lib/data/delivery-hub.ts:151) only after pure guard [`buildDeliveryHubSelectionSaveCutInPayload()`](../medusa-agency-boilerplate-storefront/src/lib/util/delivery-hub.ts) can shape a backend-ready neutral payload from existing rehearsal/preview inputs, and calls existing [`clearDeliveryHubSelection()`](../medusa-agency-boilerplate-storefront/src/lib/data/delivery-hub.ts:169) only through an explicit clear action for persisted/recent neutral selection state. The storefront cut-in still does not use Delivery Hub raw/Yandex provider payloads, raw offer ids, credentials, secret headers, arbitrary metadata or provider internals; the constructed save body is limited to cart id, connection id, neutral quote type, opaque quote reference, quote summary, pickup point and pickup window, while provider code is intentionally omitted so the backend validated/defaulted contract remains authoritative. This package still does not call [`setShippingMethod()`](../medusa-agency-boilerplate-storefront/src/lib/data/cart.ts:226) for Delivery Hub, does not unblock [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:119), does not enable live shipment execution, does not remove legacy provider, does not fork official Admin, does not add new env or secrets, and leaves full shipping-method commit/checkout cutover as a separate later package.
- `2026-04-23` fourth narrow storefront package materialized **Delivery Hub saved selection summary/reconciliation**: pure shopper-safe helper [`buildDeliveryHubSavedSelectionSummaryModel()`](../medusa-agency-boilerplate-storefront/src/lib/util/delivery-hub.ts) now summarizes persisted `metadata.delivery_hub.selection` together with readiness into a non-final UI model, and checkout surfaces it in both the open shipping block and shared [`ShippingSummary`](../medusa-agency-boilerplate-storefront/src/modules/checkout/components/shipping-summary/index.tsx). The summary shows only neutral fields — modality, quote amount/currency/ETA, pickup point name/address/code, pickup window, saved timestamp and safe correlation id — and explicitly labels the data as saved Delivery Hub metadata, not a committed Medusa shipping method and not fulfillment execution. Stale/invalid/not-ready/mismatched readiness states now show warning reconciliation messages and clear/save-again guidance instead of silent success; no automatic clear/mutation was added. [`checkout-summary/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/checkout/templates/checkout-summary/index.tsx) narrowly reads existing selection/readiness helpers so the sidebar can show the saved neutral state even when it is separate from the committed legacy shipping method. This package still does not call [`setShippingMethod()`](../medusa-agency-boilerplate-storefront/src/lib/data/cart.ts:226) for Delivery Hub, does not unblock [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:119), does not enable live shipment execution, does not remove legacy provider, does not fork official Admin, does not add new env/network requirements, and does not expose secrets/raw provider payloads/provider offer ids.
- `2026-04-23` fifth narrow storefront package materialized **Delivery Hub shipping-method commit cut-in behind saved neutral selection**: pure helper [`buildDeliveryHubCommitEligibilityModel()`](../medusa-agency-boilerplate-storefront/src/lib/util/delivery-hub.ts) now decides whether storefront may safely call [`setShippingMethod()`](../medusa-agency-boilerplate-storefront/src/lib/data/cart.ts:226) for Delivery Hub based only on persisted neutral selection, readiness reconciliation and already-available cart shipping options. The helper accepts commit only when a saved neutral selection exists, readiness is exactly `ready`, readiness selection still matches the persisted snapshot, and the cart already exposes the exact deterministic Delivery Hub shipping option id `deliveryhub:${quote_type}` under the Delivery Hub provider contour; stale/missing/mismatched selection, missing option and contour mismatch are explicitly blocked. Checkout [`shipping/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/checkout/components/shipping/index.tsx) now shows an explicit commit handoff state (`missing | blocked | ready | committed`) and exposes a dedicated commit CTA that uses only the matched shipping option id — no raw Yandex payloads, no raw offer ids, no `quote_key`, no `raw_reference`, no credentials, no secret headers and no arbitrary metadata are sent to commit. This activates the storefront `setShippingMethod()` path for Delivery Hub only behind the saved neutral selection prerequisite/handoff, while preserving legacy provider as the fallback/current path whenever Delivery Hub eligibility is not satisfied. The package still does not unblock [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:119), does not enable live shipment execution, does not remove legacy provider, does not fork official Admin, does not add new env/network requirements and does not treat saved metadata as shipment execution confirmation.
- `2026-04-23` sixth narrow backend package materialized **Delivery Hub fulfillment handoff assembly/preflight for blocked manual contour**: pure helper [`buildDeliveryHubFulfillmentHandoffSnapshot()`](../medusa-agency-boilerplate/src/modules/delivery-hub/fulfillment-provider-bridge.ts) now assembles a shopper-safe / ops-safe handoff artifact from the committed Delivery Hub shipping-method reference plus saved neutral selection / normalized fulfillment bridge data and existing order/fulfillment bridge context. The snapshot is intentionally limited to safe manual/external contour fields: provider code/id, connection id, quote type/reference, quote summary, pickup point/window summary, cart/order/fulfillment/shipping-option/location references, safe correlation id, safe timestamps and explicit blocked/manual contour flags. Handoff assembly is explicitly blocked for missing committed `shipping_option_id`, missing or invalid saved neutral selection, committed option mismatch, stale quote reference, connection/quote drift and unsupported shipping-option type/version; live provider execution still does not start from this path. [`buildDeliveryHubShipmentExecutionPlanPreview()`](../medusa-agency-boilerplate/src/modules/delivery-hub/fulfillment-provider-bridge.ts) now serializes the same handoff artifact inside its normalized preview payload when contract/preflight input is valid, and [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:119) now evaluates/logs this handoff preflight before still ending in the same blocked/manual provider result. The package does not call Yandex create-shipment, does not enable execution flags, does not remove legacy provider, does not fork Admin and does not add required env. Anti-leak boundary remains explicit: no credentials, no secret headers, no raw provider responses, no raw offer ids and no arbitrary nested metadata are serialized into the handoff artifact or logs.
- `2026-04-23` seventh narrow backend package materialized **Delivery Hub execution-ledger evidence artifact assembly preview behind manual contour**: pure helper [`buildDeliveryHubExecutionLedgerEvidenceArtifactAssembly()`](../medusa-agency-boilerplate/src/modules/delivery-hub/fulfillment-provider-bridge.ts) now assembles a testable execution-ledger evidence artifact strictly from the already blocked/manual handoff snapshot surface and returns `ready|blocked` with safe diagnostic reason instead of throwing false evidence-ready state when handoff prerequisites are absent or stale. The artifact intentionally keeps only manual/evidence-safe fields — provider code/id, connection id, quote type plus opaque `quote_reference_summary`, quote summary, pickup point/window summary, cart/order/fulfillment/shipping-option/location references, safe correlation id, artifact/handoff timestamps, explicit evidence status and blocked/manual contour markers including `ledger_persistence_enabled=false`. It explicitly does not serialize credentials, secret headers, raw provider responses, raw offer ids, arbitrary nested metadata or the full quote reference object. [`buildDeliveryHubShipmentExecutionPlanPreview()`](../medusa-agency-boilerplate/src/modules/delivery-hub/fulfillment-provider-bridge.ts) now includes the same evidence artifact assembly result under normalized preview output as a safe nested preview, while [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:119) logs only a redacted evidence summary and still ends in the same blocked/manual contour. This package does not activate execution-ledger persistence, does not change [`DeliveryHubExecutionLedgerRepositoryAssemblyPersistenceReadinessContour`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-repository-assembly-scaffold.ts:161), does not call Yandex, does not remove legacy provider, does not fork Admin and does not add required env.
- `2026-04-23` eighth narrow backend package materialized **Delivery Hub controlled fulfillment execution seam at the final direct-Yandex boundary**: new helper [`buildDeliveryHubControlledFulfillmentExecutionResult()`](../medusa-agency-boilerplate/src/modules/delivery-hub/fulfillment-execution-seam.ts) now evaluates the already assembled handoff snapshot plus optional committed connection lookup and returns a truthful structured result for [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:127) instead of the old unconditional provider throw once the normalized contract is ready. The runtime seam is explicitly controlled and still no-network: it uses only normalized fulfillment bridge data, committed handoff data and backend-resolved connection readiness, blocks missing handoff / missing PG lookup seam / missing connection / disabled connection / inactive connection / credentials-not-ready / unsupported provider / unsupported mode, and when all those prerequisites are satisfied it reaches only `dispatch_prepared_but_blocked` because the current neutral selection contract persisted only opaque [`quote_reference`](../medusa-agency-boilerplate/src/modules/delivery-hub/cart-selection.ts) and did not yet persist the provider execution reference required for real direct Yandex `create_shipment`. Result data truthfully recorded that boundary through `blocked_reason_code=provider_execution_reference_unavailable`, while anti-leak flags remained explicit (`credentials_included=false`, `raw_provider_payloads_included=false`, `raw_offer_ids_included=false`), no adapter/network call was performed, no execution-ledger persistence was performed, no required env was added, and legacy provider remained untouched as deprecated legacy compatibility surface in that package.
- `2026-04-23` ninth narrow backend package materialized **Delivery Hub backend-only persisted provider execution reference contract for direct Yandex handoff preparation**: [`buildDeliveryHubCartSelectionMetadata()`](../medusa-agency-boilerplate/src/modules/delivery-hub/cart-selection.ts) now persists a backend-only `backend_execution_reference` token inside `metadata.delivery_hub.selection` whenever backend write input includes `quote_key` (or an already assembled backend token), and [`readDeliveryHubCartSelection()`](../medusa-agency-boilerplate/src/modules/delivery-hub/cart-selection.ts) explicitly strips that field back out of the public/storefront selection surface. The persisted token is opaque at rest, validated/decrypted only on backend through [`readDeliveryHubCartSelectionBackendExecutionReference()`](../medusa-agency-boilerplate/src/modules/delivery-hub/cart-selection.ts) and [`decryptDeliveryHubProviderExecutionReference()`](../medusa-agency-boilerplate/src/modules/delivery-hub/cart-selection.ts), and is never admitted by strict store response schemas in [`shared.ts`](../medusa-agency-boilerplate/src/api/store/delivery/shared.ts). [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:127) now performs a backend-only cart lookup by committed `cart_id`, retrieves that persisted execution reference without exposing it to logs or shopper payloads, and feeds only the presence/absence verdict into [`buildDeliveryHubControlledFulfillmentExecutionResult()`](../medusa-agency-boilerplate/src/modules/delivery-hub/fulfillment-execution-seam.ts). As a result, the old final blocker `provider_execution_reference_unavailable` is now truthfully removed for valid persisted selections; the new last blocker becomes `provider_dispatch_not_materialized`, because the live direct Yandex `create_shipment` adapter call path is still intentionally not implemented in [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:127). This package still adds no required env, performs no network dispatch, persists no execution-ledger writes, exposes no raw offer ids, and does not touch deprecated legacy provider compatibility surface.
- `2026-04-23` tenth narrow backend package truthfully advanced **Direct Yandex dispatch materialization assessment behind the existing execution gate** without crossing the safe architecture boundary: [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:127) now explicitly reads optional env gate `DELIVERY_HUB_SHIPMENT_EXECUTION_ENABLED`, passes that state into [`buildDeliveryHubControlledFulfillmentExecutionResult()`](../medusa-agency-boilerplate/src/modules/delivery-hub/fulfillment-execution-seam.ts) and keeps the no-network path truthful in both contours. When the gate is disabled, controlled execution now says exactly that live dispatch remains blocked by the disabled gate; when the gate is enabled, the seam no longer hides behind a generic “adapter not materialized” statement and instead records the exact last backend-only blocker per mode. Boundary at that point: direct Yandex `create_shipment` still could not be materialized from the committed handoff because the persisted backend selection/execution-reference contract did not carry the provider-origin dispatch data required for live request assembly — for [`dropoff_point_to_pickup_point`](../medusa-agency-boilerplate/src/modules/delivery-hub/constants.ts:31) the missing field was `origin_point_id`, and for [`warehouse_to_pickup_point`](../medusa-agency-boilerplate/src/modules/delivery-hub/constants.ts:30) the missing backend-only provider origin was the resolved provider warehouse reference. No unsafe fallback to raw storefront payloads was introduced, no adapter network call was added, no execution-ledger write path was activated, no new required env was introduced, no secrets/raw offer ids were exposed, and deprecated legacy provider compatibility surface remained untouched.
- `2026-04-23` eleventh narrow backend package materialized **Delivery Hub backend-only persisted provider-origin dispatch context contract for direct Yandex handoff preparation**: quote generation now tokenizes the minimal provider-origin dispatch context inside the backend-issued opaque [`quote_reference`](../medusa-agency-boilerplate/src/modules/delivery-hub/cart-selection.ts) only when such context exists and `DELIVERY_HUB_ENCRYPTION_KEY` is configured. For [`dropoff_point_to_pickup_point`](../medusa-agency-boilerplate/src/modules/delivery-hub/constants.ts:31) the encrypted backend-only payload carries only `origin_point_id`; for [`warehouse_to_pickup_point`](../medusa-agency-boilerplate/src/modules/delivery-hub/constants.ts:30) it carries only the resolved `provider_warehouse_id`. [`buildDeliveryHubCartSelectionMetadata()`](../medusa-agency-boilerplate/src/modules/delivery-hub/cart-selection.ts) can now hydrate `backend_execution_reference` from that opaque quote reference during selection persistence, and [`readDeliveryHubProviderExecutionReferenceOriginContext()`](../medusa-agency-boilerplate/src/modules/delivery-hub/cart-selection.ts) exposes the context only to backend runtime. [`readDeliveryHubCartSelection()`](../medusa-agency-boilerplate/src/modules/delivery-hub/cart-selection.ts), store selection routes and shopper summary surfaces still strip backend-only fields and never return raw provider-origin ids as explicit public fields; logs in [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:127) expose only presence booleans, not `origin_point_id`, `provider_warehouse_id`, raw quote keys, credentials or raw provider payloads. The former final blocker `missing provider-origin dispatch context` is truthfully shifted for valid encrypted selections: with the optional execution gate disabled the seam remains blocked by the gate; with the gate enabled and context present it now reports that provider-origin context is available, but the no-network [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:127) still does not materialize the live direct Yandex adapter call. No live dispatch, execution-ledger persistence, new required env, official Admin patch, legacy provider deletion or public raw Yandex payload surface was introduced.
- `2026-04-23` next autonomous backend package materialized **Direct Yandex create-shipment payload materializer, preview-only/no-network**: new pure helper [`materializeYandexCreateShipmentPayloadPreview()`](../medusa-agency-boilerplate/src/modules/delivery-hub/adapters/yandex/create-shipment-materializer.ts:196) accepts typed backend-only execution inputs for [`warehouse_to_pickup_point`](../medusa-agency-boilerplate/src/modules/delivery-hub/constants.ts:30) and [`dropoff_point_to_pickup_point`](../medusa-agency-boilerplate/src/modules/delivery-hub/constants.ts:31), validates provider-origin dispatch context (`provider_warehouse_id` vs `origin_point_id`), destination PVZ, warehouse pickup interval, recipient/contact and package/items, and returns `ready|blocked` with explicit reason codes. The returned preview is intentionally redacted/safe: no credentials, no auth headers, no raw backend execution token, no raw provider payload and masked provider/internal references. Focused no-network coverage lives in [`delivery-hub-yandex-create-shipment-materializer.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub-yandex-create-shipment-materializer.unit.spec.ts), including ready warehouse/dropoff paths, wrong-origin blockers, missing pickup point/window/recipient/items blockers and anti-leak assertions. This package does not wire the helper into [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:135), does not perform Yandex adapter/network dispatch, does not persist execution ledger or shipment rows, does not mutate order/fulfillment state, does not touch storefront/Admin internals and does not add required env.
- `2026-04-23` previous autonomous backend package materialized **Yandex create-shipment payload materialization evidence inside the controlled execution seam**: [`buildDeliveryHubControlledFulfillmentExecutionResult()`](../medusa-agency-boilerplate/src/modules/delivery-hub/fulfillment-execution-seam.ts) now calls [`materializeYandexCreateShipmentPayloadPreview()`](../medusa-agency-boilerplate/src/modules/delivery-hub/adapters/yandex/create-shipment-materializer.ts:196) only when the seam has already reached the direct-provider dispatch-contract boundary and the committed contour makes that preview meaningful. The structured result now includes preview-only section `provider_payload_materialization` with safe fields `status`, `attempted`, `ready`, `blocked_reason_code`, `blocked_reason`, presence booleans for persisted execution reference/provider-origin dispatch context, redacted `preview_summary` flags (`source_type`, destination pickup point / pickup interval / recipient / packages / counts / connection mode / order reference / masked correlation presence) and explicit anti-leak confirmations (`credentials_included=false`, `auth_headers_included=false`, `raw_execution_token_included=false`, `raw_provider_payload_included=false`). [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:235) logs only that safe summary and still keeps runtime behavior intentionally inert: no live Yandex network call, no adapter invocation, no order/fulfillment mutation, no shipment persistence, no execution-ledger persistence activation and `live_adapter_call_performed=false` even when preview materialization is `ready`. Focused no-network coverage in [`delivery-hub-provider-validation.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub-provider-validation.unit.spec.ts) now locks dropoff/warehouse preview readiness, blocked missing-context/missing-interval cases, disabled-gate no-live-call behavior and anti-leak logging assertions.
- `2026-04-23` previous autonomous backend package materialized **direct Yandex dispatch port boundary/contract without live dispatch**: new narrow adapter-local helper [`buildYandexCreateShipmentDispatchPortContract()`](../medusa-agency-boilerplate/src/modules/delivery-hub/adapters/yandex/create-shipment-dispatch-port.ts:145) introduces an explicit direct-`create_shipment` runtime contract that remains intentionally non-executable (`implemented=false`, `dispatch_attempted=false`). [`buildDeliveryHubControlledFulfillmentExecutionResult()`](../medusa-agency-boilerplate/src/modules/delivery-hub/fulfillment-execution-seam.ts:468) now exposes truthful `provider_dispatch_port` summary alongside existing preview-only payload materialization, distinguishing at least three runtime states: execution gate disabled (`blocked_reason_code=execution_gate_disabled`), gate enabled on supported direct-Yandex contour but dispatch port still not implemented (`dispatch_port_not_implemented`), and unsupported provider/mode contours (`provider_not_supported | mode_not_supported`). [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:249) logs/passes only that safe readiness summary — no raw payloads, no adapter invocation, no Yandex network call, no shipment persistence, no execution-ledger persistence activation and no order/fulfillment mutation. Focused no-network coverage in [`delivery-hub-provider-validation.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub-provider-validation.unit.spec.ts) now additionally locks the new `provider_dispatch_port` surface, gate-disabled versus gate-enabled-but-not-implemented behavior, unsupported contour truthfulness and anti-leak log assertions.
- `2026-04-23` previous autonomous backend package materialized **direct Yandex executable create-shipment dispatch contract/builder/result normalizer without runtime wiring**: adapter-local helper [`buildYandexCreateShipmentDispatchRequest()`](../medusa-agency-boilerplate/src/modules/delivery-hub/adapters/yandex/create-shipment-dispatch-port.ts:179) now builds a real backend-only non-redacted `create_shipment` request from the same validated inputs used by preview materialization, while keeping the public preview object separate and redacted. The same layer now also exposes minimal reusable executor [`executeYandexCreateShipmentDispatch()`](../medusa-agency-boilerplate/src/modules/delivery-hub/adapters/yandex/create-shipment-dispatch-port.ts:211), which can call [`YandexDeliveryClient.post()`](../medusa-agency-boilerplate/src/modules/delivery-hub/adapters/yandex/client.ts:14) against adapter-local path [`YANDEX_CREATE_SHIPMENT_API_PATH`](../medusa-agency-boilerplate/src/modules/delivery-hub/adapters/yandex/create-shipment-dispatch-port.ts:10), plus redacted success/failure normalizer semantics returning only safe summary fields: accepted/succeeded booleans, status category, masked/presence-only correlation and provider shipment references, and label/doc presence flags. Focused coverage in [`delivery-hub-yandex-create-shipment-dispatch-port.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub-yandex-create-shipment-dispatch-port.unit.spec.ts:1) now locks warehouse/dropoff request builders, mocked executor success path, provider-failure normalization and anti-leak result assertions. That package intentionally did **not** wire the executor into [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:119), did **not** change [`fulfillment-execution-seam.ts`](../medusa-agency-boilerplate/src/modules/delivery-hub/fulfillment-execution-seam.ts) runtime behavior, did not persist shipment/execution rows, did not activate ledger writes, did not mutate orders/fulfillments and added no new required env.
- `2026-04-23` current autonomous backend package materialized **first gated direct Yandex runtime `create_shipment` dispatch wiring with redacted controlled result and still no persistence**: [`buildDeliveryHubControlledFulfillmentExecutionResult()`](../medusa-agency-boilerplate/src/modules/delivery-hub/fulfillment-execution-seam.ts:168) is now async and, together with [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:235), performs the first real direct Yandex network call only when all strict runtime conditions are simultaneously true — [`DELIVERY_HUB_SHIPMENT_EXECUTION_ENABLED`](./env_contract.md), committed provider is Yandex, mode is supported, backend-only provider execution reference is present, provider-origin dispatch context matches the mode, payload materialization preview is `ready`, and the committed connection is active/enabled with sealed credentials. All earlier blocked contours remain zero-call. Successful and failed executions now return a truthful redacted runtime surface through `status=dispatch_attempted`, `result_decision=dispatch_attempted_no_persistence`, safe [`provider_dispatch_result`](../medusa-agency-boilerplate/src/modules/delivery-hub/fulfillment-execution-seam.ts:137) and [`dispatch_result`](../medusa-agency-boilerplate/src/modules/delivery-hub/fulfillment-execution-seam.ts:138) summaries, while [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:249) logs only safe summary fields and never raw payloads, auth headers, execution tokens, quote keys or full provider responses. Focused coverage in [`delivery-hub-provider-validation.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub-provider-validation.unit.spec.ts:1) now locks gate-disabled zero-call behavior, mocked successful runtime dispatch for ready dropoff and ready warehouse contours, materializer-blocked zero-call behavior, controlled redacted failure normalization and anti-leak assertions on both result and logs. This tranche still does **not** add shipment persistence, does **not** activate execution-ledger persistence, does **not** add retries/compensation/webhooks/polling, does **not** mutate order or fulfillment state beyond the existing safe return surface, does **not** touch storefront/admin and adds no new required env.
- `2026-04-23` next narrow backend package materialized **first execution-ledger activation slice for the already gated direct Yandex runtime path plus persisted accepted shipment linkage**: runtime [`buildDeliveryHubControlledFulfillmentExecutionResult()`](../medusa-agency-boilerplate/src/modules/delivery-hub/fulfillment-execution-seam.ts:186) and [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:150) now wire the existing Postgres repository [`DeliveryHubExecutionLedgerPgRepository`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-pg-repository.ts:49) into the already materialized ready contour only around the narrow path `ready -> actual gated dispatch attempt -> accepted shipment result`. Before a live Yandex call, the runtime now creates or matches a canonical reservation from the existing deterministic execution identity/drafts and records redacted canonical progression `planned -> reserved -> dispatch_ready -> dispatch_inflight`; after a dispatch result, it records `dispatch_inflight -> result_received` and then truthfully diverges by outcome: accepted dispatches may continue through `application_ready -> completed` and create/update the safe shipment linkage, while failed dispatches are bounded at `failed_blocked` without creating a misleading shipment record. Replaying an already completed accepted execution identity is blocked before provider dispatch with `execution_ledger_replay_blocked`; replaying an already `failed_blocked` identity is blocked with `execution_ledger_failed_blocked` and explicitly does not auto-retry or become completed. Duplicate exact non-terminal reservations and drifted reservations remain pre-dispatch zero-call blockers (`execution_ledger_duplicate_execution` / `execution_ledger_drift_detected`) without shipment persistence and without misleading attempted/completed success flags. Runtime result surfaces truthfully expose ledger persistence booleans via [`dispatch_preparation`](../medusa-agency-boilerplate/src/modules/delivery-hub/fulfillment-execution-seam.ts:88), [`dispatch_result`](../medusa-agency-boilerplate/src/modules/delivery-hub/fulfillment-execution-seam.ts:140) and `contour`, while logs and shipment metadata remain anti-leak: no raw provider payloads, auth headers, execution tokens, quote keys or secrets are persisted or returned. Focused coverage in [`delivery-hub-provider-validation.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub-provider-validation.unit.spec.ts:1) now locks accepted replay, failed replay, duplicate exact non-terminal reservation, drift pre-dispatch blockers, failed no-shipment persistence and ready-contour accepted ledger/shipment linkage. This slice intentionally does **not** add polling/status sync, webhooks, retry scheduling/queue, compensation, checkout rewrite, broad admin/store contract changes or new required env.
- `2026-04-24` docs-only source-of-truth checkpoint codified the unified `delivery-hub` production-readiness roadmap in [`delivery_hub_spec.md`](./delivery_hub_spec.md) §16.17.1. Next technical tranche is **accepted shipment lifecycle read-model**, followed by **Yandex status polling v1**. This status entry is deliberately operational/documentary only: it does not claim polling, cancel, retry automation, broad shipment admin panel, checkout rewrite or new provider support is already implemented, and it preserves the existing execution gate, failed-blocked semantics, replay/duplicate/drift blockers and no-secret boundary.
- `2026-04-24` previous autonomous backend package materialized the first backend-only **accepted shipment lifecycle read-model** tranche: new helper [`shipment-lifecycle-read-model.ts`](../medusa-agency-boilerplate/src/modules/delivery-hub/shipment-lifecycle-read-model.ts) builds a sanitized snapshot over persisted accepted `delivery_shipments`, the execution ledger record and existing controlled fulfillment execution context, while [`getDeliveryShipmentByExecutionReference()`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/shipments-repository.ts) allows safe readback by execution reference and [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:150) now attaches `accepted_shipment_lifecycle` to the backend-only controlled result/log summary. Accepted snapshots are emitted only for persisted `outcome=accepted`, `status=dispatch_accepted`, `accepted=true`, `succeeded=true` shipment records; provider-failed, failed-blocked, completed replay, duplicate exact non-terminal and drifted ledger contours are explicitly classified as non-accepted and do not expose shipment records. The snapshot includes only redacted/safe fields: internal shipment id when present, provider/mode/status summary already persisted, accepted dispatch markers, safe ledger linkage/reference previews, timestamps and order/fulfillment/cart/shipping-option/location context references already present in the controlled result. It deliberately includes no raw provider payloads, auth headers, credentials, raw Yandex response body, quote key, raw provider shipment id or raw provider correlation id. Focused coverage in [`delivery-hub-provider-validation.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub-provider-validation.unit.spec.ts) now locks accepted lifecycle shape, provider-failed/no-shipment shape, replay/failed-blocked/duplicate/drift non-accepted classifications and anti-leak assertions. This tranche did **not** add Yandex status polling, provider status API calls, webhooks, cancel, retry scheduler/queue, checkout rewrite, broad admin/store API surface or new required env.
- `2026-04-24` current autonomous backend package materialized **Yandex status polling v1** as a narrow backend-only read/update foundation for already accepted persisted shipments, with follow-up correctness hardening from review. Adapter-local helper [`shipment-status.ts`](../medusa-agency-boilerplate/src/modules/delivery-hub/adapters/yandex/shipment-status.ts) now builds a conservative Yandex status request boundary, calls [`YandexDeliveryClient.post()`](../medusa-agency-boilerplate/src/modules/delivery-hub/adapters/yandex/client.ts:14) through `executeYandexShipmentStatusRefresh`, and normalizes provider status into a small neutral summary (`accepted`, `in_transit`, `ready_for_pickup`, `delivered`, `cancelled`, `failed`, `returned`, `unknown`) with unknown provider states explicit and safe. Backend helper [`refreshDeliveryHubAcceptedShipmentStatus()`](../medusa-agency-boilerplate/src/modules/delivery-hub/shipment-status-polling.ts:102) is gated by the accepted shipment lifecycle read-model plus persisted `delivery_shipments` criteria, active/sealed Yandex connection readiness and a real backend-only raw provider shipment reference; rows that only have `provider_shipment_reference_present=true` but no backend-only raw reference are blocked before `/shipments/info` with `provider_shipment_reference_required` and do not fall back to the internal execution reference. Failed, replay, duplicate, drift, blocked and absent/non-accepted paths also return blocked status-refresh summaries with zero provider calls. Shipment storage now supports narrow redacted update markers through [`recordDeliveryShipmentStatusUpdate()`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/shipments-repository.ts): only `provider_status_summary`, `status_refresh_outcome`, `status_refreshed_at` and a redacted metadata marker are written, and only for accepted persisted rows. [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:150) attaches the backend-only `accepted_shipment_status_refresh` result after accepted lifecycle evaluation; this is not a background scheduler, not a webhook, not a retry/cancel path, and not a public admin/store API. Focused coverage in [`delivery-hub-provider-validation.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub-provider-validation.unit.spec.ts) locks accepted refresh provider call/persistence only when backend-only provider reference is available, exact `/shipments/info` `shipment_id` source, accepted missing-raw-reference zero-call blocking, non-accepted zero-call blocking, unknown status normalization without corrupting the accepted snapshot, and anti-leak assertions for raw provider response/auth/token/quote/provider identifiers. This tranche adds no required env and still leaves broad shipment admin panel, cancel, manual retry policy, webhooks, scheduler/queue, checkout rewrite and new providers for later scoped work.
- `2026-04-24` next narrow reviewable slice materialized the first **admin/backend shipment operations visibility surface** for accepted shipment lifecycle plus safe manual status-refresh foundation. New backend view-model [`admin-shipment-operations.ts`](../medusa-agency-boilerplate/src/modules/delivery-hub/admin-shipment-operations.ts) projects a single `execution_reference` into a redacted operator snapshot: accepted lifecycle classification, safe provider/mode/status summary, current neutral polling summary when present, safe ledger linkage previews/counts, timestamps, action posture and blocked reason for non-accepted or non-refreshable cases. [`DeliveryHubService`](../medusa-agency-boilerplate/src/modules/delivery-hub/service.ts) now exposes admin-only snapshot and refresh methods over existing shipment/ledger/connection storage, and narrow admin routes [`GET /admin/delivery/shipments/:execution_reference/operations`](../medusa-agency-boilerplate/src/api/admin/delivery/shipments/[execution_reference]/operations/route.ts) plus [`POST /admin/delivery/shipments/:execution_reference/operations/refresh-status`](../medusa-agency-boilerplate/src/api/admin/delivery/shipments/[execution_reference]/operations/refresh-status/route.ts) return only response-boundary-sanitized fields through [`shared.ts`](../medusa-agency-boilerplate/src/api/admin/delivery/shared.ts). The refresh route intentionally reuses the accepted-only guard in [`refreshDeliveryHubAcceptedShipmentStatus()`](../medusa-agency-boilerplate/src/modules/delivery-hub/shipment-status-polling.ts:102): accepted persisted shipment, active/sealed Yandex connection and backend-only raw provider shipment reference are required before `/shipments/info`; missing provider reference and all non-accepted states remain zero-call. This slice deliberately did **not** add cancel/retry/webhooks/scheduler/background worker, did **not** expose storefront/store APIs and did **not** return raw provider payloads, auth headers, credentials, quote keys, raw provider shipment ids or raw execution secrets. Focused coverage in [`delivery-hub-admin-shipment-operations.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub-admin-shipment-operations.unit.spec.ts) locks accepted view-model shape, non-accepted blocking, backend-only refresh reference usage, missing-reference zero-call behavior and anti-leak response/view-model assertions.
- `2026-04-24` current narrow React Admin/page-state tranche materialized **shipment operations lookup on Settings -> Delivery** over those existing admin endpoints. [`page-state.ts`](../medusa-agency-boilerplate/src/admin/routes/settings/delivery/page-state.ts) now includes shipment-operations form/input state, safe snapshot/refresh response types, endpoint URL builders for [`GET /admin/delivery/shipments/:execution_reference/operations`](../medusa-agency-boilerplate/src/api/admin/delivery/shipments/[execution_reference]/operations/route.ts) and [`POST /admin/delivery/shipments/:execution_reference/operations/refresh-status`](../medusa-agency-boilerplate/src/api/admin/delivery/shipments/[execution_reference]/operations/refresh-status/route.ts), refresh request body helper and derived display/action posture model. [`page.tsx`](../medusa-agency-boilerplate/src/admin/routes/settings/delivery/page.tsx) now adds a minimal `Shipment operations lookup` section: operator can paste one `execution_reference`, load the safe snapshot, see lifecycle classification, accepted flag, provider/mode/dispatch summary, current status refresh outcome, ledger counts/previews, timestamps/context and blocked/action posture, and trigger `Refresh status` only when backend/view-model marks it available. Focused no-network coverage in [`page-state.unit.spec.ts`](../medusa-agency-boilerplate/src/admin/routes/settings/delivery/__tests__/page-state.unit.spec.ts) now locks endpoint URL encoding, refresh body shaping, accepted/blocked display models, disabled refresh posture and anti-leak display behavior. This tranche deliberately does **not** add cancel/retry buttons, webhook/scheduler/background polling, broad shipment dashboard/table/filter/export, storefront/store APIs, checkout rewrite, new provider support or new required env, and does not expose raw provider ids/auth/tokens/quotes/execution secrets in the display model.
- `2026-04-24` current narrow backend/admin tranche materialized **Cancel policy/path v1 for accepted Delivery Hub shipments** without broadening shipment operations scope. New policy helper [`shipment-cancel-policy.ts`](../medusa-agency-boilerplate/src/modules/delivery-hub/shipment-cancel-policy.ts) exposes accepted-Yandex-only readiness through the existing operations snapshot and executes a manual cancel only after all gates pass: accepted lifecycle snapshot, persisted accepted `delivery_shipments` row, supported Yandex provider/connection, active/enabled/sealed credentials posture, backend-only raw provider shipment reference, and non-terminal/non-cancelled neutral status. It blocks non-accepted, missing-reference, missing/not-ready connection, unsupported provider, already-cancelled, delivered/returned/failed terminal states before provider calls, so repeated/already-cancelled posture is truthful and not faked as a new success. New adapter seam [`shipment-cancel.ts`](../medusa-agency-boilerplate/src/modules/delivery-hub/adapters/yandex/shipment-cancel.ts) uses direct-Yandex client conventions and returns only sanitized summaries; local code/docs did not contain an authoritative Yandex cancel contract, so this seam is documented and typed as `semantics_certainty=adapter_boundary_mocked_only` around conservative path `/shipments/cancel` until live provider validation. [`DeliveryHubService.cancelAdminShipmentOperationsShipment()`](../medusa-agency-boilerplate/src/modules/delivery-hub/service.ts:423), response-boundary sanitizer [`sanitizeAdminDeliveryAdminShipmentOperationsCancelResponse()`](../medusa-agency-boilerplate/src/api/admin/delivery/shared.ts), and [`POST /admin/delivery/shipments/:execution_reference/operations/cancel`](../medusa-agency-boilerplate/src/api/admin/delivery/shipments/[execution_reference]/operations/cancel/route.ts) wire an explicit admin-only manual action that returns a safe cancel result plus refreshed operations snapshot. The React Admin shipment operations block now builds the cancel endpoint, renders cancel readiness/result rows and enables `Cancel shipment` only when backend action posture/readiness says available and no request is in flight. Focused no-network coverage in [`delivery-hub-admin-shipment-operations.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub-admin-shipment-operations.unit.spec.ts) and [`page-state.unit.spec.ts`](../medusa-agency-boilerplate/src/admin/routes/settings/delivery/__tests__/page-state.unit.spec.ts) locks allowed accepted cancel, blocked non-accepted/terminal/already-cancelled/missing-reference contours, sanitized provider failure, endpoint URL encoding, UI disabled/available posture and anti-leak assertions for provider ids, auth/tokens, quotes, raw bodies and execution secrets.
- `2026-04-24` current narrow backend/admin tranche additionally materialized **Manual retry policy/path v1 for failed/blocked shipment execution** as a strict readiness-first backend/admin-only action with blocked-only execution semantics. New helper [`shipment-retry-policy.ts`](../medusa-agency-boilerplate/src/modules/delivery-hub/shipment-retry-policy.ts) classifies retry readiness and allows retry only for a narrow safe failed/blocked contour (non-accepted + non-duplicate/drift lifecycle, non-terminal/non-in-progress neutral status, execution-ledger present in `failed_blocked`, transition history present, idempotency linkage present, persisted diagnostic execution plan present, no accepted shipment duplication, no provider shipment reference marker). Retry is blocked for accepted/cancelled/delivered/returned/unknown/in_transit/ready_for_pickup states, missing ledger/idempotency/diagnostic plan, duplicate/drift contours, and any persisted accepted/provider-reference duplicate risk. Admin action path [`DeliveryHubService.retryAdminShipmentOperationsExecution()`](../medusa-agency-boilerplate/src/modules/delivery-hub/service.ts:469), route [`POST /admin/delivery/shipments/:execution_reference/operations/retry`](../medusa-agency-boilerplate/src/api/admin/delivery/shipments/[execution_reference]/operations/retry/route.ts), middleware auth registration in [`middlewares.ts`](../medusa-agency-boilerplate/src/api/middlewares.ts), and response-boundary sanitizer [`sanitizeAdminDeliveryAdminShipmentOperationsRetryResponse()`](../medusa-agency-boilerplate/src/api/admin/delivery/shared.ts) are now wired into the existing shipment operations contour. Snapshot/read-model exposure via [`admin-shipment-operations.ts`](../medusa-agency-boilerplate/src/modules/delivery-hub/admin-shipment-operations.ts), page-state endpoint helper/render posture in [`page-state.ts`](../medusa-agency-boilerplate/src/admin/routes/settings/delivery/page-state.ts), and a minimal guarded Retry button in [`page.tsx`](../medusa-agency-boilerplate/src/admin/routes/settings/delivery/page.tsx) are materialized; button is enabled only when backend readiness/action posture says retry is available and no conflicting request is in flight. Execution semantics stay truthful/readiness-only in this v1: even when readiness passes, retry result remains blocked with `provider_call_attempted=false` and `blocked_reason_code=retry_execution_not_materialized` to avoid weakening deterministic execution identity, execution-ledger/idempotency guards, or creating duplicate shipment risk. Focused no-network coverage in [`delivery-hub-shipment-retry-policy.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub-shipment-retry-policy.unit.spec.ts), [`delivery-hub-admin-shipment-operations.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub-admin-shipment-operations.unit.spec.ts), and [`page-state.unit.spec.ts`](../medusa-agency-boilerplate/src/admin/routes/settings/delivery/__tests__/page-state.unit.spec.ts) now locks allowed/blocked readiness contours, duplicate/drift/idempotency blockers, middleware admin auth registration, retry URL encoding, UI disabled/available posture and anti-leak assertions. This retry v1 tranche deliberately does **not** add live provider redispatch, scheduler/background worker/queue, webhooks, broad shipment dashboard/table/filter/export, storefront/store shipment operations, checkout rewrite, new provider support, new required env, live-provider retry guarantee or commits.
- `2026-04-24` next narrow safety-first operations tranche materialized **manual-only Yandex provider-contract validation harness + runbook/evidence contour** without changing production runtime semantics: script [`delivery-hub-yandex-provider-contract-validation.ts`](../medusa-agency-boilerplate/src/scripts/delivery-hub-yandex-provider-contract-validation.ts) now provides default plan/dry-run output, explicit live opt-in gate `DELIVERY_HUB_YANDEX_PROVIDER_CONTRACT_VALIDATION_LIVE_ENABLED`, explicit confirm gate `--live-confirm=I_UNDERSTAND_LIVE_PROVIDER_CALLS`, fail-closed handling for missing live prerequisites (including missing `DELIVERY_HUB_ENCRYPTION_KEY` and not-ready/missing connection), and redacted structured evidence summaries through [`provider-contract-validation-evidence.ts`](../medusa-agency-boilerplate/src/modules/delivery-hub/provider-contract-validation-evidence.ts). Canonical operator/readme surface is [`delivery_hub_yandex_provider_contract_validation_runbook.md`](./delivery_hub_yandex_provider_contract_validation_runbook.md), plus script note in [`src/scripts/README.md`](../medusa-agency-boilerplate/src/scripts/README.md). Focused no-network coverage now includes [`delivery-hub-provider-contract-validation-evidence.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub-provider-contract-validation-evidence.unit.spec.ts) and [`delivery-hub-yandex-provider-contract-validation-harness.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub-yandex-provider-contract-validation-harness.unit.spec.ts), locking dry-run gating, explicit live opt-in requirement, fail-closed behavior and anti-leak evidence shape. This tranche intentionally does **not** execute real live provider calls automatically, does **not** collect live evidence by itself, does **not** materialize production retry redispatch, and does **not** add scheduler/background queue/webhooks/broad dashboard/storefront checkout cutover/new provider/required startup env/commit.
- `2026-04-24` Tranche B controlled validation session was executed through the committed harness/runbook with redacted evidence capture in [`delivery_hub_yandex_provider_contract_validation_evidence_20260424.md`](./delivery_hub_yandex_provider_contract_validation_evidence_20260424.md). Result is truthfully **blocked preflight evidence** (no live provider calls): plan-mode matrix across all runbook operations confirms fail-closed `blocked_default_dry_run`; live preflight gates confirm blocked outcomes for missing live opt-in, missing/invalid confirm, missing encryption key, and missing eligible ready Yandex connection (`blocked_connection_not_found`) when progressing to connection checks. Live semantics for create/status/cancel/repeated-cancel/missing-invalid-reference remain unproven in this environment until prerequisites exist; retry redispatch remains not materialized/not proven. This docs/evidence tranche does not change production behavior, does not enable scheduler/webhooks/background queues, and does not introduce commits.
- `2026-04-27` local runtime/config unblock for Delivery Hub testing completed: dev-only `DELIVERY_HUB_ENCRYPTION_KEY` is now present only in untracked local env files and is passed into `medusa-backend` via [`docker-compose.yml`](../docker-compose.yml); backend container was force-recreated and confirmed healthy; safe dummy-token credentials smoke proved sealed `dh.v1` envelope storage without plaintext leakage and cleaned up the test record; no live Yandex calls were executed. Admin Docker HMR was also stabilized by fixed Vite HMR `5173` config in [`medusa-config.ts`](../medusa-agency-boilerplate/medusa-config.ts) plus port mapping in [`docker-compose.yml`](../docker-compose.yml); `http://localhost:9000/app/login`, `http://localhost:9000/app/settings/delivery`, `/app/@vite/client` and Admin virtual modules were verified locally, and the previous random-port HMR `ERR_CONNECTION_REFUSED` noise did not reproduce. The next UI step is to enter a **new** test Yandex token only in **Settings → Delivery → Create Yandex connection → Write-only token**; any token previously pasted into chat must be considered compromised if real.
- `2026-04-27` follow-up `Test connection` 502 diagnosis/remediation completed without new proactive Yandex calls: existing backend logs for manual `POST /admin/delivery/connections/:id/test` showed repeated provider response `404` with safe message `No route for URL` on adapter path `/pickup-points/list`, which backend mapped to admin HTTP `502`; token/header data was redacted, and connection safe metadata showed `mode=test`, `status=error`, `enabled=false`, `credentials_state=sealed`, `credentials_present=true`, `credentials_last_error_code=DELIVERY_HUB_PROVIDER_ERROR`. Root cause is a provider route/base-host mismatch for the manual test credentials rather than encryption, admin auth, response-boundary leak or React Router warning. The Yandex client now resolves API base URL by connection mode: `test` uses sandbox/test host, `live` uses production host, with a narrow allowlisted `config.api_base_url` override exposed in the Admin **Yandex API host** selector; provider `404 No route for URL` is classified as `provider_route_mismatch` in safe diagnostics. No raw token/provider secret was documented or persisted.
  Follow-up after backend restart: safe local DB inspection showed no `delivery_hub_connections`/`delivery_hub_warehouses` tables yet, so the visible `yandexTestname` form state was unsaved UI input, not a persisted connection. The previous `DELIVERY_HUB_CONNECTION_REQUIRED` came from the Admin UI guard for missing `activeConnectionId`, and `POST /admin/delivery/test-quote` `400` is local validation when `connection_id`, `destination_point_id`, `warehouse_id` or `origin_point_id` is missing; it is not a provider/Yandex live call. Admin UX now blocks **Test connection** until successful **Create connection** and blocks **Test quote** until saved connection + mode-specific required fields are filled, with focused no-network tests and docs updated.
  Second follow-up after saving the Yandex connection: safe DB/event-log inspection confirmed persisted connection `yandexTestname` with `credentials_present=true`, sandbox/test `api_base_url`, `mode=test`, default warehouse mapping present, and latest manual `connection_test` reaching Yandex on `POST /pickup-points/list`; provider returned `403`, not local validation/encryption failure and not the earlier `404 No route for URL` route mismatch. Backend still maps provider `401/403` to admin-facing `DELIVERY_HUB_CREDENTIALS_INVALID · HTTP 401`, which is correct as credential/account/API-access failure normalization, but the Yandex client now adds safer diagnostics for Yandex HTML access-block pages via `error_category=provider_access_blocked`, an operator hint, and sanitized response shape instead of persisting raw HTML body. Focused no-network regression coverage was updated and passed.
  Third follow-up from the user-provided successful screenshot plus safe runtime event-log/connection inspection: the latest `connection_test` at `2026-04-27T10:33:48Z` is now **success/provider ok** on the documented direct-Yandex sandbox Other-day endpoint family, with persisted connection `mode=test`, `status=active`, `credentials_state=sealed`, sandbox/test `api_base_url=https://b2b.taxi.tst.yandex.net/api/b2b/platform`, `provider_status=ok`, `include_pickup_points=true`, `pickup_points_count=1079` and a safe correlation id. This means the endpoint/base-path normalization and saved connection are now confirmed through Yandex sandbox/test host. `pickup_points_count=1079` is a provider-side pickup-point catalog count observed during **Test connection** diagnostics, not a quote, shipment, or delivery price confirmation. No raw token, auth header, request body or raw provider body was printed or documented; no **Test quote**, create-shipment, status/cancel, retry or other live provider flow was executed automatically.
  Fourth follow-up for the user question "Destination point id — where do I get it": existing store route `GET /store/delivery/pickup-points` already existed but there was no operator-facing admin picker/list near **Test quote**. A minimal admin-only lookup is now materialized: [`GET /admin/delivery/pickup-points`](../medusa-agency-boilerplate/src/api/admin/delivery/pickup-points/route.ts) requires admin auth and validated query (`connection_id`, optional `city/country_code/limit`), reuses existing adapter [`listPickupPoints()`](../medusa-agency-boilerplate/src/modules/delivery-hub/adapters/types.ts:51), caps output to max `50`, returns only sanitized fields `id/code/name/address/city/available_for_dropoff/coordinates` plus count/correlation summary, and logs only summary counts. [`Settings -> Delivery`](../medusa-agency-boilerplate/src/admin/routes/settings/delivery/page.tsx) now includes a **Pickup point lookup** block with **Load pickup points**, **Use as destination**, and dropoff-only **Use as origin** buttons. This is operator-triggered only; it does not run **Test quote**, shipment create/cancel/status, retry or storefront checkout actions automatically, and it does not expose raw token/auth headers/raw provider body.
  Fifth follow-up for confirmed `warehouse_to_pickup_point` Test quote diagnosis: adapter [`listPickupWindows()`](../medusa-agency-boilerplate/src/modules/delivery-hub/adapters/yandex/index.ts:53) now calls documented direct-Yandex Other-day [`/offers/info`](../medusa-agency-boilerplate/src/modules/delivery-hub/adapters/yandex/endpoints.ts:19) with non-empty `places` derived from package summary and no longer sends `places: []`; [`quoteWarehouseToPickupPoint()`](../medusa-agency-boilerplate/src/modules/delivery-hub/adapters/yandex/index.ts:69) no longer performs a hidden pickup-window pre-call when `interval_utc` is absent. Admin-only [`GET /admin/delivery/pickup-windows`](../medusa-agency-boilerplate/src/api/admin/delivery/pickup-windows/route.ts:1) is materialized with validated query (`connection_id`, optional `warehouse_id`, `destination_point_id`, `limit`), capped sanitized output (`windows[].date/time_from/time_to/interval_utc/label`, counts, `correlation_id`) and safe summary logging. Follow-up reconciliation with Yandex Other-day `/offers/create` docs changed the warehouse Test quote guard again: **Test quote** for `warehouse_to_pickup_point` now requires saved connection, destination PVZ and saved warehouse/provider source, but no longer requires pickup windows or `interval_utc`; pickup windows lookup remains optional advanced diagnostics/helper. Operator basic path is now: **3. Найти ПВЗ** → **Использовать как destination** → **4. Проверить стоимость доставки**. No automatic Test quote, create/cancel/status shipment, retry, token/header output or raw provider response was executed.
  Sixth follow-up for **Pickup windows lookup** provider `400`: safe latest event log showed documented sandbox base `https://b2b.taxi.tst.yandex.net/api/b2b/platform`, path `/offers/info`, non-empty sanitized `places`, source/destination platform station ids present, provider status `400`, and safe provider code/message `no_delivery_options · No delivery options for interval`. Public Other-day docs for `POST /api/b2b/platform/offers/info` show `last_mile_policy` query defaults to `time_interval`; for PVZ delivery windows it must be `self_pickup`, and successful response shape is `offers[].from/to`, not legacy/internal `options[].interval_utc`. Local fix: [`listPickupWindows()`](../medusa-agency-boilerplate/src/modules/delivery-hub/adapters/yandex/index.ts:53) now calls `/offers/info?last_mile_policy=self_pickup`, extracts `offers`, and [`mapYandexPickupWindow()`](../medusa-agency-boilerplate/src/modules/delivery-hub/adapters/yandex/mapper.ts:34) normalizes documented UTC `from/to` into neutral `date/time_from/time_to/interval_utc/label`; no-network contract tests were updated. After `/offers/create` reconciliation, absence of a future pickup window no longer disables the warehouse Test quote UI; it only limits optional interval-specific diagnostics. No raw token/auth header/raw provider response/ciphertext was printed, and no Test quote or shipment operation was executed automatically.
- `2026-04-27` текущий tranche-safe backend/storefront-facing smoke contour materialized без checkout cutover: existing neutral store quote route [`GET /store/delivery/quotes`](../medusa-agency-boilerplate/src/api/store/delivery/quotes/route.ts) сохранён, а рядом добавлен нейтральный JSON-body smoke endpoint [`POST /store/delivery/quotes`](../medusa-agency-boilerplate/src/api/store/delivery/quotes/route.ts), который принимает только Delivery Hub neutral fields (`connection_id`, `mode_code`, `destination_point_id`, optional `origin_point_id|warehouse_id|interval_utc|items|currency_code`), now has explicit Medusa body middleware in [`middlewares.ts`](../medusa-agency-boilerplate/src/api/middlewares.ts), and fails closed with controlled `DELIVERY_HUB_VALIDATION_ERROR` if `req.validatedBody` is absent or raw/provider-specific fields are submitted. Successful route path calls backend [`listStoreQuotes()`](../medusa-agency-boilerplate/src/modules/delivery-hub/service.ts), returns only neutral quotes with opaque `quote_reference` and safe diagnostics (`correlation_id`, `checkout_source_of_truth=unchanged`, `contour=delivery_hub_storefront_preview`). Selection route [`POST /store/delivery/selection`](../medusa-agency-boilerplate/src/api/store/delivery/selection/route.ts) saves neutral selection in cart metadata via existing helper and also returns safe smoke diagnostics; [`DELETE /store/delivery/selection`](../medusa-agency-boilerplate/src/api/store/delivery/selection/route.ts) confirms unchanged checkout source-of-truth on clear. Response schemas in [`shared.ts`](../medusa-agency-boilerplate/src/api/store/delivery/shared.ts) remain strict and still reject `raw_reference`, raw provider/Yandex fragments, auth headers, tokens, ciphertext, backend execution references, merchant credentials and arbitrary provider metadata. Smoke harness [`delivery-hub-storefront-neutral-smoke.ts`](../medusa-agency-boilerplate/src/scripts/delivery-hub-storefront-neutral-smoke.ts) and npm alias `delivery:store-smoke` in [`package.json`](../medusa-agency-boilerplate/package.json) now document env-var invocation as canonical because local Medusa `exec` rejects unknown script CLI args; the harness safe summary includes `canonical_invocation="env_vars"` and never prints publishable key values. Safe connection investigation found two saved Yandex test connections, both with credentials present; `60208aa4-01e7-45a1-ba53-c8be5506bd86` revalidated ok but later failed quote with provider `403` and was rematerialized invalid, while `30e7b02b-71de-42d8-8587-86780c2850b8` revalidated ok, was safely enabled after `active+sealed`, and became the working public-ready test connection. Live/local store-neutral smoke then **passed** without checkout cutover: dropoff `dropoff_point_to_pickup_point` with cart `cart_01KQ82YXCRW3QQ01GP9CK5RCT8`, origin `019d2a9da5877011a771b75e903f3039`, destination `e1139f6d-e34f-47a9-a55f-31f032a861a6`, currency `RUB`, one item (`500g`, price `2000`) returned quote HTTP `200`, `quotes_count=13`, first price `181.9 rub`, and selection HTTP `200`, `selection.saved=true`, `checkout_source_of_truth="unchanged"`, correlation `9f2cdca3-6b4b-4275-903d-c739429ded34`. Optional warehouse smoke with saved warehouse `fa279b9d-316d-45c2-aa8d-aa92a77d15ba` also passed: quote HTTP `200`, `quotes_count=4`, first price `181.9 rub`, selection HTTP `200`, unchanged source-of-truth, correlation `802348f7-21b3-4389-87f8-5f81a6fc9125`. No publishable key value, token, auth header, ciphertext or raw provider body was printed; no checkout cutover, legacy delivery action or shipment create/cancel/status/retry call was made. Focused no-network coverage in [`delivery-hub-store.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub-store.unit.spec.ts), [`delivery-hub-cart-selection.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub-cart-selection.unit.spec.ts), [`delivery-hub-storefront-neutral-smoke-harness.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub-storefront-neutral-smoke-harness.unit.spec.ts) and [`delivery-hub-tranche1-coverage.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub-tranche1-coverage.unit.spec.ts) locks quote/selection shape, fail-closed POST body behavior, env-var canonical invocation, no-secret leakage, stale invalid→sealed recovery after successful safe revalidation, neutral selection persistence, checkout source-of-truth unchanged, safe harness output, and untouched legacy delivery route contour. Storefront now has an operator/dev-only preview/shadow surface in [`shipping/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/checkout/components/shipping/index.tsx) behind `NEXT_PUBLIC_DELIVERY_HUB_PREVIEW_ENABLED=true`: it calls [`previewDeliveryHubQuotes()`](../medusa-agency-boilerplate-storefront/src/lib/data/delivery-hub.ts) against [`POST /store/delivery/quotes`](../medusa-agency-boilerplate/src/api/store/delivery/quotes/route.ts), displays neutral quote count/price/currency/ETA/PVZ/correlation, can save/clear neutral metadata through existing selection helpers, and explicitly labels `checkout source-of-truth unchanged`; sandbox ids are only prefilled when `NEXT_PUBLIC_DELIVERY_HUB_PREVIEW_DEV_DEFAULTS_ENABLED=true` and are documented as dev defaults, not production behavior. This smoke/preview contour does **not** call [`setShippingMethod()`](../medusa-agency-boilerplate-storefront/src/lib/data/cart.ts:226) for Delivery Hub, does **not** switch checkout source-of-truth, does **not** run create/cancel/status/retry shipment calls, does **not** remove legacy delivery compatibility and does **not** expose Yandex DTOs/secrets to storefront.
- `2026-04-28` storefront Delivery Hub preview/shadow UI runtime validation contour hardened without checkout cutover: [`shipping/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/checkout/components/shipping/index.tsx) now exposes stable `data-testid` hooks for the preview root, feature-flag/dev-defaults guardrails, source-of-truth/no-raw-provider guardrails, quote form fields, quote/save/clear buttons, operation status, quote count/correlation, selection status/correlation, message and quote options. The visible guardrail copy now explicitly says `checkout source-of-truth unchanged`, that Delivery Hub preview metadata does not commit a Medusa shipping method, and that diagnostics are shopper-safe only with no raw provider body, token, auth header, ciphertext or publishable key value. Focused storefront source-level coverage in [`delivery-hub.spec.ts`](../medusa-agency-boilerplate-storefront/src/lib/util/delivery-hub.spec.ts) locks those manual/runtime hooks, visible success/failure status contour, no-secret guardrail wording, neutral save/clear-only wiring and absence of Delivery Hub `setShippingMethod()`/shipment execution wiring. [`delivery_hub_manual_testing_plan.md`](./delivery_hub_manual_testing_plan.md) and storefront [`README.md`](../medusa-agency-boilerplate-storefront/README.md) now document the exact manual/storefront checklist and mock-friendly Playwright locator surface. This step did not perform checkout cutover, did not call [`setShippingMethod()`](../medusa-agency-boilerplate-storefront/src/lib/data/cart.ts:226) for Delivery Hub, did not remove/change legacy delivery contour and did not add shipment create/cancel/status/retry behavior.
- `2026-04-28` mock-friendly storefront Delivery Hub preview browser smoke materialized without adding Playwright dependency or live Yandex/backend requirement: root [`package.json`](../package.json) now exposes `npm run smoke:delivery-hub-preview:browser`, backed by [`delivery-hub-preview-browser-smoke.mjs`](../scripts/delivery-hub-preview-browser-smoke.mjs). The smoke starts a local mock Store API for cart/region/fulfillment/payment prerequisites plus `POST /store/delivery/quotes`, `POST /store/delivery/selection`, and `DELETE /store/delivery/selection`, then runs temporary storefront `next dev` twice with `NEXT_PUBLIC_DELIVERY_HUB_PREVIEW_ENABLED=false` and `true`. It verifies the preview block is hidden when disabled, visible when enabled, guardrails say `checkout source-of-truth unchanged`, mocked quote count/price/currency/correlation/status render, mocked save/clear only update preview metadata selection status, no visible raw provider body/token/auth header/ciphertext/publishable key value appears, and the no legacy fallback checkout contour is required as adjacent source-of-truth. Docs in [`delivery_hub_manual_testing_plan.md`](./delivery_hub_manual_testing_plan.md) and storefront [`README.md`](../medusa-agency-boilerplate-storefront/README.md) now include the command, mocked nature, expected result and guardrails. This browser smoke still does not call [`setShippingMethod()`](../medusa-agency-boilerplate-storefront/src/lib/data/cart.ts:226) for Delivery Hub, does not replace checkout source-of-truth outside the explicit Delivery Hub gate, and does not perform shipment create/cancel/status/retry.
- `2026-04-28` current docs/flag-only checkpoint materialized the formal **Delivery Hub checkout cutover readiness plan and approval gate** in [`delivery_hub_checkout_cutover_plan.md`](./delivery_hub_checkout_cutover_plan.md). The plan records current quote/selection/preview evidence, explicitly sets the default decision to **NO-GO** until a later approval gate passes, defines backend Store API/Admin/Yandex/neutral-selection/storefront-preview/fulfillment-bridge/observability/rollback readiness checklists, canonical neutral quote/selection and high-level cart/order/fulfillment metadata shapes, required go/no-go approval record, feature-flag strategy with reserved default-off storefront flag `NEXT_PUBLIC_DELIVERY_HUB_CHECKOUT_CUTOVER_ENABLED=false`, rollback/no-fallback preservation of the Delivery Hub fail-closed contour, and the exact preconditions required before any future Delivery Hub path may call [`setShippingMethod()`](../medusa-agency-boilerplate-storefront/src/lib/data/cart.ts:226). This checkpoint is documentation/placeholder flag only: no checkout source-of-truth cutover was performed, no Delivery Hub runtime call to [`setShippingMethod()`](../medusa-agency-boilerplate-storefront/src/lib/data/cart.ts:226) was added, legacy delivery compatibility remains preserved, production/runtime code paths remain unchanged, and no shipment create/cancel/status/retry behavior was introduced.
- `2026-04-28` current controlled cutover-prep code step materialized **runtime-visible default-off checkout cutover gate/preflight status** without real checkout cutover. Storefront env/config now parses `NEXT_PUBLIC_DELIVERY_HUB_CHECKOUT_CUTOVER_ENABLED` with explicit `true` semantics only; pure helper [`buildDeliveryHubCheckoutCutoverGateStatus()`](../medusa-agency-boilerplate-storefront/src/lib/util/delivery-hub.ts) returns disabled/default-off or true/preflight status, required readiness evidence labels, visible blockers, and hard invariant `canCommitShippingMethod=false` in both modes. Checkout preview/shadow UI [`shipping/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/checkout/components/shipping/index.tsx) surfaces the status at `data-testid="delivery-hub-cutover-gate-status"`: false/absent says default-off, true says recognized but preflight/blocked-only. Browser smoke now also runs an explicit true-flag pass and verifies no checkout commit/source-of-truth change. This step still does not add a Delivery Hub [`setShippingMethod()`](../medusa-agency-boilerplate-storefront/src/lib/data/cart.ts:226) path, does not remove/change legacy delivery checkout outside the explicit Delivery Hub gate, and does not perform shipment create/cancel/status/retry; review remains pending after completion.
- `2026-04-28` follow-up controlled cutover-prep step materialized a **backend/storefront read-only checkout cutover preconditions verifier** as evidence aggregation only. Backend Store API now exposes [`GET /store/delivery/cutover-preconditions`](../medusa-agency-boilerplate/src/api/store/delivery/cutover-preconditions/route.ts), backed by read-only Delivery Hub service state and safe planner/fulfillment-bridge preview labels; it does not call Yandex/live providers, does not create shipment lifecycle operations, does not expose raw provider payloads/auth headers/tokens/ciphertext/publishable key values, and always returns `can_commit_shipping_method=false`. Storefront data helper [`retrieveDeliveryHubCutoverPreconditions()`](../medusa-agency-boilerplate-storefront/src/lib/data/delivery-hub.ts) normalizes/fails safe, pure helpers [`normalizeDeliveryHubCutoverPreconditionsResponse()`](../medusa-agency-boilerplate-storefront/src/lib/util/delivery-hub.ts) and [`buildDeliveryHubCutoverPreconditionsPreviewModel()`](../medusa-agency-boilerplate-storefront/src/lib/util/delivery-hub.ts) keep `canCommitShippingMethod=false`, and checkout surfaces the verifier near the gate at `data-testid="delivery-hub-cutover-preconditions-status"`. Browser smoke now mocks this verifier and asserts visible required/blocked labels (`operator_approval_required`, `shipment_lifecycle_not_enabled`, `can_commit_shipping_method`) plus `canCommitShippingMethod=false`. This verifier is not approval and not cutover; it does not wire Delivery Hub into [`setShippingMethod()`](../medusa-agency-boilerplate-storefront/src/lib/data/cart.ts:226), does not remove/change legacy delivery checkout outside the explicit Delivery Hub gate, and does not perform shipment create/cancel/status/retry.
- `2026-04-28` current controlled cutover-prep step materialized a **non-executable Delivery Hub checkout cutover decision artifact / operator decision record template** without real checkout cutover. Backend pure builder [`cutover-approval-artifact.ts`](../medusa-agency-boilerplate/src/modules/delivery-hub/cutover-approval-artifact.ts) and Store API endpoint [`GET /store/delivery/cutover-approval-template`](../medusa-agency-boilerplate/src/api/store/delivery/cutover-approval-template/route.ts) now assemble sanitized `artifact_type="delivery_hub_checkout_cutover_decision"` evidence from read-only preconditions plus optional candidate planner data for a `cart_id`; default `decision_status="not_requested"`, supported statuses are `not_requested`, `go_requested`, `no_go`, `approved_but_commit_disabled`, and response-boundary sanitization keeps `commit_controls.can_commit_shipping_method=false`, `commit_controls.approval_is_executable=false`, `requires_separate_implementation=true`, `requires_feature_flag=true`, pending signoff placeholders and false acknowledgement placeholders. Storefront helper [`retrieveDeliveryHubCutoverApprovalArtifact()`](../medusa-agency-boilerplate-storefront/src/lib/data/delivery-hub.ts) plus normalizer/model helpers [`normalizeDeliveryHubCutoverApprovalArtifactResponse()`](../medusa-agency-boilerplate-storefront/src/lib/util/delivery-hub.ts) and [`buildDeliveryHubCutoverApprovalArtifactPreviewModel()`](../medusa-agency-boilerplate-storefront/src/lib/util/delivery-hub.ts) fail safe and preserve `canCommitShippingMethod=false`; checkout preview/shadow UI surfaces it at `data-testid="delivery-hub-cutover-approval-artifact"` with explicit copy `Decision artifact only / no approval execution`. Browser smoke now mocks the endpoint and asserts non-executable artifact visibility, pending signoffs and false commit controls. Operator docs now include canonical evidence-only human template [`delivery_hub_cutover_decision_record_template.md`](./delivery_hub_cutover_decision_record_template.md). This artifact is not runtime approval state, not approval-to-execution wiring and not checkout cutover; it does not call or add Delivery Hub wiring to [`setShippingMethod()`](../medusa-agency-boilerplate-storefront/src/lib/data/cart.ts:226), does not remove/change legacy delivery checkout outside the explicit Delivery Hub gate, does not call live Yandex/provider, and does not perform shipment create/cancel/status/retry.
- `2026-04-28` current controlled cutover-prep step materialized **rollback/fallback drill automation** as a mock/no-network browser contour. Root [`package.json`](../package.json) now exposes `npm run smoke:delivery-hub-rollback:browser`, reusing [`delivery-hub-preview-browser-smoke.mjs`](../scripts/delivery-hub-preview-browser-smoke.mjs) with `--rollback-drill` to avoid duplicate harness code. The drill starts only a local mock Store API and temporary storefront dev servers, then verifies: all Delivery Hub flags off hides preview/gate/preconditions/candidate/decision artifact blocks and makes no Delivery Hub shipping-method commit request; preview enabled with cutover false keeps quote/save/clear metadata-only and source-of-truth unchanged; preview enabled with cutover true remains preflight/blocked-only with `canCommitShippingMethod=false`; and a simulated flags-off restart after cutover-true rehearsal removes Delivery Hub artifacts again even when mock selection exists. It also asserts no Delivery Hub-specific Medusa shipping-method commit request, no visible raw provider/auth/secret material, no shipment lifecycle strings/actions, and absence of any required legacy fallback shipping contour. This drill does not implement real checkout cutover, does not add Delivery Hub wiring to [`setShippingMethod()`](../medusa-agency-boilerplate-storefront/src/lib/data/cart.ts:226), does not make Delivery Hub source-of-truth, does not call live Yandex/provider, does not create executable approval state, and does not perform shipment create/cancel/status/retry.
- `2026-04-28` current controlled cutover-prep step materialized **Delivery Hub checkout cutover evidence bundle exporter** as a read-only operator/reviewer package builder. Root [`package.json`](../package.json) now exposes `npm run evidence:delivery-hub-cutover` and `npm run evidence:delivery-hub-cutover:check`, backed by [`delivery-hub-cutover-evidence-bundle.mjs`](../scripts/delivery-hub-cutover-evidence-bundle.mjs). The exporter generates artifact type `delivery_hub_checkout_cutover_evidence_bundle`, writes markdown by default to ignored local `.delivery-hub-cutover-evidence/delivery-hub-cutover-evidence-bundle.md`, and packages only safe summaries/placeholders for cutover plan, manual testing plan, preconditions verifier, candidate planner, non-executable decision artifact, preview browser smoke command/status, rollback smoke command/status, security guardrails and remaining blockers. `--check` validates required docs/scripts/routes plus required npm smoke commands and scans generated markdown/json for unsafe sentinel fields. The exporter performs no network/live-provider/backend/storefront/browser calls, does not execute smokes, does not expose secrets/raw provider payload/auth headers/ciphertext/token/publishable key values/raw offer ids/raw quote keys/backend execution tokens, does not create executable approval, does not change `can_commit_shipping_method=false` / `canCommitShippingMethod=false`, does not add Delivery Hub [`setShippingMethod()`](../medusa-agency-boilerplate-storefront/src/lib/data/cart.ts:226) wiring, and does not perform real checkout cutover. Operator convention is documented in [`delivery_hub_cutover_evidence_bundle.md`](./delivery_hub_cutover_evidence_bundle.md); review remains pending as a separate go/no-go step.
- `2026-04-28` final controlled cutover-prep docs step materialized **Delivery Hub checkout cutover go/no-go review index** in [`delivery_hub_cutover_go_no_go_index.md`](./delivery_hub_cutover_go_no_go_index.md). The index is operator-facing and navigation-only: it aggregates links to the readiness plan, evidence bundle convention, decision record template, manual testing plan, current work status, root smoke/evidence commands and Store API evidence endpoints; it records current status as `Preview/shadow ready`, `Cutover implementation not enabled`, and **NO-GO for production checkout source-of-truth**; it orders the review flow as preview smoke, rollback smoke, evidence check/export, endpoint artifact inspection, human decision record completion, then only a separate implementation tranche. It explicitly lists remaining blockers: no Delivery Hub-specific [`setShippingMethod()`](../medusa-agency-boilerplate-storefront/src/lib/data/cart.ts:220) path by design, `can_commit_shipping_method=false` / `canCommitShippingMethod=false` invariants, non-executable approval, shipment lifecycle hardening not live-validated, production rollout/rollback rehearsal not completed, and missing executable operator/technical-owner release gate. The then-existing legacy delivery checkout remained source-of-truth; no runtime code or cutover behavior changed.
- `2026-04-28` post-review operational status update: commit [`58d9610`](../commit/58d9610) (`docs(delivery-hub): add cutover go no-go index`) has completed review with verdict **PASS / APPROVE**, no findings and no follow-up patch required. Scope of the reviewed milestone is docs-only/operator-facing: Delivery Hub preview/shadow, rollback smoke, evidence exporter, cutover preconditions, candidate/approval artifact and go/no-go index are now reviewed/approved as a preparation/governance chain, not as production checkout execution. Progress snapshot before this tranche: Delivery Hub cutover-prep evidence/governance chain was ready for human review/planning as a preparation artifact; production checkout source-of-truth cutover remained `0% executed / not implemented`, **NO-GO for production checkout source-of-truth**, and the then-existing legacy delivery checkout remained source-of-truth/fallback. That review status did not create executable approval state or enable Delivery Hub [`setShippingMethod()`](../medusa-agency-boilerplate-storefront/src/lib/data/cart.ts:220).
- `2026-04-28` current implementation tranche materialized the first **default-off Delivery Hub checkout commit path** in the storefront. [`NEXT_PUBLIC_DELIVERY_HUB_CHECKOUT_CUTOVER_ENABLED`](../medusa-agency-boilerplate-storefront/.env.local.example) remains `false` by default and is parsed with explicit-`true` semantics only. When the flag is off, Delivery Hub preview/shadow and neutral metadata may be visible, but no Delivery Hub commit button is enabled and no Delivery Hub path calls [`setShippingMethod()`](../medusa-agency-boilerplate-storefront/src/lib/data/cart.ts:220); the then-existing legacy manual shipping selection remained the checkout source-of-truth and rollback path. When the flag is true, [`buildDeliveryHubCheckoutCutoverGateStatus()`](../medusa-agency-boilerplate-storefront/src/lib/util/delivery-hub.ts), [`evaluateDeliveryHubCutoverCandidateCommitGuard()`](../medusa-agency-boilerplate-storefront/src/lib/util/delivery-hub.ts), and [`buildDeliveryHubCommitEligibilityModel()`](../medusa-agency-boilerplate-storefront/src/lib/util/delivery-hub.ts) allow commit only for a saved neutral selection plus ready cutover candidate whose safe `candidate_shipping_option_id` maps to an available Delivery Hub Medusa shipping option; checkout [`shipping/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/checkout/components/shipping/index.tsx) then commits only that option id through existing [`setShippingMethod()`](../medusa-agency-boilerplate-storefront/src/lib/data/cart.ts:220), without provider-specific data or credentials. Missing, not-ready, unsafe, or unmapped candidates fail closed with a user-visible fallback message and preserve any already-committed legacy manual shipping selection. Browser smoke [`delivery-hub-preview-browser-smoke.mjs`](../scripts/delivery-hub-preview-browser-smoke.mjs) now exercises flag-off no-commit, flag-on ready-candidate mock commit, and rollback flag-off return to legacy behavior. Production enablement is still **NO-GO by default** and requires separate review/approval; this tranche does not implement shipment create/cancel/status/retry and does not make Delivery Hub production source-of-truth automatically.
- `2026-04-28` current operator enablement tranche after approved commit `791cd0a` materialized a minimal **controlled staging enablement layer** for the default-off Delivery Hub checkout cutover path without production default enablement. Root [`package.json`](../package.json) now exposes dedicated `npm run smoke:delivery-hub-cutover:browser`, reusing [`delivery-hub-preview-browser-smoke.mjs`](../scripts/delivery-hub-preview-browser-smoke.mjs) with `--cutover-smoke` to run only the explicit `NEXT_PUBLIC_DELIVERY_HUB_CHECKOUT_CUTOVER_ENABLED=true` local mock/no-network scenario and verify the guarded mapped-option commit. Existing `npm run smoke:delivery-hub-rollback:browser` remains the rollback proof. [`delivery_hub_manual_testing_plan.md`](./delivery_hub_manual_testing_plan.md) now includes a concise staging checklist: keep committed examples/templates false, set the cutover flag only in staging deployment config, run cutover smoke, optionally place one sanitized controlled staging cart/order if available, roll back by setting the flag false and redeploying/restarting storefront, then run rollback smoke. [`delivery_hub_checkout_cutover_plan.md`](./delivery_hub_checkout_cutover_plan.md) and [`delivery_hub_cutover_go_no_go_index.md`](./delivery_hub_cutover_go_no_go_index.md) link the new command/checklist and keep production checkout source-of-truth as **NO-GO by default**. No committed default was changed to true, no production rollout was enabled automatically, no live provider/Yandex API calls are introduced, legacy delivery fallback remained preserved at that earlier checkpoint, and shipment create/cancel/status/retry remains outside this tranche. Review for this new tranche remains pending and must be delegated separately.
- `2026-04-28` remediation for the staging enablement review finding patched the local mock cutover browser smoke so positive flag-on evidence now comes from the actual storefront commit CTA `data-testid="delivery-hub-checkout-commit-button"`, exercising the browser-side checkout handler and existing Medusa Store API shipping-method commit path instead of using a direct Node harness POST as proof. The mock backend records the resulting `POST /store/carts/:cart_id/shipping-methods` body and the smoke asserts exactly one commit with only the expected mapped `deliveryhub:<mode>` Medusa option id, no `data` payload and no provider/secret/raw quote/offer/selection fragments. This remediation does not change `smoke:delivery-hub-cutover:browser`, does not enable committed defaults, does not enable production rollout, does not call live Yandex/provider APIs, and does not add shipment create/cancel/status/retry. Remediation review remains pending and must be delegated separately.
- `2026-04-28` current tooling/docs tranche materialized **Delivery Hub staging dry-run evidence bundle** for controlled staging enablement decision input only. Root [`package.json`](../package.json) now exposes `npm run evidence:delivery-hub-staging-dry-run` and `npm run evidence:delivery-hub-staging-dry-run:check`, backed by [`delivery-hub-staging-dry-run-evidence.mjs`](../scripts/delivery-hub-staging-dry-run-evidence.mjs). The exporter writes ignored local output under `.delivery-hub-cutover-evidence/staging-dry-run/` and records timestamp, safe git commit/dirty-clean status without diff dumps, expected `smoke:delivery-hub-cutover:browser` and `smoke:delivery-hub-rollback:browser` commands, operator `PASS|FAIL|NOT_RUN` status assertions, operator staging cutover flag state assertion `true|false|unknown`, sanitized manual staging cart/order note and sanitized rollback verification note. It rejects notes/output that look like credentials, auth headers, token/ciphertext/key fragments including quoted JSON-style fields, raw provider/Yandex payloads, raw offer ids, raw quote keys, request/response bodies or `.env` contents; it performs no browser smoke execution, no live Yandex/provider calls, no production enablement and no shipment create/cancel/status/retry, while preserving the then-current legacy delivery fallback posture. Docs link the command from manual testing, cutover evidence and cutover plan. Review for this tooling/docs tranche remains pending and must be delegated separately.
- `2026-04-28` remediation for staging dry-run evidence review finding hardened [`delivery-hub-staging-dry-run-evidence.mjs`](../scripts/delivery-hub-staging-dry-run-evidence.mjs) sanitization so secret/key/token/ciphertext/auth field guardrails accept optional quotes around field names and optional quotes after `:`/`=` separators. The no-network `--check` mode now runs sanitizer self-check samples for quoted JSON-style token/password/api_key/ciphertext/Authorization examples, unquoted legacy token/password/api_key/ciphertext examples and one safe note. This remediation does not read `.env`, does not run browser/backend/provider, does not write unsafe artifacts, does not enable production rollout, does not change committed defaults/templates to true, does not remove legacy delivery fallback posture and does not add shipment create/cancel/status/retry. Remediation review remains pending and must be delegated separately.
- `2026-04-28` controlled staging dry-run evidence capture was executed as a **local surrogate only**, because this workspace does not contain a real external staging deployment config/URL/credential procedure safe to use without secrets. Commands/results: `npm run evidence:delivery-hub-staging-dry-run:check` PASS; `npm run smoke:delivery-hub-cutover:browser` PASS; `npm run smoke:delivery-hub-rollback:browser` PASS. Sanitized evidence was generated at ignored local path `.delivery-hub-cutover-evidence/staging-dry-run/delivery-hub-staging-dry-run-evidence.md` with operator assertions `cutover_smoke_status=PASS`, `rollback_smoke_status=PASS`, `staging_delivery_hub_cutover_flag_state=unknown`, manual note `Real external staging cart/order path not available in this workspace; local mock browser cutover and rollback smokes passed.`, and rollback note `Local mock rollback smoke passed; real external staging flag-off redeploy verification remains pending.` No generated artifact is committed; real external staging cart/order and flag-off redeploy verification remain pending, production rollout remains default-off/no-go, committed templates still keep `NEXT_PUBLIC_DELIVERY_HUB_CHECKOUT_CUTOVER_ENABLED=false`, and separate review for this evidence-status commit remains pending.
- `2026-04-28` real external staging dry-run attempt is truthfully **BLOCKED / NO-GO** from this workspace after safe repo-only inspection, without reading runtime `.env` contents and without printing secrets. Files inspected for safe staging hints: [`Docs/staging_deploy_path.md`](./staging_deploy_path.md), [`Docs/staging_verification_contour.md`](./staging_verification_contour.md), [`Docs/staging_checklist.md`](./staging_checklist.md), [`Docs/staging_rollback_runbook.md`](./staging_rollback_runbook.md), [`Docs/current_work.md`](./current_work.md), [`docker-compose.yml`](../docker-compose.yml), [`medusa-agency-boilerplate-storefront/.env.local.example`](../medusa-agency-boilerplate-storefront/.env.local.example), [`.env.example`](../.env.example), [`package.json`](../package.json), [`Docs/delivery_hub_manual_testing_plan.md`](./delivery_hub_manual_testing_plan.md), and safe staging search hits in [`scripts/staging-verification.sh`](../scripts/staging-verification.sh). Result: repo contains a generic staging verification contour and a manual controlled-staging checklist, but no concrete safe external staging URL, no committed staging deployment config, no provider/platform redeploy or restart procedure, no safe credential handoff, and no rollback command that can be executed here without secrets. [`docker-compose.yml`](../docker-compose.yml) remains local baseline only and storefront is not part of that compose topology; [`scripts/staging-verification.sh`](../scripts/staging-verification.sh) requires a prepared staging env file/URLs and notification auth or data-plane inputs that are intentionally not present in the repository. Therefore no real staging cutover/cart/order path, no staging flag-on redeploy, and no staging flag-off rollback redeploy were executed; no new staging PASS evidence was generated. Local surrogate evidence from commit `ee9ef32` remains PASS, but external staging remains **NO-GO** until an operator supplies a safe staging URL/deployment config/credential procedure outside the repo. Committed examples/templates still keep `NEXT_PUBLIC_DELIVERY_HUB_CHECKOUT_CUTOVER_ENABLED=false`; production rollout remains default-off/no-go; legacy delivery fallback remained preserved at that earlier checkpoint; review for this blocker-status commit remains pending.
- `2026-04-28` local localhost Delivery Hub end-to-end dry run after review commit `c3fa643` is truthfully **PARTIAL**. Safe repo/script inspection found the relevant root commands [`smoke:backend`](../package.json:33), [`smoke:delivery-hub-cutover:browser`](../package.json:38), [`smoke:delivery-hub-rollback:browser`](../package.json:39) and [`evidence:delivery-hub-staging-dry-run:check`](../package.json:43), plus backend harness [`delivery:store-smoke`](../medusa-agency-boilerplate/package.json:20). Running services were present locally (`medusa-backend`, `medusa-db`, `medusa-redis`); `npm run smoke:backend` passed against `http://localhost:9000/health`; safe Admin shell/HMR readiness probes for `http://localhost:9000/app/login`, `http://localhost:9000/app/settings/delivery` and `http://localhost:9000/app/@vite/client` returned HTTP `200`; unauthenticated `GET /admin/delivery/providers` returned `401`, confirming the admin Delivery API is protected and no admin credentials were read. `npm run evidence:delivery-hub-staging-dry-run:check` passed; `npm run smoke:delivery-hub-cutover:browser` passed with local mock Store API and explicit `NEXT_PUBLIC_DELIVERY_HUB_CHECKOUT_CUTOVER_ENABLED=true`, exercising the browser commit CTA and asserting only the mapped `deliveryhub:<mode>` mock option id was committed; `npm run smoke:delivery-hub-rollback:browser` passed with local mock Store API and flag-off rollback. Live/local neutral Store quote+selection harness attempts against the previously documented local ids for both `dropoff_point_to_pickup_point` and `warehouse_to_pickup_point` returned controlled safe summaries with `status=failed_quote_request`, quote HTTP `400`, no selection save and no checkout cutover; no raw provider DTO, raw response body, token, auth header, ciphertext, publishable key value, raw quote key or raw offer id was printed. Therefore localhost proves backend health, Admin shell reachability/protected API posture, mock browser cutover and mock rollback, but does **not** prove current live Yandex `Test connection`, live Yandex `Test quote`, real neutral quote+selection save, real checkout order placement, shipment create/status/cancel/retry, or production/staging GO. User instruction now changes the next implementation sequencing: legacy delivery fallback/checkout references were still present in the current cutover/rollback code and docs as a rollback dependency, so the next separate hard-cutover tranche should remove legacy delivery fallback/checkout path and update rollback semantics after this dry-run record; this dry-run tranche intentionally did not delete legacy delivery files/routes/modules or mix large migration/removal with validation.
- `2026-04-28` localhost Delivery Hub store quote `HTTP 400` diagnosis completed safely after dry-run commit `fcc7912`: likely suspects considered were route Zod validation/body parsing, missing connection/warehouse/PVZ state, stale backend, provider Yandex rejection, store middleware/publishable-key guard, and script sanitization hiding top-level errors. Safe service-level diagnostics via [`delivery-hub-store-quote-safe-diagnostics.ts`](../medusa-agency-boilerplate/src/scripts/delivery-hub-store-quote-safe-diagnostics.ts) confirmed local persisted Yandex readiness (`enabled=true`, `status=active`, `credentials_state=sealed`, default warehouse mapped) and live service quote success without printing secrets/raw DTOs: `dropoff_point_to_pickup_point` returned `quotes_count=13`, `warehouse_to_pickup_point` returned `quotes_count=4`. The same payload through real `POST /store/delivery/quotes` reproduced `HTTP 400` with safe top-level Medusa error `type=not_allowed`, message `Publishable API key required in the request header: x-publishable-api-key`; therefore root cause is Store API publishable-key middleware/precondition in the localhost smoke invocation, not Delivery Hub route validation, not missing warehouse/PVZ/connection state, not Yandex `/offers/create` provider rejection, and not legacy delivery fallback. Scoped fix: [`delivery-hub-storefront-neutral-smoke.ts`](../medusa-agency-boilerplate/src/scripts/delivery-hub-storefront-neutral-smoke.ts) now extracts safe top-level Medusa error `type/message` as `code/message`, so future safe summaries show `not_allowed` instead of generic `Store quote request failed`; focused coverage in [`delivery-hub-storefront-neutral-smoke-harness.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub-storefront-neutral-smoke-harness.unit.spec.ts) covers this sanitized middleware-400 behavior. Checks: targeted unit test PASS, `npm run typecheck` PASS, provider/service quote diagnostics PASS for both quote modes, and real store smoke remains blocked/failing at quote with `code=not_allowed` until a valid local `MEDUSA_PUBLISHABLE_KEY`/`x-publishable-api-key` is supplied. No `.env` values, merchant credentials, tokens, auth headers, ciphertext, raw provider DTOs, raw Yandex bodies, raw quote keys, raw offer ids, selection save, checkout cutover, shipment lifecycle call, legacy delivery call or legacy delivery removal were performed.
- `2026-04-28` повторный localhost Delivery Hub dry run после диагностического fix commit `90d4206` подтвердил предыдущий blocker truthfully: publishable API key для Store API локально недоступен. Safe presence-check проверил только факт наличия `MEDUSA_PUBLISHABLE_KEY` / `DELIVERY_HUB_STORE_SMOKE_PUBLISHABLE_KEY` в окружении и local backend `.env` без печати значений; результат `absent`, backend health `200`. Поэтому key-backed real Store quote/selection через `npm run delivery:store-smoke` для обоих mode codes не выполнялся как PASS и не подменялся fake success; harness dry invocation без required inputs завершился `blocked_missing_input` и показал `publishable_key_provided=false`. Service-level provider quote readiness остаётся подтверждённой по latest safe diagnostics/event-log evidence: `dropoff_point_to_pickup_point` имеет recent `quotes_count=13`, `warehouse_to_pickup_point` имеет recent `quotes_count=4`, выбранная Yandex connection active/enabled/sealed, но fresh service quote attempt в этом rerun был skipped из-за отсутствующих smoke input env ids, а не provider failure. Re-run checks: `npm run smoke:backend` PASS; `npm run smoke:delivery-hub-cutover:browser` PASS with local mock Store API and mapped `deliveryhub:<mode>` commit assertion; `npm run smoke:delivery-hub-rollback:browser` PASS; focused unit [`delivery-hub-storefront-neutral-smoke-harness.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub-storefront-neutral-smoke-harness.unit.spec.ts) PASS; root `npm run typecheck` PASS; [`git diff --check`](../) PASS. No `.env` values, publishable key, merchant credentials, tokens, auth headers, ciphertext, raw provider DTO/body, raw quote keys or raw offer ids were printed; no staging/prod GO is claimed. Production/staging checkout cutover remains **NO-GO**; legacy delivery fallback removal remains the next separate hard-cutover tranche only after key-backed quote/selection is healthy and reviewed.
- `2026-04-28` precondition/evidence tranche for local publishable-key-backed Store API smoke is truthfully **PARTIAL / BLOCKED at provider readiness after key precondition was closed**. Scoped safe helper logic in [`delivery-hub-storefront-neutral-smoke.ts`](../medusa-agency-boilerplate/src/scripts/delivery-hub-storefront-neutral-smoke.ts) now auto-discovers a local publishable key in-memory through Medusa query/workflow surfaces, links it to the default sales channel when possible, may create a local smoke key only if none exists, and prints only presence/status fields (`publishable_key_provided`, `source`, `found`, `created`, `publishable_key_value_printed=false`) without printing the key value. Local run found an existing publishable key (`existing_publishable_key_count=1`, title only recorded as safe metadata) and did not create a new key; key value was not printed, logged, documented or committed. The previous Store middleware `not_allowed / x-publishable-api-key required` blocker is therefore closed. Key-backed real Store HTTP smoke then reached the Delivery Hub/provider route for both modes but did **not** close quote/selection green: `dropoff_point_to_pickup_point` with the existing smoke ids reached `POST /store/delivery/quotes`, returned quote HTTP `401` mapped to safe `DELIVERY_HUB_CREDENTIALS_INVALID` / provider `403` access-block diagnostics, so `quotes_count=0` and selection save was not attempted; `warehouse_to_pickup_point` with explicit saved connection/warehouse reached `POST /store/delivery/quotes`, returned quote HTTP `409` with safe `DELIVERY_HUB_VALIDATION_ERROR · Delivery connection is not active for store/public use`, so `quotes_count=0` and selection save was not attempted. Fresh safe service diagnostics confirmed the selected Yandex connection is now `enabled=true` but `status=error`, `credentials_state=invalid`, `credentials_last_error_code=DELIVERY_HUB_CREDENTIALS_INVALID`; recent historical event-log evidence still contains earlier successful service quotes (`dropoff_point_to_pickup_point` `quotes_count=13`, `warehouse_to_pickup_point` `quotes_count=4`) before the current credential/access-block invalidation. Checks in this tranche: focused unit [`delivery-hub-storefront-neutral-smoke-harness.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub-storefront-neutral-smoke-harness.unit.spec.ts) PASS including safe key discovery/no-value-output coverage; backend `npm run typecheck` PASS; `npm run smoke:backend` PASS; `npm run smoke:delivery-hub-cutover:browser` PASS; `npm run smoke:delivery-hub-rollback:browser` PASS; [`git diff --check`](../) PASS. No `.env` values, publishable key value, merchant credentials, Yandex token, auth header, ciphertext, raw provider/Yandex DTO/body, raw quote key, raw offer id, checkout cutover, shipment lifecycle call, legacy delivery call or legacy delivery removal was performed. No staging/prod GO is claimed; production/staging checkout cutover remains **NO-GO**. Next step is to review this commit and restore/revalidate a ready local Yandex sandbox connection before rerunning key-backed Store quote/selection; legacy delivery no-fallback removal remains a separate reviewed tranche only after key-backed quote/selection evidence is green.
- `2026-04-28` follow-up safe local Delivery Hub/Yandex readiness investigation after reviewed commit `bcc48fd` remains **BLOCKED / NOT RESTORED**. Possible sources considered: expired/blocked sandbox credentials, provider-side `403` access block, connection disabled/not active for public Store use, wrong selected connection, stale credential status after a previous failure, and config/test-data mismatch. Safe inspection found two local Yandex sandbox connections, both `enabled=true`, `status=error`, `credentials_state=invalid`, credentials present, same default warehouse mapping present, and no ready `active+sealed` connection. Recent logs show successful quotes for the previously working connection at `2026-04-28T16:04Z` (`dropoff_point_to_pickup_point` `quotes_count=13`, `warehouse_to_pickup_point` `quotes_count=4`), followed by a newer provider failure at `2026-04-28T16:39Z` on `/offers/create` with provider `403`, safe code `DELIVERY_HUB_CREDENTIALS_INVALID`, `error_category=provider_access_blocked`. One bounded safe `testConnection` revalidation per saved connection was attempted through existing service APIs; both returned provider `403` normalized to `DELIVERY_HUB_CREDENTIALS_INVALID` and persisted/kept `status=error`, `credentials_state=invalid`. A new safe repair helper [`delivery-hub-local-connection-status-repair.ts`](../medusa-agency-boilerplate/src/scripts/delivery-hub-local-connection-status-repair.ts) was added to prevent unsafe stale-status restoration: dry-run evidence refused to mark the connection ready because the latest failure is newer than latest success (`repair_blocker=newer_provider_failure_after_success`). Key-backed Store smoke was rerun only enough to confirm readiness remains blocked: publishable key auto-discovery still works without printing the key, but quote fails before provider call with `HTTP 409`, safe `DELIVERY_HUB_VALIDATION_ERROR`, message `Delivery connection is not active for store/public use`; selection was not attempted. Root cause/status: local credentials/API-access for the saved sandbox Yandex connections are currently not provider-ready after a new `403` rejection; this is not a publishable-key blocker, not missing warehouse/PVZ ids, not checkout cutover, and not legacy delivery. No `.env` values, Yandex token, publishable key value, auth headers, ciphertext, raw provider body/DTO, raw quote keys, raw offer ids or checkout/shipment/legacy delivery actions were printed or executed. Next step: operator must supply/re-enter valid sandbox Yandex credentials or resolve Yandex sandbox/API/network access block, then run the same safe readiness check and key-backed Store quote/selection smoke again.
- `2026-04-28` follow-up Delivery Hub credential/evidence and legacy cleanup tranche remains truthfully **PARTIAL / BLOCKED for live Store quote-selection evidence**. A safe manual-only helper [`delivery-hub-yandex-credentials-apply.ts`](../medusa-agency-boilerplate/src/scripts/delivery-hub-yandex-credentials-apply.ts) plus operator note [`delivery-hub-yandex-credentials-apply-readme.md`](../medusa-agency-boilerplate/src/scripts/delivery-hub-yandex-credentials-apply-readme.md) were added so a Yandex Delivery token can be written into encrypted Delivery Hub connection storage and optionally revalidated without printing token values, token length, token fingerprints, auth headers, ciphertext or raw provider bodies. Attempts to pass the operator-provided token from this agent session were intentionally stopped/blocked by the non-interactive tool environment: `/dev/tty` was unavailable and stdin could not be delivered through the `medusa exec` invocation without exposing the secret in a visible command, so the helper was committed for local operator use instead. The separate cleanup tranche also removed remaining active-source/docs references to the old provider name in Delivery Hub/storefront/smoke surfaces, renamed the non-executable approval acknowledgement from a provider-specific fallback acknowledgement to `legacy_fallback_available`, updated no-fallback copy to generic legacy-delivery wording, and removed empty old local source directories under [`src/api/store`](../medusa-agency-boilerplate/src/api/store), [`src/api/admin`](../medusa-agency-boilerplate/src/api/admin) and [`src/admin/routes/settings`](../medusa-agency-boilerplate/src/admin/routes/settings). Source-focused cleanup search across `medusa-agency-boilerplate/src`, `medusa-agency-boilerplate-storefront/src`, `scripts`, `Docs/delivery_hub_*.md` and [`medusa-agency-boilerplate-storefront/README.md`](../medusa-agency-boilerplate-storefront/README.md) returned no old-provider-name hits; generated/local/history artifacts such as `.git`, `.medusa`, `.next`, `.playwright-mcp` and ignored evidence bundles are not treated as active source-of-truth and were not hand-edited. Validation after the cleanup passed: backend `npm run typecheck`, storefront neutral util test [`delivery-hub.spec.ts`](../medusa-agency-boilerplate-storefront/src/lib/util/delivery-hub.spec.ts), backend Store route unit [`delivery-hub-store.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub-store.unit.spec.ts), `npm run evidence:delivery-hub-cutover:check`, `npm run evidence:delivery-hub-staging-dry-run:check`, and [`git diff --check`](../). Live Store quote/selection remained unclosed at that point, so no staging/prod GO or live quote/selection PASS was claimed.
- `2026-04-28` current legacy delivery no-fallback hard-cutover removal tranche materialized the first safe no-fallback step in active checkout. Repo-wide safe inventory found no active storefront legacy delivery provider data/helper files under [`medusa-agency-boilerplate-storefront/src`](../medusa-agency-boilerplate-storefront/src); active checkout references were limited to fallback copy/smoke expectations, while backend legacy delivery provider surfaces are already absent from source directories inspected in this workspace (old provider-specific API/module surfaces contain no source files). The storefront checkout path in [`shipping/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/checkout/components/shipping/index.tsx) now treats Delivery Hub as the only eligible delivery commit contour: the continue-to-payment gate only treats committed `deliveryhub` shipping option ids/provider metadata as committed, blocked Delivery Hub readiness shows controlled fail-closed retry/operator messages, and no legacy delivery shipping method is selected automatically. The cutover helper copy in [`delivery-hub.ts`](../medusa-agency-boilerplate-storefront/src/lib/util/delivery-hub.ts) now states default-off/blocked readiness is fail-closed with no legacy delivery fallback. Browser smoke [`delivery-hub-preview-browser-smoke.mjs`](../scripts/delivery-hub-preview-browser-smoke.mjs) was updated so the rollback drill no longer requires `legacy delivery fallback shipping`; instead it mocks only a Delivery Hub shipping option, asserts no legacy fallback contour is visible or required, verifies flag-off no-commit posture, and still verifies explicit flag-on Delivery Hub commit only to the mapped `deliveryhub:<mode>` option. Storefront docs [`README.md`](../medusa-agency-boilerplate-storefront/README.md) now document Delivery Hub-only/no-fallback active checkout and rollback/no-fallback smoke semantics. Backend cutover precondition/artifact copy was aligned in [`cutover-preconditions.ts`](../medusa-agency-boilerplate/src/modules/delivery-hub/cutover-preconditions.ts) and [`cutover-approval-artifact.ts`](../medusa-agency-boilerplate/src/modules/delivery-hub/cutover-approval-artifact.ts) without deleting backend APIs blindly. Provider readiness/Yandex `/offers/create` still blocks full live key-backed Store quote/selection evidence, so this is **not staging/prod GO** and does not claim both-mode live quote/selection PASS.
- `2026-04-28` follow-up after operator-side token application changed the local blocker from credential storage/readiness to a provider access-block on one quote mode. The operator ran the safe helper locally and reported `credentials_applied_and_ready` for Yandex connection `60208aa4-01e7-45a1-ba53-c8be5506bd86`: before state was `enabled=true`, `status=error`, `credentials_state=invalid`, default warehouse `fa279b9d-316d-45c2-aa8d-aa92a77d15ba`; revalidation attempted `true` and returned provider status `ok`; after state became `enabled=true`, `status=active`, `credentials_state=sealed`, `credentials_last_error_code=null`. A fresh key-backed Store smoke for `dropoff_point_to_pickup_point` reached the direct Yandex `/offers/create` path but returned quote HTTP `502` with safe `DELIVERY_HUB_PROVIDER_ERROR`, provider status `403`, `error_category=provider_access_blocked`; `quotes_count=0` and selection was not attempted. A fresh key-backed Store smoke for `warehouse_to_pickup_point` with the same connection, warehouse `fa279b9d-316d-45c2-aa8d-aa92a77d15ba` and PVZ `e1139f6d-e34f-47a9-a55f-31f032a861a6` succeeded: quote HTTP `200`, `quotes_count=4`, selection HTTP `200`, `saved=true`, `checkout_source_of_truth=unchanged`, contour `delivery_hub_storefront_preview`; no checkout cutover or shipment lifecycle call was performed. Code was patched so Yandex HTML/provider access-block `403` is classified as `DELIVERY_HUB_PROVIDER_ERROR`/HTTP `502`, not `DELIVERY_HUB_CREDENTIALS_INVALID`; this prevents a route/network/provider access-block page from invalidating otherwise sealed credentials while preserving true `401/403` auth failures as credential invalid. Focused unit coverage in [`delivery-hub-tranche1-coverage.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub-tranche1-coverage.unit.spec.ts) and [`delivery-hub.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub.unit.spec.ts) now covers access-block classification and no invalidation of sealed credentials for provider-access-block quote failure. Targeted tests passed via `npm run test:unit -- --runTestsByPath src/workflows/__tests__/delivery-hub.unit.spec.ts src/workflows/__tests__/delivery-hub-tranche1-coverage.unit.spec.ts`. Safety posture remained intact: no Yandex token, publishable key value, auth header, ciphertext, raw provider body/DTO, raw quote key or raw offer id was printed, logged or documented. Full live Store quote/selection evidence remains **PARTIAL** until `dropoff_point_to_pickup_point` also produces quote HTTP `200` plus selection HTTP `200`; production/staging checkout cutover remains **NO-GO**.
- `2026-04-28` follow-up key-backed Store evidence attempt closed the immediate local quote/selection evidence gap for both Delivery Hub modes while preserving no-secret/no-cutover boundaries. Initial safe working-tree inspection left unrelated [`generated-images/`](../generated-images/) untouched. Connection `60208aa4-01e7-45a1-ba53-c8be5506bd86` was checked with safe summary only and remained ready for Store/public use: `enabled=true`, `status=active`, `credentials_state=sealed`, credentials present, default warehouse mapped; later checks after quote attempts confirmed the access-block classification did not invalidate sealed credentials. Because no existing script gave a point-id-specific safe dropoff summary, a minimal script [`delivery-hub-yandex-pickup-point-safe-diagnostics.ts`](../medusa-agency-boilerplate/src/scripts/delivery-hub-yandex-pickup-point-safe-diagnostics.ts) was added; it prints only safe point summaries and confirmed origin point `019d2a9da5877011a771b75e903f3039` exists and has `available_for_dropoff=true`. The Store smoke harness was also tightened so successful summaries expose `selected_quote_reference.id_present=true` plus version only, not the opaque raw quote-reference id. During this run, transient Yandex HTML/provider access-block `403` on `/offers/create` was observed and normalized as safe `DELIVERY_HUB_PROVIDER_ERROR`/HTTP `502` without credential invalidation; subsequent retries succeeded. Final key-backed Store smoke evidence: `dropoff_point_to_pickup_point` with the confirmed origin point and destination PVZ returned quote HTTP `200`, `quotes_count=13`, selection HTTP `200`, `saved=true`, `checkout_source_of_truth=unchanged`, contour `delivery_hub_storefront_preview`; warehouse regression with warehouse `fa279b9d-316d-45c2-aa8d-aa92a77d15ba` and the same destination PVZ returned quote HTTP `200`, `quotes_count=4`, selection HTTP `200`, `saved=true`, `checkout_source_of_truth=unchanged`, contour `delivery_hub_storefront_preview`. No checkout source-of-truth cutover, no `setShippingMethod()` staging/prod enablement, no create/cancel/status/retry shipment call, no legacy-delivery action and no production rollout was performed; staging/prod checkout cutover remains **NO-GO** pending separate reviewed rollout/approval despite both local key-backed quote+selection smokes now being green. No Yandex token, publishable key value, auth header, ciphertext, raw provider body/DTO, raw quote key, raw offer id or raw quote-reference id was printed or documented.

- уже materialized: foundation backend module, encrypted credentials storage, direct `Yandex Delivery` adapter, admin API, event-log storage и admin extension page [`page.tsx`](../medusa-agency-boilerplate/src/admin/routes/settings/delivery/page.tsx); поверх этого `2026-04-22` materialized отдельный backend tranche-1 test layer [`delivery-hub-tranche1-coverage.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub-tranche1-coverage.unit.spec.ts), который без сети и без реальных секретов закрывает baseline confidence around encryption/redaction boundary, direct Yandex normalization boundary и service-level [`testConnection()`](../medusa-agency-boilerplate/src/modules/delivery-hub/service.ts:321) / [`testQuote()`](../medusa-agency-boilerplate/src/modules/delivery-hub/service.ts:407) contour;
- частично done: страница `Settings -> Delivery` больше не является planned-only и теперь имеет user-friendly основной путь на русском: **1. Подключение Яндекс Доставки**, **2. Проверить подключение**, **3. Найти ПВЗ**, **4. Проверить стоимость доставки**. Advanced/diagnostic поверхности (`Providers/capabilities`, advanced connection fields, warehouse/source mapping, pickup windows lookup, technical JSON diagnostics, shipping-option/fulfillment/execution previews, event logs и shipment operations) отодвинуты в раскрываемые details/диагностические блоки. Basic Test quote для `warehouse_to_pickup_point` больше не требует pickup windows before quote: `/offers/create` использует saved warehouse `provider_warehouse_id` как `source.platform_station.platform_id`, destination PVZ id из lookup как `destination.platform_station.platform_id`, а `interval_utc` остаётся optional advanced input только если заполнены `from/to`. UI явно помечает ключевые поля как **Обязательно/Необязательно** и объясняет write-only token, warehouse/source, destination PVZ/platform station id, origin dropoff point с `available_for_dropoff=true`, `platform_station_id` и ожидаемый `5 Post` в Yandex sandbox catalog. Backend/admin теперь также имеет первый shipment-operations visibility surface через [`GET /admin/delivery/shipments/:execution_reference/operations`](../medusa-agency-boilerplate/src/api/admin/delivery/shipments/[execution_reference]/operations/route.ts), guarded manual refresh foundation через [`POST /admin/delivery/shipments/:execution_reference/operations/refresh-status`](../medusa-agency-boilerplate/src/api/admin/delivery/shipments/[execution_reference]/operations/refresh-status/route.ts) и explicit backend-guarded manual cancel action через [`POST /admin/delivery/shipments/:execution_reference/operations/cancel`](../medusa-agency-boilerplate/src/api/admin/delivery/shipments/[execution_reference]/operations/cancel/route.ts), а React page-state/widget integration for shipment operations materialized как single-reference operator lookup + guarded refresh/cancel без broad dashboard; поля `Enabled`, `Auto confirm`, `Label format` сохраняются в connection config, а `Default warehouse` выбирается через materialized warehouse entity и резолвится в provider warehouse id внутри backend service; shipping-option preview блок читает только admin route [`GET /admin/delivery/shipping-options/preview`](../medusa-agency-boilerplate/src/api/admin/delivery/shipping-options/preview/route.ts) и показывает summary/diff buckets `create_candidates | update_candidates | unchanged | orphaned_managed_options | ignored_foreign_options`, desired projected options, deferred planner issues и per-connection planner state без create/update/delete side-effects; fulfillment bridge preview блок читает только admin route [`GET /admin/delivery/fulfillment-bridge/preview`](../medusa-agency-boilerplate/src/api/admin/delivery/fulfillment-bridge/preview/route.ts) и truthfully показывает, для каких `deliveryhub` mode-code bridge seam может materialize neutral selection → shipping option contract → fulfillment payload → create-fulfillment preview payload, оставаясь diagnostic-only, no-network и без shipment execution; execution-plan observability блок читает только admin route [`GET /admin/delivery/execution-plan/preview`](../medusa-agency-boilerplate/src/api/admin/delivery/execution-plan/preview/route.ts) и показывает redacted provider execution plan contour без live provider calls, без order/cart mutations и без checkout cutover; provider-time path внутри [`validateFulfillmentData()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:62) использует тот же normalized fulfillment-contract verdict, что и order-time path внутри [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:107); manual sync блок вызывает admin-only route [`POST /admin/delivery/shipping-options/sync`](../medusa-agency-boilerplate/src/api/admin/delivery/shipping-options/sync/route.ts), default-но работает как safe `dry_run`, а execute-path требует exact guard string, explicit Medusa ids для create-context и deliberate secondary action без accidental one-click execution; все `dry_run` и `execute` попытки пишут safe audit summary в existing logs contour, поэтому manual sync history видима через [`GET /admin/delivery/logs`](../medusa-agency-boilerplate/src/api/admin/delivery/logs/route.ts) без UI refactor;
- реально существующие automated tests теперь truthfully закрывают три no-network слоя: [`delivery-hub.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub.unit.spec.ts) и [`delivery-hub-manual-sync-audit.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub-manual-sync-audit.unit.spec.ts) держат pure/service seams вокруг planner, reconciliation, operation-plan, manual sync audit и canonical `deliveryhub` shipping-option metadata contract; focused seam test [`delivery-hub-fulfillment-provider-bridge.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub-fulfillment-provider-bridge.unit.spec.ts) теперь отдельно фиксирует backend-only bridge materialization для persisted cart selection -> Medusa-facing fulfillment payload, mode compatibility guard, create-fulfillment bridge validation, order-side diagnostic report для valid/missing/drifted create-fulfillment inputs, projected/deferred readiness-preview semantics, execution-plan preview seam, deterministic identity/idempotency preview stability/change-sensitivity, preview-only persistence/audit plan, normalized shipment-result preview seam, fulfillment application mutation preview seam, новый failure-handling / compensation preview seam и admin observability projection для ready/blocked mode contours без live network и без shipment automation; дополнительный focused provider seam test [`delivery-hub-provider-validation.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub-provider-validation.unit.spec.ts) отдельно фиксирует parity между [`validateFulfillmentData()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:62) и [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:119) для valid input, missing required fragment, provider/shape drift, execution-plan preview logging, new controlled-execution `provider_payload_materialization` evidence for ready/blocked direct Yandex preview paths, disabled-gate no-live-dispatch behavior и preserved execution block без live network и без shipment execution; [`delivery-hub-admin.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub-admin.unit.spec.ts) закрывает compact HTTP-boundary seam через прямой вызов exported route handlers для admin connections/providers/test-connection/test-quote/preview/fulfillment-bridge-preview/execution-plan-preview/manual-sync/logs, фиксируя happy-path response shapes, missing-entity paths и active response-boundary hardening для всех materialized admin routes, включая stricter nested readiness-preview boundary для `selection`, `shipping_option_data`, `fulfillment_payload` и `create_fulfillment_payload`, а теперь и strict execution-plan observability boundary для `readiness_verdict`, `issues`, `execution_plan`, `execution_identity`, `outbound_payload_preview`, `persistence_audit_preview`, `shipment_result_preview`, `failure_handling_preview`, `fulfillment_application_preview` и redacted secret-like fragments; [`page-state.unit.spec.ts`](../medusa-agency-boilerplate/src/admin/routes/settings/delivery/__tests__/page-state.unit.spec.ts) отдельно закрывает operator-facing admin page contour [`page.tsx`](../medusa-agency-boilerplate/src/admin/routes/settings/delivery/page.tsx) через minimal pure/view-model seam [`page-state.ts`](../medusa-agency-boilerplate/src/admin/routes/settings/delivery/page-state.ts), фиксируя derived/render-state happy path для `providers/connections/warehouses/event logs/shipping option preview/fulfillment bridge preview/execution-plan observability preview/manual sync/shipment operations lookup`, empty/error/guard states, Yandex-only filtering, write-only token semantics, read-only deterministic identity rendering, read-only persistence/audit render block, read-only shipment-result verdict render block, read-only fulfillment application verdict render block, новый read-only failure-handling verdict / retry posture / compensation posture render block, shipment-operations endpoint URL/request helpers, available/blocked refresh action posture and отсутствие leakage secret-like fragments без live network;
- частично done на shopper/store side: backend уже materializes neutral public contract routes [`GET /store/delivery/catalog`](../medusa-agency-boilerplate/src/api/store/delivery/catalog/route.ts), [`GET /store/delivery/quotes`](../medusa-agency-boilerplate/src/api/store/delivery/quotes/route.ts), [`GET /store/delivery/pickup-points`](../medusa-agency-boilerplate/src/api/store/delivery/pickup-points/route.ts), [`GET /store/delivery/pickup-windows`](../medusa-agency-boilerplate/src/api/store/delivery/pickup-windows/route.ts), [`GET /store/delivery/readiness`](../medusa-agency-boilerplate/src/api/store/delivery/readiness/route.ts) и selection surface [`GET/POST/DELETE /store/delivery/selection`](../medusa-agency-boilerplate/src/api/store/delivery/selection/route.ts); новый catalog contract возвращает shopper-usable connections через safe neutral поля `connection_id`, `label`, `state`, `ready`, `quote_types`, `supports_pickup_points`, `supports_pickup_windows`, `supports_dropoff` и optional `default_connection_id`, при этом intentionally не раскрывает `provider_code`, `credentials_state`, raw `enabled/status` fragments и другие provider/internal детали; `quotes` contract теперь тоже truthfully закрыт по boundary: shopper-facing [`GET /store/delivery/quotes`](../medusa-agency-boilerplate/src/api/store/delivery/quotes/route.ts) не возвращает internal `quote_key`, выдаёт server-side opaque `quote_reference` и очищает `raw_reference`; readiness contract возвращает только neutral `status + issues[] + selection + quote_context`, причём `quote_context.connection` intentionally ограничен safe summary `connection_id + state + ready`, проверяет persisted selection shape и shopper-facing connection readiness, но ещё не делает live re-quote или shipping-method lifecycle validation; поверх существующего route seam coverage [`src/workflows/__tests__/delivery-hub-store.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub-store.unit.spec.ts) теперь напрямую вызывает exported handlers для `catalog/settings/selection/readiness/quotes/pickup-points/pickup-windows` без server bootstrap и без сети, фиксирует happy-path response shapes, malformed-query / missing-cart guard paths, а для [`GET()`](../medusa-agency-boilerplate/src/api/store/delivery/selection/route.ts:33) и [`POST()`](../medusa-agency-boilerplate/src/api/store/delivery/selection/route.ts:58) дополнительно доказывает active response-boundary enforcement против leaked internal/provider-specific selection fragments; leaked поля теперь не проходят boundary validation и конвертируются в controlled `DELIVERY_HUB_VALIDATION_ERROR` вместо пассивного проброса в shopper payloads; storefront-facing neutral scaffold truthfully остаётся разделённым между thin data helper [`delivery-hub.ts`](../medusa-agency-boilerplate-storefront/src/lib/data/delivery-hub.ts), shared guardrail layer [`delivery-hub.ts`](../medusa-agency-boilerplate-storefront/src/lib/util/delivery-hub.ts) с shopper-safe normalization и отсечением leaked internal/provider-specific fragments, и preview-only util split в [`delivery-hub-preview.ts`](../medusa-agency-boilerplate-storefront/src/lib/util/delivery-hub-preview.ts). Последний approved storefront package уже закоммичен как `b2dabf5` `storefront: add delivery hub neutral selection rehearsal preview`: поверх этого neutral data layer materialized только **read-only neutral selection rehearsal preview** для shopper/storefront стороны. Следующим truthfully materialized шагом стал **pre-cutin read-only handoff preview seam**, затем **preview-only parity seam**, потом **read-only handoff contract matrix preview seam**, потом **preview-only persisted selection contract parity seam**, потом **preview-only projected commit parity seam**, потом **preview-only selection write seam preview**, потом **preview-only write-intent contract preview**, а теперь поверх того же preview stack materialized ещё и **preview-only selection payload parity seam**: в [`delivery-hub.ts`](../medusa-agency-boilerplate-storefront/src/lib/util/delivery-hub.ts) появился pure builder [`buildDeliveryHubSelectionPayloadParityPreviewModel()`](../medusa-agency-boilerplate-storefront/src/lib/util/delivery-hub.ts) и shopper-safe diagnostic model для future [`POST /store/delivery/selection`](../medusa-agency-boilerplate/src/api/store/delivery/selection/route.ts:58), который сравнивает projected storefront payload shape c expected neutral save-contract shape только по safe fields `connection_id`, `quote_type`, `quote_reference`, `pickup_point`, `pickup_window`, `selection_version`, shape completeness и blocked reasons, reuses только existing readonly preview seams вроде [`buildDeliveryHubSelectionWriteSeamPreviewModel()`](../medusa-agency-boilerplate-storefront/src/lib/util/delivery-hub.ts) и existing neutral candidate surfaces, возвращает preview-only vocabulary `matched | incomplete | blocked | informational_only`, field-level parity statuses, shopper-safe target label и hard-coded inert flags `mutation_intent=false`, `network_required_now=false`; в [`shipping/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/checkout/components/shipping/index.tsx) появился ещё один informational block рядом с existing rehearsal/parity/persisted/projected/write-seam/write-intent/handoff preview blocks, который truthfully показывает selection payload parity verdict, field-level parity и blocked/incomplete notes без CTA, submit wiring, persistence wiring или network intent; focused coverage в [`delivery-hub.spec.ts`](../medusa-agency-boilerplate-storefront/src/lib/util/delivery-hub.spec.ts) теперь отдельно фиксирует happy path для `matched`, incomplete cases по missing `quote_reference`, required `pickup_point`, required `pickup_window`, blocked state для degraded readiness/parity blockers, shopper-safe/no-secret boundary, no mutation wording и no-network posture для нового selection payload parity seam. Поверх preview seams теперь начат controlled storefront selection save/clear cut-in: [`shipping/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/checkout/components/shipping/index.tsx) wiring'ит [`saveDeliveryHubSelection()`](../medusa-agency-boilerplate-storefront/src/lib/data/delivery-hub.ts:151) и [`clearDeliveryHubSelection()`](../medusa-agency-boilerplate-storefront/src/lib/data/delivery-hub.ts:169) только за явными кнопками save/clear, а save дополнительно gated через pure helper [`buildDeliveryHubSelectionSaveCutInPayload()`](../medusa-agency-boilerplate-storefront/src/lib/util/delivery-hub.ts), который строит backend-ready neutral payload из existing rehearsal input и блокирует missing/stale/readiness/parity cases. Эти storefront seams больше не являются strictly read-only, но остаются selection-metadata-only: standard shipping method commit path остаётся generic and provider-neutral, Delivery Hub neutral save/clear/summary present, live shipment dispatch gated/not enabled, а удалённые старые provider-specific routes/helpers больше не являются template/runtime dependency. Backend execution guardrails остаются без изменений: [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:119) всё ещё hard-blocked, а contour [`DeliveryHubExecutionLedgerRepositoryAssemblyPersistenceReadinessContour`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-repository-assembly-scaffold.ts:161) по-прежнему фиксирует `artifact_defined -> manual_application_external -> snapshot_verification_available -> activation_blocked`.
- deferred: storefront cut-in, full shipping-option wiring/sync to `deliveryhub`, DB rollout/seed migration для shipping options, shipment/order widgets, live Yandex integration tests, legacy provider cutover/removal и mature multi-provider orchestration; planner, reconciliation, operation-plan и новый manual sync step intentionally не создают automatic rollout, не добавляют checkout/storefront UI changes и не делают checkout cutover; при этом warehouse surface пока intentionally minimal — без sync из провайдера, без mode matrix, без shipment lifecycle — а logs surface остается узким read-only scope без pagination/detail/export/replay/delete;
- известный contract nuance зафиксирован truthfully: успешный `Test connection` отдаёт correlation context внутри diagnostics, а успешный `Test quote` — отдельным top-level `correlation_id`; оба значения пишутся в event log storage и доступны через `GET /admin/delivery/logs`; manual shipping-option sync audit events уже materialized в том же logs contour как safe summary-only payload, при этом raw mutation payloads, merchant credentials, confirmation strings и прочие sensitive fragments по-прежнему не пишутся; отдельно `2026-04-22` закрыт remaining admin-only response-boundary gap: `GET /admin/delivery/providers`, `POST /admin/delivery/connections/:id/test`, `GET /admin/delivery/logs`, `GET /admin/delivery/shipping-options/preview` и `POST /admin/delivery/shipping-options/sync` теперь больше не пассивно пробрасывают provider/internal/secret-like fragments без route-level sanitation.
- новый post-tranche backend/admin-only execution gate seam materialized поверх execution-plan / identity-idempotency / persistence-audit previews: [`buildDeliveryHubShipmentExecutionPlanPreview()`](../medusa-agency-boilerplate/src/modules/delivery-hub/fulfillment-provider-bridge.ts) и [`buildDeliveryHubExecutionPlanObservabilityPreview()`](../medusa-agency-boilerplate/src/modules/delivery-hub/fulfillment-provider-bridge.ts) теперь включают deterministic dry-run `preflight_eligibility` verdict с `current_mode=preview_only`, `decision=eligible_when_enabled|not_ready`, stable reason codes, future-only inert `DELIVERY_HUB_SHIPMENT_EXECUTION_ENABLED` prerequisite name, required future prerequisites, confirmations that real execution/provider calls/persistence writes/checkout cutover remain disabled, and blocked live actions; поверх того же contour materialized deterministic `shipment_result_preview` object с neutral `deliveryhub_shipment_result` target, projected/not-materialized vocabulary, identity/idempotency linkage, sanitized artifact presence summary, blocked materialization actions и explicit confirmations что provider response fetch / adapter invocation / shipment creation / label persistence / order mutation / fulfillment persistence / checkout cutover остаются disabled; следующим узким шагом поверх уже существующих preview contours теперь materialized и deterministic `fulfillment_application_preview` object с neutral `medusa_fulfillment_mutation_plan` target, projected/not-applied vocabulary, mutation/persistence linkage presence, blocked application actions и explicit confirmations что order mutation / fulfillment persistence / shipment persistence / label persistence / event persistence / checkout cutover остаются disabled; поверх тех же contours теперь materialized и deterministic `failure_handling_preview` object с neutral preview-only vocabulary для future failure classification / retry / compensation posture (`projected_retry_policy|no_live_failure_path`, narrow failure classes, retry eligibility, compensation requirement, manual intervention markers, blocked failure actions и disabled confirmations), но без scheduler/materializer; следующим smallest-safe шагом поверх preview stack теперь materialized reusable controlled execution scaffold через [`shipment-execution-contract.ts`](../medusa-agency-boilerplate/src/modules/delivery-hub/shipment-execution-contract.ts): canonical neutral states `planned → reserved → dispatch_ready → dispatch_inflight → result_received → application_ready → completed` + `failed_blocked`, pure transition validation helpers, `can-start|reserve|dispatch|apply|finalize|fail-blocked` predicates, deterministic execution identity / reservation fingerprint contract, pure execution-record / reservation / audit drafts и explicit reservation drift comparison без БД writes и без live dispatch; [`buildDeliveryHubShipmentExecutionPlanPreview()`](../medusa-agency-boilerplate/src/modules/delivery-hub/fulfillment-provider-bridge.ts) теперь reuses этот scaffold внутри `persistence_audit_preview`, exposing contract-linked draft summaries instead of ad-hoc persistence placeholders, а admin route sanitation in [`shared.ts`](../medusa-agency-boilerplate/src/api/admin/delivery/shared.ts) truthfully keeps that exposure read-only/admin-safe with redaction for nested draft payloads and audit identity fields. Поверх этого backend/domain-storage-only contour теперь materialized explicit controlled execution ledger persistence boundary через [`execution-ledger-repository.ts`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-repository.ts) плюс controlled Postgres implementation [`execution-ledger-pg-repository.ts`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-pg-repository.ts): узкий repository port (`getExecutionByReference`, `getExecutionByIdempotencyKey`, `reserveExecution`, `recordTransition`, `appendAuditEvent`) reuses canonical controlled-execution drafts/states from [`shipment-execution-contract.ts`](../medusa-agency-boilerplate/src/modules/delivery-hub/shipment-execution-contract.ts), storage records/mappers from [`execution-ledger-storage-adapter-scaffold.ts`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-storage-adapter-scaffold.ts), descriptor/query-plan/transaction-plan builders from approved scaffolds, explicit injected [`DeliveryHubPgConnection`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/pg.ts) only, deterministic idempotent reserve semantics, reservation drift detection, canonical transition validation/current-state guard, redacted audit append and mapping round-trip. Следующим controlled persistence infrastructure tranche теперь materialized explicit inert Postgres schema artifact [`execution-ledger-pg-migration-artifact.ts`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-pg-migration-artifact.ts): `up` DDL creates descriptor-aligned main ledger table plus append-only transition/audit tables, idempotency unique constraint/index for `ON CONFLICT (idempotency_key) DO NOTHING`, deterministic `(execution_reference, sequence)` uniqueness and FK links to the main ledger; `down` DDL drops audit, transition, then main table. Текущий controlled persistence infrastructure tranche normalized только main-record refresh/update path: добавлен canonical inert descriptor [`buildDeliveryHubExecutionLedgerUpdateMainRecordQueryDescriptor()`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-storage-descriptor-scaffold.ts:530) для approved main payload columns `execution_payload`, `reservation_payload`, `transitions_payload`, `audit_events_payload` по ключу `execution_reference`, добавлен query-plan builder [`buildDeliveryHubExecutionLedgerRefreshMainRecordPlan()`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-query-plan-scaffold.ts:286), а PG repository [`updateMainRecord()`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-pg-repository.ts:392) теперь потребляет descriptor/query-plan SQL и bindings вместо inline bespoke SQL. Это не меняет schema artifact, не включает transaction runner, не активирует runtime wiring и не добавляет новые migration/table-creation semantics. Assembly readiness now truthfully reports `pg_repository_implementation_available` and `schema_migration_artifact_available=true`, while missing activation prerequisites still include migration/table creation, transaction runner, explicit runtime wiring, operational runbook and safety review because the artifact is reviewable only and is not auto-applied here. There are still no automatic migrations, no runtime DDL/table creation, no container resolution, no global singleton, no production registration and no call from hard-blocked [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:119); live execution, provider dispatch, shipment creation, order/fulfillment mutation, retry scheduling, compensation writes, checkout cutover and storefront write paths remain disabled.

- Текущий pure schema verification scaffold для execution-ledger persistence contract materialized в [`execution-ledger-schema-verification-scaffold.ts`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-schema-verification-scaffold.ts): verifier builds expected contract from approved [`listDeliveryHubExecutionLedgerStorageEntityDescriptors()`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-storage-descriptor-scaffold.ts:147) and inert [`buildDeliveryHubExecutionLedgerPgMigrationArtifact()`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-pg-migration-artifact.ts:44), then compares only supplied normalized plain snapshot objects for tables, columns, unique/index constraints and foreign keys. Result vocabulary is deterministic `compatible | incompatible` with structured mismatch inventory for missing table, table-name mismatch, missing column, missing unique/index constraint, missing foreign key and wrong foreign key target, plus source traceability for descriptor/artifact tables and columns checked. Focused coverage lives in [`delivery-hub-execution-ledger-schema-verification.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub-execution-ledger-schema-verification.unit.spec.ts) and covers default/custom table names, missing main/child tables, required payload columns, idempotency uniqueness, transition/audit sequence uniqueness, child-table FK missing/wrong target, deterministic repeated result and explicit no-connection/no-repository/no-DB-adapter requirement. This is pure snapshot comparison only: no DB connection, no runtime activation, no schema change, no migration application, no table creation, no SQL execution, no admin exposure and no change to hard-blocked [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:119).
- Следующий smallest-safe persistence step теперь materialized в [`execution-ledger-schema-check-plan-scaffold.ts`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-schema-check-plan-scaffold.ts): check-plan scaffold принимает только externally supplied plain schema snapshot, pure-но нормализует его в verifier-compatible [`DeliveryHubExecutionLedgerSuppliedSchemaSnapshot`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-schema-verification-scaffold.ts:73), строит deterministic operator-readable plan vocabulary `mode=pure_snapshot_check_plan`, `source=externally_supplied_schema_snapshot`, `verifier_mode=pure_snapshot_verification`, planned checks for tables / columns / unique-or-index constraints / foreign keys и затем делегирует compatibility authority существующему [`verifyDeliveryHubExecutionLedgerSchemaSnapshot()`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-schema-verification-scaffold.ts:243) без дублирования compatibility rules. Focused coverage lives in [`delivery-hub-execution-ledger-schema-check-plan.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub-execution-ledger-schema-check-plan.unit.spec.ts) and locks compatible plan generation, normalization/delegation parity with verifier, preserved incompatible mismatches, custom `table_name`, stable repeated output and explicit disabled confirmations that DB connection, DB introspection, repository requirement, SQL execution, migration application, runtime table creation, runtime wiring, transaction runner and admin exposure all remain false. This scaffold is pure snapshot-only adapter/orchestrator: no DB connection, no introspection, no runtime activation, no schema change, no migration application, no table creation, no SQL execution, no admin exposure and no change to hard-blocked [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:119).
- Текущий approved plan-only/admin-unexposed readiness enrichment теперь materialized в [`buildDeliveryHubExecutionLedgerRepositoryAssemblyReadiness()`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-repository-assembly-scaffold.ts:229): assembly readiness стал canonical machine-readable source of truth для contiguous persistence-readiness contour `artifact_defined -> manual_application_external -> snapshot_verification_available -> activation_blocked`. Readiness now truthfully surfaces descriptor layer, inert migration artifact, pure schema verification scaffold and pure schema check-plan scaffold as review/manual-preparation capabilities available now, while separately encoding that schema application remains external/manual and runtime activation remains blocked. Новый [`schema_verification_layer`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-repository-assembly-scaffold.ts:123) explicitly confirms `verifier_available=true`, `check_plan_available=true`, `verifier_source=supplied_snapshot_only`, and all guardrails remain disabled: no DB connection, no DB introspection, no repository prerequisite, no DB adapter prerequisite, no SQL execution, no migration application, no runtime table creation, no runtime wiring, no transaction runner and no admin exposure. Focused repository readiness coverage in [`delivery-hub-execution-ledger-repository.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub-execution-ledger-repository.unit.spec.ts) now locks default/custom-table readiness semantics, deterministic repeated output, preserved repository status mapping/component inventory/operation readiness/migration layer/disabled confirmations, and preserved `missing_activation_prerequisites` including manual migration or table creation, transaction runner, explicit runtime wiring, operational runbook and safety review. Current tranche sync is documentation-only: canonical operator runbook now lives in [`delivery_hub_execution_ledger_manual_runbook.md`](./delivery_hub_execution_ledger_manual_runbook.md), and canonical operator/reviewer evidence package format now lives in [`delivery_hub_execution_ledger_manual_evidence_template.md`](./delivery_hub_execution_ledger_manual_evidence_template.md). The new evidence template truthfully materializes one docs-defined handoff/checklist contract for artifact identity review, external environment classification without secrets, manual external application confirmation, supplied snapshot provenance, plain snapshot shaping aligned with [`DeliveryHubExecutionLedgerExternallySuppliedPlainSchemaSnapshot`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-schema-check-plan-scaffold.ts:47), [`DeliveryHubExecutionLedgerPlainSchemaTableSnapshot`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-schema-check-plan-scaffold.ts:37), [`DeliveryHubExecutionLedgerSuppliedSchemaSnapshot`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-schema-verification-scaffold.ts:73) and [`normalizeDeliveryHubExecutionLedgerSuppliedSchemaSnapshot()`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-schema-check-plan-scaffold.ts:152), plus check-plan/verifier verdict summary through [`buildDeliveryHubExecutionLedgerSchemaCheckPlan()`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-schema-check-plan-scaffold.ts:124), [`runDeliveryHubExecutionLedgerSchemaCheckPlan()`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-schema-check-plan-scaffold.ts:146) and [`verifyDeliveryHubExecutionLedgerSchemaSnapshot()`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-schema-verification-scaffold.ts:243), while preserving final contour posture `activation_blocked`. This tranche enriches documentation/reporting only: no DB connection, no DB access, no introspection, no SQL execution, no migration execution by app runtime, no runtime wiring, no admin exposure, no activation and no change to hard-blocked [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:119).
- Следующий ровно-один approved `planning/scaffold` step теперь materialized как локальный offline validator scaffold для externally supplied JSON snapshot: pure helper [`runDeliveryHubExecutionLedgerLocalOfflineValidator()`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-local-offline-validator-scaffold.ts:98) локально читает supplied JSON file, валидирует canonical externally supplied fixture contract через [`validateDeliveryHubExecutionLedgerSuppliedSchemaSnapshotFixtureContract()`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-schema-check-plan-scaffold.ts:180), затем передаёт authority существующему pure stack через [`buildDeliveryHubExecutionLedgerSchemaCheckPlan()`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-schema-check-plan-scaffold.ts:124), а стабильно emitted evidence-oriented JSON envelope сохраняет тот же versioned shape с artifact/invocation identity, input file identity, resolved table name, deterministic summary (`compatible | incompatible`, mismatch count, explicit `activation_blocked` / local-offline posture), lossless mismatch inventory и explicit disabled/non-runtime confirmations. Thin local entrypoint lives at [`validateDeliveryHubExecutionLedgerSnapshot()`](../medusa-agency-boilerplate/src/scripts/validate-delivery-hub-execution-ledger-snapshot.ts:11) and remains only a local `medusa exec` wrapper against externally supplied snapshot JSON; stdout is still canonical JSON envelope only, exit codes remain explicit/reviewable (`0` = syntactically valid `compatible`, `2` = syntactically valid `incompatible`, `3` = malformed input or contract failure), and it still does not add DB access, DB introspection, runtime DDL, migration application, repository activation, transaction-runner activation, runtime wiring, admin/public exposure or any activation path. This same step now truthfully standardizes one canonical fixture contract around [`normalizeDeliveryHubExecutionLedgerSuppliedSchemaSnapshot()`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-schema-check-plan-scaffold.ts:167): code-level contract metadata [`DELIVERY_HUB_EXECUTION_LEDGER_SUPPLIED_SCHEMA_SNAPSHOT_FIXTURE_CONTRACT`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-schema-check-plan-scaffold.ts:57), contract alias [`DeliveryHubExecutionLedgerSuppliedSchemaSnapshotFixtureContract`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-schema-check-plan-scaffold.ts:54), canonical validator [`validateDeliveryHubExecutionLedgerSuppliedSchemaSnapshotFixtureContract()`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-schema-check-plan-scaffold.ts:180) and small docs note [`delivery_hub_execution_ledger_snapshot_fixture_contract.md`](./delivery_hub_execution_ledger_snapshot_fixture_contract.md) together now make accepted, optional, normalized, ignored and rejected fixture semantics explicit while preserving existing pure verification authority and offline-only posture. Focused coverage in [`delivery-hub-execution-ledger-schema-check-plan.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub-execution-ledger-schema-check-plan.unit.spec.ts:1) and [`delivery-hub-execution-ledger-local-offline-validator.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub-execution-ledger-local-offline-validator.unit.spec.ts:1) now additionally locks canonical minimal fixture, explicit column-object fixture, custom `table_name` fixture, intentionally mismatching fixture example, deterministic contract-boundary rejection for malformed/drifted fixture shapes, plus preserved stdout/exit semantics at the local script boundary. `activation_blocked` plus hard-blocked [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:119) remain unchanged.
- Текущий ровно-один approved docs-only bundle index and release-handoff step теперь materialized в [`delivery_hub_execution_ledger_release_handoff_index.md`](./delivery_hub_execution_ledger_release_handoff_index.md): новый документ intentionally остаётся navigation-only artifact и truthfully cross-links existing authorities for readiness, exact contour wording, manual workflow, evidence package, fixture-contract semantics and local offline validator semantics without introducing a second source of truth. Index explicitly points back to [`buildDeliveryHubExecutionLedgerRepositoryAssemblyReadiness()`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-repository-assembly-scaffold.ts:229), preserves exact contour `artifact_defined -> manual_application_external -> snapshot_verification_available -> activation_blocked` via [`DeliveryHubExecutionLedgerRepositoryAssemblyPersistenceReadinessContour`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-repository-assembly-scaffold.ts:161), identifies [`delivery_hub_execution_ledger_manual_runbook.md`](./delivery_hub_execution_ledger_manual_runbook.md), [`delivery_hub_execution_ledger_manual_evidence_template.md`](./delivery_hub_execution_ledger_manual_evidence_template.md), [`delivery_hub_execution_ledger_snapshot_fixture_contract.md`](./delivery_hub_execution_ledger_snapshot_fixture_contract.md), [`validateDeliveryHubExecutionLedgerSuppliedSchemaSnapshotFixtureContract()`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-schema-check-plan-scaffold.ts:184), [`runDeliveryHubExecutionLedgerLocalOfflineValidator()`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-local-offline-validator-scaffold.ts:98) and [`validateDeliveryHubExecutionLedgerSnapshot()`](../medusa-agency-boilerplate/src/scripts/validate-delivery-hub-execution-ledger-snapshot.ts:11) as the canonical authorities, and explicitly states that validator output remains evidence-oriented JSON only, fixture-contract authority remains on the approved fixture-contract surfaces, nothing in the index implies runtime activation, and hard-blocked [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:119) remains unchanged. This tranche is docs-only status sync only: no runtime, persistence, validator behavior, fixture authority or activation posture changed.
- Следующий ровно-один approved docs-only evidence-submission checklist refinement теперь materialized в [`delivery_hub_execution_ledger_evidence_submission_checklist.md`](./delivery_hub_execution_ledger_evidence_submission_checklist.md): новый canonical checklist document фиксирует reviewer-ready intake contract для manual/offline execution-ledger handoff без расширения capability, explicitly разделяет required narrative fields и required attached artifacts, задаёт canonical attachment set, completeness matrix, validator JSON envelope attachment rule when used, mismatch versus no-mismatch declaration requirement, return conditions for incomplete packages и docs-level disposition outcomes `complete_for_review`, `incomplete_return_to_operator`, `review_complete_activation_still_blocked`. Existing docs minimally linked truthfully: [`delivery_hub_execution_ledger_manual_evidence_template.md`](./delivery_hub_execution_ledger_manual_evidence_template.md) теперь ссылается на canonical intake checklist for completeness/return conditions, [`delivery_hub_execution_ledger_manual_runbook.md`](./delivery_hub_execution_ledger_manual_runbook.md) points to reviewer-ready submission completeness before stopping at `activation_blocked`, and [`delivery_hub_execution_ledger_release_handoff_index.md`](./delivery_hub_execution_ledger_release_handoff_index.md) now includes the checklist in the authority-preserving handoff map. This refinement explicitly does not redefine [`buildDeliveryHubExecutionLedgerRepositoryAssemblyReadiness()`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-repository-assembly-scaffold.ts:229), [`validateDeliveryHubExecutionLedgerSuppliedSchemaSnapshotFixtureContract()`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-schema-check-plan-scaffold.ts:184), [`runDeliveryHubExecutionLedgerLocalOfflineValidator()`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-local-offline-validator-scaffold.ts:98), [`validateDeliveryHubExecutionLedgerSnapshot()`](../medusa-agency-boilerplate/src/scripts/validate-delivery-hub-execution-ledger-snapshot.ts:11) or activation authority; exact contour `artifact_defined -> manual_application_external -> snapshot_verification_available -> activation_blocked` remains unchanged, no code/runtime changes were introduced, and hard-blocked [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:119) remains unchanged.
- Следующий ровно-один approved docs-only evidence bundle packaging and naming convention refinement теперь materialized в [`delivery_hub_execution_ledger_evidence_bundle_packaging_convention.md`](./delivery_hub_execution_ledger_evidence_bundle_packaging_convention.md): новый canonical convention document фиксирует stable package identity format, sanitized environment label convention, UTC timestamp convention, revision suffix convention для resubmissions, canonical attachment naming table, canonical bundle layout с mandatory versus conditional attachments, explicit metadata-only rule для folder and filename labels, package-ID binding rules across all attachments, reviewer packaging checks и return triggers for ambiguous or incomplete naming. Existing docs minimally linked truthfully: [`delivery_hub_execution_ledger_manual_evidence_template.md`](./delivery_hub_execution_ledger_manual_evidence_template.md) now points to the packaging convention for bundle identity and attachment naming determinism, [`delivery_hub_execution_ledger_evidence_submission_checklist.md`](./delivery_hub_execution_ledger_evidence_submission_checklist.md) now requires canonical package identity and naming alignment for intake completeness, [`delivery_hub_execution_ledger_release_handoff_index.md`](./delivery_hub_execution_ledger_release_handoff_index.md) now includes the packaging convention in the authority-preserving handoff map, and [`delivery_hub_execution_ledger_manual_runbook.md`](./delivery_hub_execution_ledger_manual_runbook.md) now points operators to package the evidence bundle before stopping at `activation_blocked`. This tranche is docs-only guidance only: it does not change validator behavior, does not redefine fixture-contract authority, does not redefine readiness authority, does not change runtime state, does not change activation posture, preserves exact contour `artifact_defined -> manual_application_external -> snapshot_verification_available -> activation_blocked`, and preserves hard-blocked [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:119).
- Docs authority sync for the operator/reviewer quickstart is now truthfully reconciled around existing surfaces: [`delivery_hub_execution_ledger_operator_reviewer_quickstart.md`](./delivery_hub_execution_ledger_operator_reviewer_quickstart.md) remains the approved navigation-only artifact for the operator/reviewer entry path without becoming a second source of truth. The quickstart continues to send readers back to readiness authority in [`buildDeliveryHubExecutionLedgerRepositoryAssemblyReadiness()`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-repository-assembly-scaffold.ts:229), manual workflow authority in [`delivery_hub_execution_ledger_manual_runbook.md`](./delivery_hub_execution_ledger_manual_runbook.md), evidence package/checklist/packaging authorities in [`delivery_hub_execution_ledger_manual_evidence_template.md`](./delivery_hub_execution_ledger_manual_evidence_template.md), [`delivery_hub_execution_ledger_evidence_submission_checklist.md`](./delivery_hub_execution_ledger_evidence_submission_checklist.md) and [`delivery_hub_execution_ledger_evidence_bundle_packaging_convention.md`](./delivery_hub_execution_ledger_evidence_bundle_packaging_convention.md), fixture-contract authority in [`delivery_hub_execution_ledger_snapshot_fixture_contract.md`](./delivery_hub_execution_ledger_snapshot_fixture_contract.md) and [`validateDeliveryHubExecutionLedgerSuppliedSchemaSnapshotFixtureContract()`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-schema-check-plan-scaffold.ts:184), plus offline-validator authority in [`runDeliveryHubExecutionLedgerLocalOfflineValidator()`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-local-offline-validator-scaffold.ts:99) and [`validateDeliveryHubExecutionLedgerSnapshot()`](../medusa-agency-boilerplate/src/scripts/validate-delivery-hub-execution-ledger-snapshot.ts:11). [`delivery_hub_execution_ledger_manual_runbook.md`](./delivery_hub_execution_ledger_manual_runbook.md) already contains the minimal navigation-only pointer to the quickstart, so this reconciliation keeps that pointer as-is and does not expand scope. This entry is intentionally recorded as docs authority reconciliation, not as a claim that the literal latest repository step or latest diff was docs-only. Validator behavior, fixture authority, readiness authority, runtime state and activation posture remain unchanged; canonical contour remains exactly `artifact_defined -> manual_application_external -> snapshot_verification_available -> activation_blocked`; validator output remains evidence-oriented JSON only; nothing implies runtime activation; and hard-blocked [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:119) remains unchanged.

- Previous test-only repository-to-migration compatibility simulation harness remains materialized only in focused unit coverage [`delivery-hub-execution-ledger-repository.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/delivery-hub-execution-ledger-repository.unit.spec.ts): test-local fake [`raw`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/pg.ts:10) connection captures SQL, bindings and statement order, returns synthetic rows/rowCount, never connects to DB and never applies DDL. Coverage drives repository methods themselves for reserve success, conflict/reread, transition refresh and audit refresh, compares emitted SQL/bindings to descriptor/query-plan builders including normalized [`buildDeliveryHubExecutionLedgerRefreshMainRecordPlan()`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-query-plan-scaffold.ts:289), checks default/custom table names against inert [`buildDeliveryHubExecutionLedgerPgMigrationArtifact()`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-pg-migration-artifact.ts:44), and locks [`updateMainRecord()`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-pg-repository.ts:392) to update only `execution_payload`, `reservation_payload`, `transitions_payload`, `audit_events_payload` filtered by `execution_reference` with descriptor binding order. This remains test-only proof: no runtime activation, no schema change, no DB connection, no migration application, no transaction-runner wiring and no change to hard-blocked [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:119).

`Payload CMS v1` как content layer маркетинговых страниц уже реализован и закрыт как отдельный workstream после стабилизации storefront core.
В каноническом плане это больше не будущий трек **Фаза 5.5**, а завершённый source-of-truth этап перед переходом к **Фазе 6** с client customization.

Отдельная обязательная рамка текущего sequencing:

- этот master repo по умолчанию делается для **российского рынка**;
- для payments текущий v1 path остается **YooKassa-first**;
- для shipping следующий целевой track по умолчанию — **собственный `delivery-hub` с `Yandex-first` adapter path**;
- решения, не подходящие для типового магазина в РФ, нельзя выбирать как default path только потому, что они лучше документированы в экосистеме Medusa.

### Текущий статус

Подтвержден честный clean-state сценарий:

1. `cp .env.example .env`
2. `npm run bootstrap`
3. `npm run preflight`
4. `npm run dev`

После этого подтвержден template-ready backend baseline:

- `ru` region;
- `rub` как baseline currency;
- publishable API key;
- default sales channel;
- минимальный shipping skeleton;
- отсутствие обязательного demo-catalog baseline.

Дополнительно подтвержден storefront fix:

- edge-case с redirect-loop в `medusa-agency-boilerplate-storefront/src/middleware.ts` исправлен;
- повторная проверка подтвердила, что loop больше не воспроизводится;
- нормальная семантика middleware сохранена.

Дополнительно подтвержден закрытый результат **notification hardening v1**:

- baseline-safe режим без внешних notification secrets подтвержден;
- `NOTIFICATION_EMAIL_PROVIDER=local` остается baseline-default;
- `unisender` без `UNISENDER_API_KEY` не ломает startup, build и runtime и корректно падает обратно на local provider;
- authenticated smoke path реально работает через secret admin API key и Basic auth;
- helper `createSecretAdminApiKey()` теперь обязан создавать свежий `sk_*` key для канонического smoke path и не полагается на reuse старого считанного token;
- route `POST /admin/notifications/smoke` возвращает стабильную форму ответа с блоками `ok`, `request`, `auth`, `provider`, `notification`;
- workflow и runtime helper согласованы по `requested provider` и `resolved provider`;
- документация и skill не должны содержать реальные ключи, токены и секреты.

Это значит:

- **Gate A закрыт для clean local onboarding**;
- **Фаза 1 подтверждена как baseline локального старта**;
- **Фаза 2 подтверждена полностью, без оговорок**;
- **notification track Фазы 3 закрыт на уровне hardening v1**;
- **payment track v1 подтвержден для текущего YooKassa-first scope** и не является активным кодовым blocker;
- **shipping slice `provider_aware_v1` подтверждён runtime-проверкой** `2026-04-20`: production token получен и активирован, provider включён через [`medusa-config.ts`](../medusa-agency-boilerplate/medusa-config.ts), route [`GET /store/delivery/rates`](../medusa-agency-boilerplate/src/api/store/delivery/rates/route.ts) подтверждён, grouped rates из the historical Yandex-backed rate path начали возвращаться;
- blocker по legacy provider **закрыт**: проблема оказалась не в account-state, а была устранена targeted code fixes в [`route.ts`](../medusa-agency-boilerplate/src/api/store/delivery/rates/route.ts) и [`seed.ts`](../medusa-agency-boilerplate/src/scripts/seed.ts);
- **`delivery-hub` tranche-1 materialized как параллельный foundation layer**: direct `Yandex Delivery` admin path, encrypted connection handling, admin page [`page.tsx`](../medusa-agency-boilerplate/src/admin/routes/settings/delivery/page.tsx), minimal local warehouse domain, admin/store response-boundary hardening и focused no-network coverage уже есть; отдельно materialized minimal pure admin page seam [`page-state.ts`](../medusa-agency-boilerplate/src/admin/routes/settings/delivery/page-state.ts) для derived/render-state coverage, а storefront surface ограничена neutral selection metadata save/clear/summary поверх neutral data layer [`delivery-hub.ts`](../medusa-agency-boilerplate-storefront/src/lib/data/delivery-hub.ts) и shared guardrail layer [`delivery-hub.ts`](../medusa-agency-boilerplate-storefront/src/lib/util/delivery-hub.ts). Quote boundary remediation `2026-04-21` закрыла leak internal `quote_key` через backend-issued opaque `quote_reference`; standard shipping method commit остаётся generic/provider-neutral, без provider-specific commit payloads и без live dispatch.
- для подтверждённого runtime path использовался production token с explicit live-mode opt-in;
- storefront `500` на checkout отделен от shipping slice и снят как ложный blocker: при валидном cart checkout route отвечает `200`;
- **checkout end-to-end validation v1 подтвержден runtime/E2E pass**: полный flow `shipping → hosted YooKassa payment → automatic return → review → order placement → confirmed order page` реально пройден;
- targeted storefront fixes для этого pass зафиксированы в [`payment-button/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/checkout/components/payment-button/index.tsx) и [`cookies.ts`](../medusa-agency-boilerplate-storefront/src/lib/data/cookies.ts): YooKassa больше не инициирует преждевременный `placeOrder()`, а cart cookie сохраняется с `sameSite: "lax"` для cross-site return;
- **bootstrap idempotency hardening v1 подтвержден runtime validation**: clean DB, dirty DB idempotent rerun и dirty DB conflict injection — все три сценария прошли;
- **`storefront core baseline v1` закрыт и больше не считается current work**: shared storefront shell, runtime/config baseline и starter/demo cleanup синхронизированы как предыдущий repo-level шаг и зафиксированы коммитом `6f9a5499e2c9fcf08e2e6d1fffa75f350e82f5bb`;
- повторный `npm run bootstrap` поверх уже заполненной БД теперь является подтвержденным idempotent path: seed reuse-or-fail семантика корректно работает, publishable key стабильно извлекается, дубликаты не создаются;
- при конфликтном состоянии базы (несколько publishable keys без baseline title match) bootstrap корректно завершается с exit code 1 и не обновляет storefront env.

### Уже закреплено и остается опорой

Подтверждено кодом и прогоном:

- добавлен корневой orchestration-слой через [package.json](../package.json) и папку [scripts/](../scripts);
- добавлены команды `bootstrap`, `preflight`, `dev`, `backend:build`, `storefront:build`, `smoke:backend`, `permissions:fix`;
- `permissions:fix` чинит `.medusa` и `node_modules/.vite`;
- bootstrap использует application-level seed из [medusa-agency-boilerplate/src/scripts/seed.ts](../medusa-agency-boilerplate/src/scripts/seed.ts), а не SQL dump как канонический путь;
- root `npm run dev` подтвержден в схеме:
  - backend через `docker compose`;
  - storefront локально;
  - root preflight перед стартом;
- env-контракт зафиксирован в [env_contract.md](./env_contract.md);
- добавлен [scripts/env-sync.sh](../scripts/env-sync.sh) для синхронизации backend и storefront env из root `.env`;
- добавлен tracked-шаблон [medusa-agency-boilerplate-storefront/.env.local.example](../medusa-agency-boilerplate-storefront/.env.local.example);
- clean bootstrap теперь поднимает template-ready RU baseline вместо demo-oriented baseline.

- notification slice v1 и notification hardening v1 подтверждены в backend-only контуре:
  - Notification Module;
  - local provider для dev и baseline-default;
  - `UniSender` path для production;
  - fallback к local provider при `unisender` без `UNISENDER_API_KEY`;
  - provider-agnostic workflow;
  - admin smoke route;
  - authenticated smoke через Basic auth и secret admin API key;
  - opt-in helper для on-demand fresh secret admin API key.
  - roadmap уже больше не описывает SendGrid как актуальный bridge: целевой email provider для шаблона зафиксирован как `UniSender` для service и marketing email.
- payment slice v1 реализован как YooKassa-first minimal vertical slice и ранее подтвержден end-to-end для текущего payment scope:
  - provider registration;
  - session initiation;
  - hosted redirect and return path;
  - webhook and status handling;
  - minimal storefront provider-aware adaptation.
- env-contract для payment path расширен opt-in переменными:
  - `YOOKASSA_SHOP_ID`;
  - `YOOKASSA_SECRET_KEY`;
  - `YOOKASSA_RETURN_URL`;
  - `YOOKASSA_STOREFRONT_RETURN_ORIGINS`;
  - `YOOKASSA_WEBHOOK_URL`;
  - `YOOKASSA_WEBHOOK_SECRET`;
  - `YOOKASSA_ALLOW_UNSIGNED_WEBHOOKS`;
  - storefront flag `NEXT_PUBLIC_YOOKASSA_ENABLED`.
- shipping slice v1 реализован как backend-first legacy provider rate-selection slice and now explicitly fenced as deprecated legacy compatibility surface; default/new fulfillment contour for fresh templates is Delivery Hub/direct Yandex:
  - Delivery Hub default contract в [`fulfillment-contour-contract.ts`](../medusa-agency-boilerplate/src/modules/fulfillment-contour-contract.ts) and default provider registration в [`medusa-config.ts`](../medusa-agency-boilerplate/medusa-config.ts);
  - legacy provider opt-in provider registration в [medusa-config.ts](../medusa-agency-boilerplate/medusa-config.ts);
  - deprecated fulfillment provider в the removed backend fulfillment-provider module;
  - store route для rate lookup в [src/api/store/delivery/rates/route.ts](../medusa-agency-boilerplate/src/api/store/delivery/rates/route.ts);
  - seed-path для shipping option в [src/scripts/seed.ts](../medusa-agency-boilerplate/src/scripts/seed.ts);
  - storefront data layer в the removed storefront legacy delivery helper и [src/lib/data/cart.ts](../medusa-agency-boilerplate-storefront/src/lib/data/cart.ts);
  - checkout shipping selection и restore/summary guardrails в [src/modules/checkout/components/shipping/index.tsx](../medusa-agency-boilerplate-storefront/src/modules/checkout/components/shipping/index.tsx) и [src/modules/checkout/components/shipping-summary/index.tsx](../medusa-agency-boilerplate-storefront/src/modules/checkout/components/shipping-summary/index.tsx);
  - safe-by-default legacy provider test/live mode: пустое/невалидное значение теперь оставляет integration в test-mode, а live включается только явным `false`;
  - root orchestration доведен до той же семантики: [scripts/env-sync.sh](../scripts/env-sync.sh) теперь пишет в backend env безопасный default safe test-mode fallback, если переменная отсутствует в root `.env`;
  - локальный [medusa-agency-boilerplate/.env](../medusa-agency-boilerplate/.env) выровнен под эту политику и больше не хранит explicit live-mode opt-in как silent live-default;
  - route quotes сохраняют backward-compatible ETA mapping по `daysMin/daysMax` и добавляют fallback на `workDays*` и `calendarDays*`;
  - request error handling теперь сохраняет status/code/message/description и отдельную diagnosability для account-state, billing и config failures без логирования токена;
  - текущая storefront checkout-семантика теперь truthfully зафиксирована как `provider_aware_v2`: grouped quotes и pickup points refetch-ятся по address fingerprint внутри той же корзины, recoverable ошибки и отсутствие тарифов остаются видимым UI state вместо silent disappearance shipping option, а persisted selection различает shopper mode key, реальный `shipping_option.id` и address-bound freshness guardrails.
- подтверждено verification pass, что baseline не сломан:
  - `npm run bootstrap`, `npm run preflight` и `npm run dev` продолжают работать без обязательных payment secrets, без обязательных credentials удалённого legacy provider и без обязательных внешних notification secrets;
  - notification runtime сохраняет baseline-safe semantics и не требует `UNISENDER_API_KEY` для local default path;
  - Delivery Hub provider is the default/new contour and starts without new required env; first real direct Yandex runtime dispatch now exists only behind the optional strict gate [`DELIVERY_HUB_SHIPMENT_EXECUTION_ENABLED`](./env_contract.md), and the first narrow execution-ledger activation slice now truthfully persists canonical reservation/result transitions plus safe shipment-to-ledger linkage only for accepted results on the already ready gated direct-Yandex contour; the accepted shipment lifecycle read-model now exposes only sanitized backend-only snapshots for accepted persisted shipments and explicit non-accepted classifications for provider-failed, failed-blocked, replay, duplicate and drift contours; accepted-only Yandex status polling now writes only redacted neutral markers for accepted persisted shipments, and the admin/backend shipment operations surface now exposes a sanitized snapshot plus guarded manual refresh/cancel/retry actions by execution reference without raw provider identifiers or execution secrets; completed replay, failed replay, duplicate exact non-terminal reservations and drifted reservations stay pre-dispatch zero-call blockers and do not claim executed state or create misleading shipment records, while webhooks/auto-retry scheduling/order mutation and fulfillment mutation remain outside the active slice;
  - legacy provider реально включается только при наличии токена и теперь является deprecated legacy compatibility surface, not recommended for fresh stores;
  - safe-by-default закрыт не только в runtime-коде, но и на orchestration/env-sync уровне: отсутствие legacy provider test/live mode setting в root env после sync больше не ведет backend env в live;
  - shipping option the historical courier shipping option появляется после повторного seed в historical provider-enabled окружении;
  - storefront `500` на checkout оказался runtime and data-state проблемой отсутствующего или невалидного cart, а не подтвержденной code regression;
  - payload mapping, endpoint usage и live and test режим по текущей реализации выглядят корректно;
  - расширение env-contract осталось opt-in и baseline-safe.

### Статус после закрытия `marketing layer v1`

**`storefront core baseline v1` уже закрыт коммитом `6f9a5499e2c9fcf08e2e6d1fffa75f350e82f5bb`, `VK ID v1` закрыт коммитом `f48a02658d116a04afd794c1134ac72e0ab00bc8` `feat(vk): add vk id linking v1`, `MTS Exolve` закрыт коммитом `b13f6fa93473bb8bc0320566a75d264d60739784` `feat(notification): add MTS Exolve SMS workstream`, а `marketing layer v1` закрыт коммитом `a4711906b16523dcf03da9601ccf1a914702ca7d` `feat(marketing-layer): add marketing preferences and campaign workflows`.**

Новый подтвержденный результат закрытого `marketing layer v1` шага:

- metadata-first consent/preferences contract материализован в `customer.metadata.marketing`: каноническая структура описана типом [`MarketingPreferences`](../medusa-agency-boilerplate/src/modules/marketing-preferences.ts:37), а channel bindings нормализуются через [`resolveMarketingBindings()`](../medusa-agency-boilerplate/src/modules/marketing-preferences.ts:202) из existing customer email, phone и VK metadata surfaces;
- customer self-service и admin updates пишут обратно в один и тот же metadata contract через store route [`POST()`](../medusa-agency-boilerplate/src/api/store/customers/me/marketing-preferences/route.ts:80) и admin route [`PUT()`](../medusa-agency-boilerplate/src/api/admin/marketing/campaigns/route.ts:110), а не через отдельный consent storage;
- campaign and journal surface материализованы в [`ensureMarketingLayerTables()`](../medusa-agency-boilerplate/src/modules/marketing-layer.ts:305) как внутренние таблицы `marketing_campaign` и `marketing_delivery_journal` с audience selection, frequency cap и delivery audit trail;
- execution semantics теперь зафиксированы как single-channel per campaign: каждая campaign record хранит один `channel`, а [`sendMarketingCampaignWorkflow`](../medusa-agency-boilerplate/src/workflows/send-marketing-campaign.ts:508) при launch использует ровно один delivery runtime, пишет `sent/skipped/failed` journal rows, уважает global/channel consent, `suppressed_until`, recipient binding availability и frequency cap;
- store API surface материализован в [`GET()`](../medusa-agency-boilerplate/src/api/store/customers/me/marketing-preferences/route.ts:45) и [`POST()`](../medusa-agency-boilerplate/src/api/store/customers/me/marketing-preferences/route.ts:80) для `/store/customers/me/marketing-preferences`;
- admin API surface материализован в campaign list/create/preferences route [`GET()`](../medusa-agency-boilerplate/src/api/admin/marketing/campaigns/route.ts:72), [`POST()`](../medusa-agency-boilerplate/src/api/admin/marketing/campaigns/route.ts:85), [`PUT()`](../medusa-agency-boilerplate/src/api/admin/marketing/campaigns/route.ts:110) и в per-campaign route [`GET()`](../medusa-agency-boilerplate/src/api/admin/marketing/campaigns/[id]/route.ts:28) / [`POST()`](../medusa-agency-boilerplate/src/api/admin/marketing/campaigns/[id]/route.ts:62); auth and validation boundary синхронизирован через [`defineMiddlewares()`](../medusa-agency-boilerplate/src/api/middlewares.ts:20);
- storefront profile preferences section материализован в компоненте [`ProfileMarketingPreferences`](../medusa-agency-boilerplate-storefront/src/modules/account/components/profile-marketing-preferences/index.tsx:92), data helpers [`retrieveMarketingPreferences()`](../medusa-agency-boilerplate-storefront/src/lib/data/marketing.ts:61) и [`updateMarketingPreferences()`](../medusa-agency-boilerplate-storefront/src/lib/data/marketing.ts:80), а также встраивании в [`Profile()`](../medusa-agency-boilerplate-storefront/src/app/[countryCode]/(main)/account/@dashboard/profile/page.tsx:35);
- validation and review outcome закрыты: backend typecheck PASS, storefront typecheck PASS, targeted tests в [`marketing-layer.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/marketing-layer.unit.spec.ts:27) PASS `1` suite / `6` tests, review verdict = `approve`;
- review notes остались только non-blocker: admin [`PUT()`](../medusa-agency-boilerplate/src/api/admin/marketing/campaigns/route.ts:110) всё ещё опирается на `customer_id` query ergonomics, а dynamic route пока использует manual URL parsing в [`getCampaignId()`](../medusa-agency-boilerplate/src/api/admin/marketing/campaigns/[id]/route.ts:21).

Это значит для дальнейшего sequencing:

- не переоткрывать `storefront core baseline v1`, `VK Community Messaging v1 foundation`, `VK ID v1`, `MTS Exolve`, `marketing layer v1` и теперь уже закрытый `Payload CMS v1` без нового evidence о регрессии;
- approved communication stack теперь materialized end-to-end как `UniSender + VK Community Messaging + optional VK ID + MTS Exolve + marketing layer`, при этом role split остается прежним: `Payload = content`, `admin = operations`, `marketing layer = orchestration`;
- `Payload CMS v1` уже зафиксирован как закрытый workstream: отдельный app [`payload-cms`](../payload-cms), storefront content integration, preview/revalidate path, globals и fallback behaviour materialized и закоммичены как [`22486388f4c89d884b4c3cbe668ebec4ab695dee`](../package.json:1) `feat(content): add Payload CMS marketing content layer`;
- storefront build blocker перед `Фазой 6` дополнительно закрыт: [`generateStaticParams()`](../medusa-agency-boilerplate-storefront/src/app/[countryCode]/(main)/categories/[...category]/page.tsx:19), [`generateStaticParams()`](../medusa-agency-boilerplate-storefront/src/app/[countryCode]/(main)/collections/[handle]/page.tsx:21) и уже защищенный [`generateStaticParams()`](../medusa-agency-boilerplate-storefront/src/app/[countryCode]/(main)/products/[handle]/page.tsx:15) теперь не роняют `next build` при недоступном Store API, а деградируют до пустых static params;
- активным roadmap step теперь считать **Фазу 6**: client customization layer поверх уже materialized commerce-core и content-layer, без повторного открытия Payload integration track;
- `Фаза 6` уже продвинулась дальше стартового foundation slice: [`storefront-client-config.ts`](../medusa-agency-boilerplate-storefront/src/lib/storefront-client-config.ts) теперь удерживает sanctioned preset catalog `atelier|market`, env-driven switch `NEXT_PUBLIC_STOREFRONT_PRESET`, anti-fork guardrails и два реально разных client scenarios на одном storefront core; typed section registry и preset-driven composition уже покрывают `home`, `collectionLanding`, `contentPage` и `postPage`, а adjacent product support surface продолжает жить в том же customization layer без дублирования cart/checkout/account/order logic;
- не смешивать текущий client customization track с template release packaging до соответствующих фаз плана;
- сохранять distinction между каноническим root clean-start workflow и ad-hoc local debug через прямой [`npx medusa develop`](../medusa-agency-boilerplate/package.json) плюс отдельный storefront runtime.

Отдельно зафиксировано на уровне плана:

- `Payload CMS v1` уже встроен как отдельный headless content service через app [`payload-cms`](../payload-cms);
- текущий roadmap track после закрытого `Payload CMS v1` — уже стартовавшая **Фаза 6** с client customization layer;
- approved communication stack больше не находится в статусе открытого выбора:
  - `UniSender` — целевой email provider для service и marketing email;
  - `VK Community Messaging` — целевой VK transport для service и marketing messages;
  - `VK ID` — optional auth/identity link для привязки пользователя к VK-каналу, а не самостоятельный transport;
  - `MTS Exolve` — целевой SMS provider для service и marketing SMS;
  - внешний marketing hub вроде `Sendsay` не является выбранным направлением для этого master repo.
- marketing architecture больше не planning-only и читается так:
  - `customer.metadata.marketing` = consent/preferences source of truth;
  - `marketing_campaign` + `marketing_delivery_journal` = campaign and audit surface;
  - `Payload` отвечает за marketing content, promo blocks, campaign copy и landing content;
  - `admin` отвечает за operational visibility, ручные запуски, статусы кампаний и пользовательские channel preferences.

---

## 3. Где ведется работа прямо сейчас

На текущем этапе основная рабочая поверхность остается здесь:

- [Docs/master_repo_plan_v2.md](./master_repo_plan_v2.md)
- [Docs/current_work.md](./current_work.md)
- [Docs/plan_analysis.md](./plan_analysis.md)
- [Docs/env_contract.md](./env_contract.md)
- [Docs/template_readiness_regression.md](./template_readiness_regression.md)
- [.codex/skills/medusa-master-repo/SKILL.md](../.codex/skills/medusa-master-repo/SKILL.md)
- [package.json](../package.json)
- [scripts/bootstrap.sh](../scripts/bootstrap.sh)
- [scripts/notification-smoke.sh](../scripts/notification-smoke.sh)
- [medusa-agency-boilerplate/src/scripts/create-secret-admin-api-key.ts](../medusa-agency-boilerplate/src/scripts/create-secret-admin-api-key.ts)
- [medusa-agency-boilerplate/src/api/admin/notifications/smoke/route.ts](../medusa-agency-boilerplate/src/api/admin/notifications/smoke/route.ts)
- [medusa-agency-boilerplate/src/api/store/payment/yookassa/route.ts](../medusa-agency-boilerplate/src/api/store/payment/yookassa/route.ts)
- [medusa-agency-boilerplate/src/api/store/payment/yookassa/return/route.ts](../medusa-agency-boilerplate/src/api/store/payment/yookassa/return/route.ts)
- [medusa-agency-boilerplate/src/api/yookassa/webhook/route.ts](../medusa-agency-boilerplate/src/api/yookassa/webhook/route.ts)
- [medusa-agency-boilerplate/src/api/store/delivery/rates/route.ts](../medusa-agency-boilerplate/src/api/store/delivery/rates/route.ts)

Если в рамках следующего шага будут вноситься изменения, то в первую очередь это ожидается в таких местах:

- документы по operational status, sequencing и source of truth;
- один канонический regression-pack с воспроизводимыми командами, expected results и failure signals;
- минимальные helper commands для локальной проверки уже подтвержденных путей;
- env-contract для opt-in notification, payment и shipping переменных без превращения их в baseline requirement;
- кодовые изменения по integration runtime только если они действительно нужны для regression-support, а не для нового product scope.

Payload-related рабочая поверхность уже **materialized и закрыта как completed track**, а её source of truth теперь удерживает:

- отдельное приложение [`payload-cms`](../payload-cms);
- storefront block renderer, content-provider boundary, globals integration и fallback behaviour;
- content schema docs, preview and revalidate flow и root orchestration scripts для payload runtime.

Для закрытого notification-track канонический source of truth остается в:

- документах по operational status, sequencing и env-contract;
- contract `order.placed` path `subscriber → workflow → Notification Module`, включая anti-duplicate semantics hardening v1.1;
- existing notification storage как dedupe authority для `order.placed`, а не отдельный ledger;
- admin smoke route, workflow и helper для authenticated smoke как отдельном baseline/regression anchor.

Для подтверждённого legacy provider track рабочая поверхность включает:

- документы по shipping status и ограничениям;
- server-side shipping adapter и contracts вокруг **historical provider-aware** сценария;
- storefront checkout shipping selection и cart and runtime validation.

Текущий roadmap step после уже закрытых shipping/payment/checkout proof points, завершённых placed + shipped + failed-payment + canceled slices, закрытых `post-UniSender cleanup-step`, `VK Community Messaging v1 foundation`, `storefront core baseline v1`, `VK ID v1`, `MTS Exolve`, `marketing layer v1`, `Payload CMS v1` и теперь уже закрытой `Фазы 6 storefront customization` — это readiness-переход к **Фазе 7 client/template packaging**: sequencing больше не возвращается к storefront-core, VK identity-linking, SMS transport, marketing orchestration, Payload content layer или Phase 6 browse customization и должен читать следующий workstream только как post-Phase-6 roadmap stage.

---

## 4. Конкретные задачи текущего этапа

### 4.1. Зафиксировать checkout end-to-end validation v1 как закрытый входной инвариант

Теперь source of truth должен явно удерживать, что checkout path уже подтвержден runtime/E2E:

- `shipping → hosted YooKassa payment → automatic return → review → order placement → confirmed order page` реально пройден;
- blocker вокруг `payment_collection` снят и больше не считается реальным ограничением checkout path;
- targeted fix в [`payment-button/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/checkout/components/payment-button/index.tsx) предотвращает преждевременный `placeOrder()` до hosted authorization;
- targeted fix в [`cookies.ts`](../medusa-agency-boilerplate-storefront/src/lib/data/cookies.ts) меняет cart cookie policy с `sameSite: "strict"` на `sameSite: "lax"`, чтобы cross-site return не терял checkout state.

### 4.2. Закрытый предыдущий operational workstream — `storefront core baseline v1`

Этот шаг уже завершён как предыдущий repo-level track: `UniSender email migration v1`, `post-UniSender cleanup-step` и `VK Community Messaging v1 foundation` были его входным sequencing, затем storefront core был закрыт отдельным delivery-result и больше не считается текущим operational focus. Его подтверждённый scope сохраняется здесь как closed snapshot, чтобы не переоткрывать drift.

### 4.2.1. Exact implementation scope для `storefront core baseline v1`

**В baseline входят:**

- shared commerce shell в [`medusa-agency-boilerplate-storefront/src/app/[countryCode]/(main)/page.tsx`](../medusa-agency-boilerplate-storefront/src/app/[countryCode]/(main)/page.tsx), [`medusa-agency-boilerplate-storefront/src/modules/layout/templates/nav/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/layout/templates/nav/index.tsx), [`medusa-agency-boilerplate-storefront/src/modules/layout/templates/footer/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/layout/templates/footer/index.tsx), catalog/category/collection/product/cart/checkout/account/order entrypoints и их shared templates;
- runtime/config слой в [`medusa-agency-boilerplate-storefront/src/lib/storefront-config.ts`](../medusa-agency-boilerplate-storefront/src/lib/storefront-config.ts), [`medusa-agency-boilerplate-storefront/.env.local.example`](../medusa-agency-boilerplate-storefront/.env.local.example), [`medusa-agency-boilerplate-storefront/src/lib/config.ts`](../medusa-agency-boilerplate-storefront/src/lib/config.ts), [`medusa-agency-boilerplate-storefront/src/middleware.ts`](../medusa-agency-boilerplate-storefront/src/middleware.ts), [`medusa-agency-boilerplate-storefront/src/lib/data/regions.ts`](../medusa-agency-boilerplate-storefront/src/lib/data/regions.ts) и [`medusa-agency-boilerplate-storefront/src/lib/data/locales.ts`](../medusa-agency-boilerplate-storefront/src/lib/data/locales.ts);
- shared customer-facing copy для home, navigation, footer, cart, checkout, account и order surfaces;
- provider-aware storefront adaptation только для уже подтвержденных backend inputs: YooKassa-first payment path, manual fallback, optional Stripe-compatible reference path при наличии такого provider в backend и legacy provider `provider_aware_v1` grouped delivery UI;
- удаление или нейтрализация starter/demo/admin-onboarding surfaces из shopper-facing entrypoints и storefront docs.

### 4.2.2. Какие проблемы и drifts нужно закрыть в первую очередь

- starter branding ещё протекает в shopper-facing shell через [`medusa-agency-boilerplate-storefront/src/modules/layout/components/medusa-cta/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/layout/components/medusa-cta/index.tsx), [`medusa-agency-boilerplate-storefront/src/modules/layout/templates/footer/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/layout/templates/footer/index.tsx) и [`medusa-agency-boilerplate-storefront/src/app/[countryCode]/(checkout)/layout.tsx`](../medusa-agency-boilerplate-storefront/src/app/[countryCode]/(checkout)/layout.tsx);
- demo/onboarding UX всё ещё доступен покупателю через [`medusa-agency-boilerplate-storefront/src/modules/products/components/product-onboarding-cta/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/products/components/product-onboarding-cta/index.tsx), [`medusa-agency-boilerplate-storefront/src/modules/order/components/onboarding-cta/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/order/components/onboarding-cta/index.tsx), [`medusa-agency-boilerplate-storefront/src/modules/order/templates/order-completed-template.tsx`](../medusa-agency-boilerplate-storefront/src/modules/order/templates/order-completed-template.tsx) и helper [`medusa-agency-boilerplate-storefront/src/lib/data/onboarding.ts`](../medusa-agency-boilerplate-storefront/src/lib/data/onboarding.ts);
- storefront README в [`medusa-agency-boilerplate-storefront/README.md`](../medusa-agency-boilerplate-storefront/README.md) всё ещё описывает Medusa starter и Stripe-first setup, а не template backend contract текущего repo;
- region/default semantics частично выровнены, но не унифицированы до конца: [`middleware.ts`](../medusa-agency-boilerplate-storefront/src/middleware.ts) уже опирается на env-driven default region, тогда как [`getRegion()`](../medusa-agency-boilerplate-storefront/src/lib/data/regions.ts) всё ещё содержит жесткий fallback на `ru`;
- optional Stripe-compatible code path может оставаться reference adapter, но не должен формировать ощущение, что Stripe env — обязательный baseline для шаблона.

### 4.2.3. Какие backend/storefront boundaries уже достаточны

- текущие store APIs и runtime contracts уже достаточны для baseline-step: `/store/regions`, optional `/store/locales`, cart/checkout/account/order surfaces, confirmed YooKassa hosted return contract и legacy rates route;
- storefront на этом шаге должен адаптироваться к уже существующим provider IDs, checkout semantics и confirmed region data, а не требовать новые backend endpoints или новый orchestration layer;
- notification stack, `VK Community Messaging`, `VK ID`, `MTS Exolve`, marketing-layer и Payload content layer не являются prerequisites для storefront core baseline.

### 4.2.4. Env и runtime assumptions, которые нужно зафиксировать

- baseline runtime остаётся привязан к текущему root orchestration contract: backend на `9000`, storefront на `8000`, template region = `ru` по умолчанию;
- storefront baseline env ограничивается `MEDUSA_BACKEND_URL`, `NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY`, `NEXT_PUBLIC_BASE_URL` и `NEXT_PUBLIC_DEFAULT_REGION`, а `NEXT_PUBLIC_STOREFRONT_PRESET` теперь допускается как optional `Фаза 6` switch для sanctioned client scenarios;
- `NEXT_PUBLIC_YOOKASSA_ENABLED` остаётся opt-in public flag для уже подтвержденного payment slice;
- `NEXT_PUBLIC_STRIPE_KEY`, `NEXT_PUBLIC_MEDUSA_PAYMENTS_PUBLISHABLE_KEY` и `NEXT_PUBLIC_MEDUSA_PAYMENTS_ACCOUNT_ID` допускаются только как optional compatibility env для reference adapter path, а не как baseline requirement;
- отсутствие `/store/locales` должно оставаться baseline-safe и не ломать build/runtime;
- storefront не получает новые public secrets для notification/VK/Payload tracks и не делает эти tracks частью core baseline.

### 4.2.5. Required storefront files/areas для implementation шага

- config/runtime: [`medusa-agency-boilerplate-storefront/.env.local.example`](../medusa-agency-boilerplate-storefront/.env.local.example), [`medusa-agency-boilerplate-storefront/src/lib/storefront-config.ts`](../medusa-agency-boilerplate-storefront/src/lib/storefront-config.ts), [`medusa-agency-boilerplate-storefront/src/lib/config.ts`](../medusa-agency-boilerplate-storefront/src/lib/config.ts), [`medusa-agency-boilerplate-storefront/src/middleware.ts`](../medusa-agency-boilerplate-storefront/src/middleware.ts), [`medusa-agency-boilerplate-storefront/src/lib/data/regions.ts`](../medusa-agency-boilerplate-storefront/src/lib/data/regions.ts), [`medusa-agency-boilerplate-storefront/src/lib/data/locales.ts`](../medusa-agency-boilerplate-storefront/src/lib/data/locales.ts);
- shared shell and copy: [`medusa-agency-boilerplate-storefront/src/app/[countryCode]/(main)/page.tsx`](../medusa-agency-boilerplate-storefront/src/app/[countryCode]/(main)/page.tsx), [`medusa-agency-boilerplate-storefront/src/modules/home/components/hero/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/home/components/hero/index.tsx), [`medusa-agency-boilerplate-storefront/src/modules/layout/templates/footer/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/layout/templates/footer/index.tsx), [`medusa-agency-boilerplate-storefront/src/modules/layout/templates/nav/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/layout/templates/nav/index.tsx), [`medusa-agency-boilerplate-storefront/src/modules/layout/components/country-select/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/layout/components/country-select/index.tsx);
- checkout/provider adaptation: [`medusa-agency-boilerplate-storefront/src/modules/checkout/components/payment/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/checkout/components/payment/index.tsx), [`medusa-agency-boilerplate-storefront/src/modules/checkout/components/payment-wrapper/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/checkout/components/payment-wrapper/index.tsx), [`medusa-agency-boilerplate-storefront/src/modules/checkout/components/payment-container/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/checkout/components/payment-container/index.tsx), [`medusa-agency-boilerplate-storefront/src/modules/checkout/components/payment-button/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/checkout/components/payment-button/index.tsx), [`medusa-agency-boilerplate-storefront/src/modules/checkout/components/shipping/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/checkout/components/shipping/index.tsx);
- demo/onboarding cleanup: [`medusa-agency-boilerplate-storefront/src/modules/products/components/product-onboarding-cta/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/products/components/product-onboarding-cta/index.tsx), [`medusa-agency-boilerplate-storefront/src/modules/order/components/onboarding-cta/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/order/components/onboarding-cta/index.tsx), [`medusa-agency-boilerplate-storefront/src/modules/order/templates/order-completed-template.tsx`](../medusa-agency-boilerplate-storefront/src/modules/order/templates/order-completed-template.tsx), [`medusa-agency-boilerplate-storefront/src/lib/data/onboarding.ts`](../medusa-agency-boilerplate-storefront/src/lib/data/onboarding.ts), [`medusa-agency-boilerplate-storefront/README.md`](../medusa-agency-boilerplate-storefront/README.md).

### 4.2.6. Expected deliverables, validation strategy и sequencing

**Expected deliverables:**

- storefront core без shopper-visible starter/demo branding;
- синхронизированный RU-neutral copy/config baseline;
- унифицированные region/runtime assumptions без скрытого возврата к demo defaults;
- provider-aware checkout and shipping presentation, совместимый с текущими backend inputs без новых API;
- storefront onboarding/readme notes, описывающие template backend contract, а не оригинальный starter.

**Validation strategy:**

- статическая проверка shared entrypoints на отсутствие shopper-visible `Medusa`, `demo`, `onboarding` и admin-setup CTA там, где это больше не часть baseline;
- regression review для home, catalog, product, cart, checkout, account и confirmed order surfaces;
- отдельная проверка того, что region switching и optional locales остаются baseline-safe;
- отдельная проверка того, что текущий YooKassa-first checkout path и legacy provider `provider_aware_v1` grouped selection semantics не ломаются адаптацией storefront shell;
- при implementation step использовать storefront build/lint/runtime smoke как validation surface, но не расширять baseline до новых integration tracks.

**Recommended sequencing:**

1. Сначала унифицировать runtime/config и region assumptions.
2. Затем убрать shopper-visible starter/onboarding surfaces и starter docs.
3. После этого дочистить shared copy и metadata до RU-neutral baseline.
4. Затем довести payment/shipping presentation до provider-aware baseline без новых backend APIs.
5. В конце зафиксировать validation notes и docs sync.

### 4.2.7. Explicit non-goals для этого workstream

- не запускать `VK ID`, `MTS Exolve`, Payload CMS, marketing pages или другой соседний roadmap track;
- не делать клиентский visual branding system, theme engine или глубокую витринную кастомизацию под конкретного заказчика;
- не вводить новые backend endpoints, новые payment providers, новый shipping scope или новый notification logic;
- не расширять scope до multi-quote delivery UX, `providerConnectId` / `extraParams`, campaign/service pages или полноценного translation-management layer.

### 4.3. Почему после `VK Community Messaging` сначала шёл `storefront core baseline v1`, а теперь следующим стал `MTS Exolve`

- communication expansion `VK Community Messaging` уже материализован и закрыт, поэтому повторно называть его текущим planning-selected шагом больше нельзя;
- storefront core действительно был ближайшим repo track и уже закрыт как предыдущий storefront-focused шаг, согласованный с [`Фаза 5. Общий storefront core и RU baseline`](./master_repo_plan_v2.md#фаза-5-общий-storefront-core-и-ru-baseline);
- после закрытия storefront core и `VK ID v1` следующим явным opt-in communication track остаётся `MTS Exolve`;
- sequencing по-прежнему безопаснее читать именно так, потому что marketing-layer и `Payload CMS v1` идут только после materialized channel stack и устойчивого storefront core.

### 4.4. Закрытый delivery snapshot — `VK Community Messaging`

**Что означает этот workstream в текущем repo state**

- это **новый notification channel/provider** для Notification Module, а не замена существующего email runtime;
- email path через [`getNotificationEmailRuntime()`](../medusa-agency-boilerplate/src/modules/notification-email.ts:119), [`getNotificationEmailProviderDefinition()`](../medusa-agency-boilerplate/src/modules/notification-email.ts:143), [`sendNotificationSmokeWorkflow`](../medusa-agency-boilerplate/src/workflows/send-notification-smoke.ts) и [`POST()`](../medusa-agency-boilerplate/src/api/admin/notifications/smoke/route.ts:26) остаётся source of truth для email и не переписывается в рамках VK rollout;
- первый VK scope должен использовать **уже существующие lifecycle events/workflows как trigger source pattern**, а не открывать новый event surface без подтвержденной необходимости.

**Какие notification slices входят в `v1`**

- обязательный foundation slice: отдельный authenticated smoke path для канала `vk`;
- service notification slices первого этапа:
  - `order.placed`;
  - `shipment.created`;
  - `order.canceled`;
- `payment_session.failed.customer.notification_requested` в `v1` **не входит**: pre-order `cart`-контекст и guest-checkout recipient binding пока хуже подходит для первого VK rollout, чем уже закрытые post-order order-level triggers;
- marketing campaigns, broadcasts и audience orchestration в `v1` не входят, даже если transport позже будет переиспользован marketing-layer.

**Trigger source и event surface**

- source of truth для trigger boundary остаётся тем же, что и у email lifecycle paths: существующие Medusa events `order.placed`, `shipment.created`, `order.canceled`;
- для `v1` не нужен новый upstream event surface, если достаточно подписаться на те же события отдельными VK subscribers или вызвать отдельные VK workflows из параллельного subscriber layer;
- новый internal event surface допускается только позже, если появится подтвержденный runtime-pressure для multi-channel fan-out, но это не часть `v1` blueprint.

**Required backend files/modules**

- новый provider module: `medusa-agency-boilerplate/src/modules/notification-vk-community.ts`;
- новый VK runtime/helper module для env parsing и recipient normalization: `medusa-agency-boilerplate/src/modules/notification-vk.ts`;
- расширение provider registration в [`medusa-config.ts`](../medusa-agency-boilerplate/medusa-config.ts) через добавление opt-in VK provider в Notification Module рядом с текущим email provider;
- отдельный smoke workflow: `medusa-agency-boilerplate/src/workflows/send-vk-notification-smoke.ts`;
- отдельный admin smoke route: `medusa-agency-boilerplate/src/api/admin/notifications/smoke/vk/route.ts`;
- lifecycle workflows `v1`:
  - `medusa-agency-boilerplate/src/workflows/send-order-placed-vk-notification.ts`;
  - `medusa-agency-boilerplate/src/workflows/send-order-shipped-vk-notification.ts`;
  - `medusa-agency-boilerplate/src/workflows/send-order-canceled-vk-notification.ts`;
- lifecycle subscribers `v1`:
  - `medusa-agency-boilerplate/src/subscribers/order-placed-vk-notification.ts`;
  - `medusa-agency-boilerplate/src/subscribers/order-shipped-vk-notification.ts`;
  - `medusa-agency-boilerplate/src/subscribers/order-canceled-vk-notification.ts`;
- targeted validation surface:
  - `medusa-agency-boilerplate/src/workflows/__tests__/notification-vk-runtime.unit.spec.ts`;
  - `medusa-agency-boilerplate/src/workflows/__tests__/send-order-placed-vk-notification.unit.spec.ts`;
  - `medusa-agency-boilerplate/src/workflows/__tests__/send-order-shipped-vk-notification.unit.spec.ts`;
  - `medusa-agency-boilerplate/src/workflows/__tests__/send-order-canceled-vk-notification.unit.spec.ts`.

**Env contract и provider credentials surface**

- `NOTIFICATION_VK_PROVIDER` — opt-in selector для VK transport runtime, где baseline-default = `disabled`, а `community` означает явный запрос на VK Community Messaging provider;
- `VK_COMMUNITY_ACCESS_TOKEN` — required только при `NOTIFICATION_VK_PROVIDER=community`;
- `VK_COMMUNITY_GROUP_ID` — required только при `NOTIFICATION_VK_PROVIDER=community`;
- `VK_API_VERSION` — optional override для VK API version, если implementation решит не хардкодить её в provider module;
- storefront и public env не должны получать никакие VK transport secrets;
- отсутствие всех `VK_*` переменных должно оставаться baseline-safe и не ломать clean onboarding, bootstrap, preflight, build или local email runtime.

**Recipient resolution, dedupe и skip semantics**

- canonical recipient для VK `v1` — не email, а нормализованный `vk_peer_id` как string;
- первый storage surface для привязки recipient должен быть максимально узким и opt-in: `customer.metadata.vk_peer_id` как canonical mapping source для service notifications `v1`;
- следствие для `v1`: VK lifecycle slices допускают controlled skip, если у order нет customer linkage или у customer отсутствует `metadata.vk_peer_id`;
- dedupe authority не меняется: source of truth по duplicate suppression остаётся existing notification storage;
- canonical dedupe identity для VK повторяет уже подтвержденную модель, но с `channel=vk` и VK recipient: `trigger_type + resource_type + resource_id + channel + template + normalized recipient`;
- duplicate match должен оставаться controlled skip с diagnostics, а не отдельным resend-flow;
- accepted limitation сохраняется: query-before-create race window остаётся допустимым ограничением и для VK `v1`.

**Ожидаемая интеграция с Notification Module**

- VK provider должен регистрироваться как второй opt-in provider в том же Notification Module, а не как отдельный ad-hoc transport bypass;
- smoke и lifecycle VK workflows должны, как и email, создавать notification entity через Notification Module, чтобы не потерять единый storage, statuses и dedupe surface;
- email и VK должны жить как параллельные каналы: одинаковый trigger source может приводить к email notification, VK notification или обоим, но без смешения provider-specific runtime helpers;
- `VK Community Messaging` `v1` не должен требовать переписывания существующих email workflows, runtime helpers или smoke route.

**Smoke и validation strategy**

- существующий email smoke path через [`sendNotificationSmokeWorkflow`](../medusa-agency-boilerplate/src/workflows/send-notification-smoke.ts:60) и [`POST()`](../medusa-agency-boilerplate/src/api/admin/notifications/smoke/route.ts:26) остаётся неизменным regression anchor;
- для VK добавляется sibling smoke path с отдельным route/workflow, который проверяет auth, provider resolution и успешное создание `channel=vk` notification без касания email route contract;
- unit validation для VK runtime должна подтвердить provider resolution semantics `disabled | community`, required credential guardrails и baseline-safe no-secret mode;
- targeted workflow validation для VK lifecycle slices должна зеркалить email harness pattern: single send, missing recipient skip, duplicate suppression и отсутствие ложного dedupe между двумя разными resources;
- live smoke на реального VK recipient допустим только как opt-in operator validation и не становится baseline regression requirement.

**Sequencing относительно `VK ID`, `MTS Exolve` и marketing-layer**

- `VK Community Messaging` идёт раньше `VK ID`: transport можно внедрить поверх manual или operator-managed `customer.metadata.vk_peer_id`, а `VK ID` позже автоматизирует и стандартизирует linking, но не владеет delivery;
- `MTS Exolve` не смешивается с VK rollout и не использует его env/runtime surface;
- marketing-layer позже должен переиспользовать transport как delivery adapter, но не должен появляться внутри `v1` transport implementation;
- `Payload` и `admin` остаются выше по стеку: content и operations, а не transport credential owners.

**Explicit non-goals**

- не переписывать existing email provider/runtime surface и не объединять его с VK в один большой cross-channel helper в том же шаге;
- не менять existing lifecycle email workflows, their templates, recipient rules и dedupe boundaries;
- не запускать `VK ID`, storefront linking UX, consent center, campaign builder, segmentation, suppression journal или marketing orchestration;
- не покрывать `payment failed` в первом VK rollout;
- не делать public/storefront env contract для VK secrets;
- не превращать live VK smoke в обязательный regression-pack baseline;
- не вводить новый dedicated delivery ledger, queue framework или storage-level lock только ради VK `v1`.

**Фактический implementation result этого шага**

1. Opt-in VK provider/runtime surface и sibling authenticated VK smoke path материализованы без изменения email smoke contract.
2. Lifecycle slice `order.placed` реализован для `channel=vk` поверх existing trigger source и existing notification storage dedupe model.
3. Lifecycle slices `shipment.created` и `order.canceled` добавлены тем же pattern'ом.
4. Step закрыт targeted validation без перехода к `VK ID`, `MTS Exolve` или marketing-layer.

---

## 5. Что сейчас не делаем

До закрытия remaining tracks не начинаем:

- объявлять всю Фазу 3 или Фазу 4 завершенной только потому, что подтверждены notification slice, notification hardening, payment path, shipping slice, regression-pack и checkout E2E;
- подменять подтвержденный checkout end-to-end pass заявлением, будто уже закрыт весь post-order и order-lifecycle контур;
- возвращать в статус open уже закрытый storefront `500` blocker, если речь идет о ранее подтвержденной проблеме отсутствующего или невалидного cart;
- раскрывать реальный пользовательский token или secret в документах и skill;
- переоткрывать notification hardening как активный workstream без нового подтвержденного evidence о регрессии;
- переоткрывать payment selection в сторону нецелевых для РФ провайдеров только из-за их official and first-party статуса;
- начинать shipping track с произвольного провайдера, минуя historical provider-aware направление, если пользователь явно не сменил рынок проекта;
- реализацию Payload CMS как отдельного content layer раньше storefront core и до завершения integration tracks;
- глубокую русификацию и брендирование storefront;
- систему клиентских тем и секций;
- template release и автоматизацию создания нового клиента;
- staging and prod packaging как будто remaining integration path уже утвержден.

Простыми словами:
checkout path уже закрыт, а текущий шаг — это не новый выбор payment/shipping направления, а первый post-order operational workstream поверх уже подтверждённого order placement.

---

## 6. Подтвержденные ограничения и notes текущего этапа

- clean-state сценарий `cp .env.example .env` → `npm run bootstrap` → `npm run preflight` → `npm run dev` подтвержден и не считается открытым блокером;
- bootstrap baseline теперь подтвержден как RU and template-ready skeleton, а не demo-oriented baseline;
- redirect-loop в storefront middleware закрыт и не считается текущим открытым дефектом;
- notification slice v1, notification hardening v1 и order lifecycle notifications hardening v1.1 подтверждены как закрытый notification result Фазы 3;
- authenticated local smoke path теперь считается каноническим только в схеме `fresh secret admin API key` → `Basic auth` → `POST /admin/notifications/smoke`;
- для этого канонического пути теперь допустим минимальный helper `npm run smoke:notification`, который создает fresh key и вызывает smoke route без ручной сборки Basic auth;
- payment track v1 подтвержден для текущего YooKassa-first scope и не считается активным blocker;
- первый shipping slice теперь читается только как historical note: прежний provider-aware opt-in/rate-selection baseline был удалён из current supported provider activation surface, а текущий checkout commit surface остаётся generic/provider-neutral;
- env-contract для payment path расширен opt-in переменными `YOOKASSA_SHOP_ID`, `YOOKASSA_SECRET_KEY`, `YOOKASSA_RETURN_URL`, `YOOKASSA_STOREFRONT_RETURN_ORIGINS`, `YOOKASSA_WEBHOOK_URL`, `YOOKASSA_WEBHOOK_SECRET`, `YOOKASSA_ALLOW_UNSIGNED_WEBHOOKS` и storefront flag `NEXT_PUBLIC_YOOKASSA_ENABLED`;
- canonical root orchestration теперь явно синхронизирует YooKassa guardrails `YOOKASSA_STOREFRONT_RETURN_ORIGINS`, `YOOKASSA_WEBHOOK_URL` и `YOOKASSA_ALLOW_UNSIGNED_WEBHOOKS` из root `.env` в backend `.env`, поэтому путь `cp .env.example .env` → `npm run bootstrap` действительно переносит новый hardening contract в runtime;
- env-contract для shipping path теперь явно отражает removed-provider cleanup semantics: Delivery Hub/direct Yandex selected/default, removed-provider env activation surface отсутствует, а historical DB residue очищается только отдельной external/operator-approved процедурой;
- verification pass подтвердил, что baseline and no-secret startup path не сломан, notification runtime сохраняет fallback semantics, removed delivery provider не имеет env activation surface, а env-contract остался safe for clean onboarding;
- cheapest-only semantics теперь честно зафиксирована в коде и docs как текущий v1 scope, а не как полноценный multi-quote checkout;
- storefront `500` на checkout снят как ложный blocker для текущего shipping slice: проблема была в отсутствии или невалидности cart, а при валидном cart route отвечает `200`;
- для template readiness теперь должен существовать один канонический regression-pack/source-of-truth с командами, expected results и failure signals в [Docs/template_readiness_regression.md](./template_readiness_regression.md);
- remaining risks текущего этапа ограничены и явно известны:
  - email runtime на `UniSender`, post-migration cleanup, `VK Community Messaging v1 foundation`, `storefront core baseline v1`, `VK ID v1`, `MTS Exolve` и `marketing layer v1` уже не являются активными blockers;
  - `customer.metadata.vk_peer_id` и structured `customer.metadata.vk_link` остаются совместимым opt-in VK binding surface, а source-of-truth для marketing consent/preferences теперь материализован в `customer.metadata.marketing`;
  - campaign storage и audit surface уже materialized как внутренние таблицы `marketing_campaign` и `marketing_delivery_journal`, поэтому marketing layer больше не считается design-only целью;
  - `providerConnectId` / `extraParams` support и true multi-quote checkout остаются deferred;
  - race window в query-before-create dedupe для placed, shipped, failed-payment, canceled, VK и SMS paths осознанно принят как accepted limitation и пока не закрыт отдельным storage-level lock;
  - legacy provider `provider_aware_v1` подтверждён runtime-проверкой `2026-04-20`, но live multi-provider switching на одном тестовом адресе пока не подтверждено;
  - review notes по marketing layer остаются только non-blocker: query ergonomics для admin preferences update и manual URL parsing в dynamic admin campaign route.
- повторный `npm run bootstrap` поверх уже заполненной БД подтвержден runtime validation как idempotent path и больше не является открытым hardening concern;
- checkout end-to-end validation v1 закрыт: полный flow `shipping → hosted YooKassa payment → automatic return → review → order placement → confirmed order page` подтверждён runtime/E2E, а blockers сняты targeted fixes в [`payment-button/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/checkout/components/payment-button/index.tsx) и [`cookies.ts`](../medusa-agency-boilerplate-storefront/src/lib/data/cookies.ts);
- order lifecycle notifications v1 уже реализован как первый production-like customer-facing slice, а hardening v1.1 синхронизирован как действующий operational contract: subscriber [`orderPlacedNotificationHandler()`](../medusa-agency-boilerplate/src/subscribers/order-placed-notification.ts:5) слушает `order.placed`, workflow [`sendOrderPlacedNotificationWorkflow`](../medusa-agency-boilerplate/src/workflows/send-order-placed-notification.ts:308) делает query по минимальной форме `{ id, display_id, email }`, dedupe authority остается existing notification storage, canonical dedupe identity использует `trigger_type + resource_type + resource_id + channel + template + normalized recipient`, а duplicate suppression выполняется как controlled skip с diagnostics без второго notification;
- `order shipped notification v1` уже реализован и его targeted validation закрыта через [send-order-shipped-notification.unit.spec.ts](../medusa-agency-boilerplate/src/workflows/__tests__/send-order-shipped-notification.unit.spec.ts): event = `shipment.created`, payload baseline = `{ id, no_notification }`, query baseline = `{ fulfillment.id, order.id, order.display_id, order.email }`, template = `order-shipped-v1`, trigger type = `shipment.created.customer.notification_requested`, canonical recipient = `order.email`, canonical dedupe resource identity = `fulfillment.id`;
- `payment failed notification v1` уже реализован и его targeted validation закрыта через [`send-payment-failed-notification.unit.spec.ts`](../medusa-agency-boilerplate/src/workflows/__tests__/send-payment-failed-notification.unit.spec.ts): event = internal `payment_session.failed.customer.notification_requested`, canonical recipient = `cart.email`, canonical dedupe resource identity = `payment_session.id`, а duplicate suppression не смешивает два разных failed attempts одного cart;
- `order canceled notification v1` уже реализован и его targeted validation закрыта через [send-order-canceled-notification.unit.spec.ts](../medusa-agency-boilerplate/src/workflows/__tests__/send-order-canceled-notification.unit.spec.ts): event = `order.canceled`, canonical recipient = `order.email`, canonical dedupe resource identity = `order.id`, а duplicate suppression не смешивает два разных canceled orders одного recipient;
- `MTS Exolve` уже реализован и закрыт как SMS channel: runtime/provider resolution материализованы в [`notification-sms.ts`](../medusa-agency-boilerplate/src/modules/notification-sms.ts), provider implementation — в [`notification-exolve.ts`](../medusa-agency-boilerplate/src/modules/notification-exolve.ts), smoke/admin route — в [`route.ts`](../medusa-agency-boilerplate/src/api/admin/notifications/smoke/sms/route.ts), middleware boundary — в [`defineMiddlewares()`](../medusa-agency-boilerplate/src/api/middlewares.ts:17), а targeted validation закрыта с результатом `3/3` suites PASS, `12/12` tests PASS и backend typecheck PASS;
- `marketing layer v1` уже реализован и закрыт коммитом `a4711906b16523dcf03da9601ccf1a914702ca7d` `feat(marketing-layer): add marketing preferences and campaign workflows`: metadata-first consent surface живет в [`marketing-preferences.ts`](../medusa-agency-boilerplate/src/modules/marketing-preferences.ts), campaign and journal surface — в [`marketing-layer.ts`](../medusa-agency-boilerplate/src/modules/marketing-layer.ts), launch workflow — в [`send-marketing-campaign.ts`](../medusa-agency-boilerplate/src/workflows/send-marketing-campaign.ts), storefront profile section — в [`profile-marketing-preferences/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/account/components/profile-marketing-preferences/index.tsx), а validation закрыта как backend typecheck PASS + storefront typecheck PASS + `1` suite / `6` tests PASS с review verdict `approve`;
- `Payload CMS v1` уже реализован и закрыт коммитом [`22486388f4c89d884b4c3cbe668ebec4ab695dee`](../package.json:1) `feat(content): add Payload CMS marketing content layer`: отдельный app [`payload-cms`](../payload-cms) materialized как content layer маркетинговых страниц, storefront получил content integration, preview/revalidate, globals и fallback behaviour, а root orchestration layer теперь включает payload scripts, env sync и blocker-fix по нормализации `NODE_ENV` в [`scripts/payload-run.sh`](../scripts/payload-run.sh:28);
- финальный validation state для этого workstream зафиксирован как PASS по [`payload:types`](../package.json:25), [`payload:importmap`](../package.json:26) и [`payload:build`](../package.json:23), а final review verdict зафиксирован как approveable без blocking findings для commit;
- residual non-blocking observations после review больше не считаются открытым security aftermath: preview access для draft globals и signed preview-exit доведены до закрытого состояния post-review hardening.
- текущий roadmap step больше не находится внутри **Фазы 6**: `storefront customization` truthfully re-closed post-remediation checkpoint `2026-04-19`, а следующий roadmap transition теперь читается как readiness к **Фазе 7. Шаблонизация и ускорение запуска нового клиента**;
- все sanctioned базовые `Phase 6` workstreams закрыты и остаются historical source-of-truth markers: [`Preset-driven landing-surface contract v1`](../plans/preset-driven-landing-surface-contract-v1.md), [`adjacent-preset-rollout-product-support-highlights.md`](../plans/adjacent-preset-rollout-product-support-highlights.md), [`preset-driven-listing-surface-contract-v1.md`](../plans/preset-driven-listing-surface-contract-v1.md), [`preset-driven-global-shell-contract-v1.md`](../plans/preset-driven-global-shell-contract-v1.md) и [`preset-driven-catalog-shell-contract-v1.md`](../plans/preset-driven-catalog-shell-contract-v1.md);
- storefront customization удерживается на одном shared storefront core и на двух центральных selector/config authority:
  - sanctioned preset selector остаётся только [`NEXT_PUBLIC_STOREFRONT_PRESET`](../medusa-agency-boilerplate-storefront/src/lib/env.ts:21) с preset catalog `atelier|market`;
  - sanctioned preset config authority остаётся [`storefront-client-config.ts`](../medusa-agency-boilerplate-storefront/src/lib/storefront-client-config.ts);
  - typed [`landingSurfaces`](../medusa-agency-boilerplate-storefront/src/lib/storefront-client-config.ts:317) покрывают `home`, `collectionLanding`, `contentPage` и `postPage` через [`collection-landing-surface/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/storefront-customization/components/collection-landing-surface/index.tsx), [`content-page-surface/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/storefront-customization/components/content-page-surface/index.tsx), [`post-page-surface/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/storefront-customization/components/post-page-surface/index.tsx) и shared [`landing-surface-sections/index.tsx`](../medusa-agency-boilerplate-storefront/src/modules/storefront-customization/components/landing-surface-sections/index.tsx);
  - adjacent product display surface [`productSurfaces.supportHighlights`](../medusa-agency-boilerplate-storefront/src/lib/storefront-client-config.ts:323) materialized через [`resolveProductSupportHighlightsSurface()`](../medusa-agency-boilerplate-storefront/src/modules/storefront-customization/components/product-surface-resolver.ts:14) и [`ProductSupportHighlights`](../medusa-agency-boilerplate-storefront/src/modules/storefront-customization/components/product-support-highlights/index.tsx:16);
  - typed listing/card contract [`listingSurfaces.productCard`](../medusa-agency-boilerplate-storefront/src/lib/storefront-client-config.ts:324) materialized через [`resolveDefaultProductCardSurface()`](../medusa-agency-boilerplate-storefront/src/modules/storefront-customization/components/listing-surface-resolver.ts:14), [`resolveFeaturedProductCardSurface()`](../medusa-agency-boilerplate-storefront/src/modules/storefront-customization/components/listing-surface-resolver.ts:17) и [`ProductCardSurface`](../medusa-agency-boilerplate-storefront/src/modules/storefront-customization/components/product-card-surface.tsx:55);
  - typed global shell contract [`StorefrontShellConfig`](../medusa-agency-boilerplate-storefront/src/lib/storefront-client-config.ts:74) materialized через [`resolveNavShellSurface()`](../medusa-agency-boilerplate-storefront/src/modules/storefront-customization/components/shell-surface-resolver.ts:15), [`resolveSideMenuShellSurface()`](../medusa-agency-boilerplate-storefront/src/modules/storefront-customization/components/shell-surface-resolver.ts:18), [`resolveFooterShellSurface()`](../medusa-agency-boilerplate-storefront/src/modules/storefront-customization/components/shell-surface-resolver.ts:21) и thin seam в [`RootLayout`](../medusa-agency-boilerplate-storefront/src/app/layout.tsx:14);
  - typed catalog shell contract [`catalogShell`](../medusa-agency-boilerplate-storefront/src/lib/storefront-client-config.ts:325) materialized через [`resolveStoreCatalogIntroSurface()`](../medusa-agency-boilerplate-storefront/src/modules/storefront-customization/components/catalog-shell-resolver.ts:15), [`resolveStoreCatalogResultsSurface()`](../medusa-agency-boilerplate-storefront/src/modules/storefront-customization/components/catalog-shell-resolver.ts:18), [`resolveCollectionCatalogResultsSurface()`](../medusa-agency-boilerplate-storefront/src/modules/storefront-customization/components/catalog-shell-resolver.ts:21), [`resolveFeaturedRailCatalogShellSurface()`](../medusa-agency-boilerplate-storefront/src/modules/storefront-customization/components/catalog-shell-resolver.ts:24), [`StoreCatalogIntroSurface`](../medusa-agency-boilerplate-storefront/src/modules/storefront-customization/components/catalog-shell-surface.tsx:51), [`CatalogResultsShellSurface`](../medusa-agency-boilerplate-storefront/src/modules/storefront-customization/components/catalog-shell-surface.tsx:104) и [`FeaturedRailCatalogShellSurface`](../medusa-agency-boilerplate-storefront/src/modules/storefront-customization/components/catalog-shell-surface.tsx:130).
- truthful closure narrative для `Фазы 6` теперь зафиксирован явно:
  - прежний closure verdict был overstated после закрытия базовых preset-driven slices и позже пересмотрен;
  - reopening был признан валидным по трём gap'ам: category browse route вне sanctioned [`catalogShell`](../medusa-agency-boilerplate-storefront/src/lib/storefront-client-config.ts:325) contour, related products вне sanctioned listing surface contract и loading/skeleton state вне card/listing contract;
  - remediation slice по category browse contour закрыт коммитом `adb8df25ed64d9540e36588ee91dc5ff24951009` `fix(storefront): route category browse through catalogShell contour`;
  - remediation slice по related products rail закрыт коммитом `275dc4d823b8203bd1d49364ba4d02211bf42799` `fix(storefront): move related products to sanctioned listing surface contract`;
  - remediation slice по loading/skeleton sync закрыт коммитом `97a4837c483b054d25511f216ee487bf150306b4` `fix(storefront): align skeleton loading states with card surface contract`.
- post-remediation regression/readiness checkpoint зафиксирован как **PASS**:
  - category browse теперь routed через sanctioned `catalogShell` contour;
  - related products rail теперь routed через sanctioned listing surface contract;
  - loading/skeleton state синхронизирован с sanctioned card/listing contract;
  - cross-preset typecheck/build для `atelier` и `market` завершён успешно;
  - storefront `npm run lint` теперь проходит clean, а remaining warnings ограничены только controlled Store API warnings во время static params generation и не относятся к reopened gaps.
- финальный readiness verdict для `Фазы 6` теперь такой: sanctioned preset-driven storefront customization **truthfully закрыта и готова к следующему roadmap stage**;
- после truthful re-closure `Фазы 6` открытых пунктов внутри storefront customization больше нет, а дальнейшие открытые пункты лежат только в следующих roadmap стадиях: **Фаза 7** template/client packaging и **Фаза 8** release-grade checks, CI, staging и production readiness.
- `Phase 7 / tranche 1` `client-init contract and placeholder-safe template baseline` теперь truthfully закрыт closure-коммитом `a96aa81adfd655ddda9b6fea03dacf61c3174737` `feat(template): add client-init contract baseline`.
- канонический baseline tranche 1 materialized в [`Docs/client_init_contract.md`](./client_init_contract.md), а env/source-of-truth narrative синхронизирован с runtime: `MEDUSA_BACKEND_URL`, `NEXT_PUBLIC_BASE_URL` и `NEXT_PUBLIC_DEFAULT_REGION` зафиксированы как optional storefront inputs с safe fallback semantics; `Phase 6` guardrails по [`NEXT_PUBLIC_STOREFRONT_PRESET`](../medusa-agency-boilerplate-storefront/src/lib/env.ts:14) и [`storefront-client-config.ts`](../medusa-agency-boilerplate-storefront/src/lib/storefront-client-config.ts) остаются неизменными.
- blocking inconsistency между docs/manifest/runtime classification этих ключей была закрыта truthful remediation, после чего повторный review дал verdict **APPROVE**.
- `Phase 7 / tranche 2` теперь materialized как markdown-only packaging step через [`Docs/template_release_handoff.md`](./template_release_handoff.md): этот artifact является canonical source of truth для template release checklist, onboarding path и clean release-package contour.
- текущий packaging step truthfully ограничен docs/readiness/handoff narrative: он не переоткрывает `Фазу 6`, не делает overclaim про `Фазу 8` и не смешивается с unrelated working-tree changes вроде `payload-cms`, backend marketing work или несвязанных storefront refactor paths.
- `Phase 8 / tranche 1` baseline integrity contour теперь materialized как первый implementation slice release-grade gates:
  - root [`package.json`](../package.json) теперь держит sanctioned aggregation scripts [`lint`](../package.json:31), [`typecheck`](../package.json:32), [`smoke:browser`](../package.json:36), CI-compatible split [`integrity:baseline:static`](../package.json:37) / [`integrity:baseline:runtime-smoke`](../package.json:38) и единый baseline entrypoint [`integrity:baseline`](../package.json:39);
  - truthful lint contour пока intentionally минимален: canonical root lint gate агрегирует только storefront [`lint`](../medusa-agency-boilerplate-storefront/package.json:14), потому что backend lint tooling в текущем repo не materialized и не был выдуман сверх scope;
  - canonical typecheck path теперь materialized в backend [`typecheck`](../medusa-agency-boilerplate/package.json:22) и storefront [`typecheck`](../medusa-agency-boilerplate-storefront/package.json:15), а root aggregate запускает оба sanctioned path'а;
  - storefront local dev path для truthful browser smoke remediation стабилизирован на sanctioned non-Turbopack runtime: [`dev`](../medusa-agency-boilerplate-storefront/package.json:11) теперь использует `next dev`, а [`scripts/storefront-dev.sh`](../scripts/storefront-dev.sh:17) продолжает сбрасывать stale `.next` artifacts перед стартом при подозрении на corrupted runtime state;
  - minimal browser smoke baseline materialized в [`scripts/browser-smoke.sh`](../scripts/browser-smoke.sh:1): headless Chrome через DevTools Protocol теперь открывает storefront login surface `/ru/account` напрямую, ждёт complete load и подтверждает минимальный user-facing contour по pathname, non-empty title, login form content и наличию nav account link;
  - минимальный automation boundary вокруг этого gate теперь materialized в GitHub Actions workflow [`integrity-baseline.yml`](../.github/workflows/integrity-baseline.yml): workflow поднимает template-safe baseline env из [`.env.example`](../.env.example), устанавливает backend/storefront dependencies, подготавливает Chrome для browser smoke, выполняет sanctioned startup path `bootstrap → preflight → dev` и затем запускает тот же root entrypoint [`integrity:baseline`](../package.json:39), без staging/prod jobs и без новых checks вне утверждённого local contour;
  - этот tranche 1 slice intentionally не трогает staging/prod automation, deploy, rollback, backup/restore или infra hardening и остаётся узким baseline integrity contour поверх уже существующих build + HTTP smoke gates;
  - локальный static contour [`integrity:baseline:static`](../package.json:37) уже подтверждён PASS;
  - локальный runtime contour [`integrity:baseline:runtime-smoke`](../package.json:38) тоже подтверждён PASS `2026-04-20`: `smoke:backend`, `smoke:notification`, `smoke:storefront`, `smoke:browser`.
- `Phase 7 / tranche 3` теперь truthfully закрыт closure-check `2026-04-20`:
  - environmental blocker вокруг busy `8000` в [`check_local_port()`](../scripts/preflight.sh:45) был подтверждён именно как environment normalization concern, а не repo defect;
  - docs/runtime expectation defect вокруг [`smoke:backend`](../package.json:33) был сохранён как уже закрытая docs-remediation: probe теперь читается только как health-check для уже поднятого backend runtime;
  - обнаруженный repo-level blocker для authenticated notification smoke оказался не в route contract, а в helper harness [`scripts/notification-smoke.sh`](../scripts/notification-smoke.sh:1): local `medusa exec` наследовал root orchestration `DATABASE_URL` и `REDIS_URL` с docker-network hostnames вместо backend-local env;
  - targeted remediation закрыла этот blocker: helper теперь запускает local admin API key script с явным `env -u DATABASE_URL -u REDIS_URL` и bounded timeout, поэтому local exec больше не зависает на root orchestration database/cache settings;
  - после этой remediation canonical RC rerun `bootstrap → preflight → dev` плюс root runtime gate [`integrity:baseline:runtime-smoke`](../package.json:38) завершился PASS без новых blockers.
- новый человек теперь должен читать `Phase 7` так: tranche 1 = canonical init contract baseline в [`Docs/client_init_contract.md`](./client_init_contract.md), tranche 2 = canonical handoff/release packaging narrative в [`Docs/template_release_handoff.md`](./template_release_handoff.md), tranche 3 = closure-validated RC smoke/normalization step; вся `Фаза 7` теперь truthfully закрыта, а следующий открытый roadmap stage — только `Фаза 8`.
- Для последнего `delivery-hub` review verdict immediate continuation step нужно читать не как новый backend/storefront implementation tranche, а как process/review isolation step: сначала требуется clean review surface без unrelated legacy diff в the removed backend legacy provider module, и только после этого возможен следующий clean approval attempt. Это explicitly не является заявлением, что legacy diff уже устранён.

---

## 7. Порядок действий для агента с пустым контекстом

Если пользователь пишет `начинай` или `продолжай реализацию`, агент должен действовать так:

1. Прочитать этот файл: [current_work.md](./current_work.md).
2. Открыть дорожную карту: [master_repo_plan_v2.md](./master_repo_plan_v2.md).
3. Прочитать аудит состояния: [plan_analysis.md](./plan_analysis.md).
4. Принять как уже подтвержденные инварианты:
   - `cp .env.example .env` → `npm run bootstrap` → `npm run preflight` → `npm run dev`;
   - `ru` region и `rub` baseline currency;
   - publishable key, sales channel и минимальный shipping skeleton;
   - отсутствие обязательного demo-catalog baseline;
   - закрытый storefront redirect-loop;
   - закрытый notification hardening v1;
   - канонический authenticated smoke path в схеме `fresh sk_* key` → `Basic auth` → `POST /admin/notifications/smoke`;
   - подтвержденный YooKassa-first payment path v1 без обязательных secret and env требований для baseline startup;
   - historical provider-aware shipping slice считать только removed/historical context, не текущей supported provider activation surface; current checkout commit surface остаётся generic/provider-neutral;
   - storefront `500` для checkout уже отделен как cart and data-state issue, а не как доказанная shipping code regression.
5. Проверить рабочую поверхность текущего этапа:
   - [Docs/current_work.md](./current_work.md)
   - [Docs/plan_analysis.md](./plan_analysis.md)
   - [Docs/env_contract.md](./env_contract.md)
   - [.codex/skills/medusa-master-repo/SKILL.md](../.codex/skills/medusa-master-repo/SKILL.md)
   - [package.json](../package.json)
   - [medusa-agency-boilerplate/src/scripts/seed.ts](../medusa-agency-boilerplate/src/scripts/seed.ts)
6. По умолчанию работать так:
   - считать notification-track закрытым на уровне hardening v1 и order lifecycle notifications hardening v1.1;
   - считать order lifecycle notifications v1 уже реализованным как первый production-like customer-facing slice с anti-duplicate contract поверх existing notification storage, включая shipped, failed-payment и canceled expansions;
   - считать `UniSender email migration v1`, `post-UniSender cleanup-step`, `VK Community Messaging v1 foundation`, `storefront core baseline v1`, `VK ID v1`, `MTS Exolve`, `marketing layer v1` и `Payload CMS v1` закрытыми workstreams;
   - считать payment v1 подтвержденным YooKassa-first path;
   - считать прежний shipping v1 provider-aware rate-selection slice `provider_aware_v1` только historical/removed baseline evidence from `2026-04-20`, без current provider activation support;
   - считать **bootstrap idempotency hardening v1** подтвержденным runtime validation;
   - считать checkout end-to-end validation v1 закрытым подтвержденным runtime/E2E результатом;
   - считать payload content layer закрытым commit-уровнем, validation PASS по [`payload:types`](../package.json:25), [`payload:importmap`](../package.json:26), [`payload:build`](../package.json:23) и review-level residual observations без blocking findings;
   - использовать [template_readiness_regression.md](./template_readiness_regression.md) как source of truth по локальным regression-командам и failure signals;
   - считать storefront production build перед `Фазой 6` защищённым от обязательной live-зависимости на backend во время SSG static params collection;
   - считать **Фазу 6 storefront customization** truthfully закрытой только после уже зафиксированного reopen/remediation cycle: sanctioned preset-driven stack materialized, три reopened gap'а закрыты remediation-коммитами, post-remediation cross-preset checkpoint зафиксирован как PASS, поэтому повторно Phase 6 не переоткрывать без нового evidence и использовать authenticated smoke path только как отдельный regression anchor вне storefront customization scope.
7. Не начинать template release или phase `7/8` work, пока по плану не дойдем до соответствующих фаз после **Фазы 6**.
8. Для текущего `delivery-hub` review continuation не открывать широкий новый code tranche до targeted review текущего admin/backend shipment-operations cancel+retry-readiness tranche; следующий шаг должен оставаться узким и не расширять scope за пределы approved micro-slices. Historical readiness contour [`DeliveryHubExecutionLedgerRepositoryAssemblyPersistenceReadinessContour`](../medusa-agency-boilerplate/src/modules/delivery-hub/storage/execution-ledger-repository-assembly-scaffold.ts:161) `artifact_defined -> manual_application_external -> snapshot_verification_available -> activation_blocked` по-прежнему остаётся source-of-truth только для manual/offline rollout and release-handoff materials, но runtime [`createFulfillment()`](../medusa-agency-boilerplate/src/modules/deliveryhub.ts:119) больше не является globally hard-blocked on the ready direct-Yandex contour: narrow ledger-backed dispatch + persisted shipment path is now materialized behind the existing gate, blocked contours still remain zero-call, lifecycle read-model and polling v1 are materialized for accepted shipments, current admin/backend operations surface already includes guarded cancel + retry-readiness/blocked-only retry request path, while webhook/auto-retry/scheduler automation and broad dashboard remain deferred.
9. После значимых изменений обновить:
   - [current_work.md](./current_work.md)
   - [template_readiness_regression.md](./template_readiness_regression.md), если меняется сам regression-pack;
   - [master_repo_plan_v2.md](./master_repo_plan_v2.md), если меняется сам план;
   - [plan_analysis.md](./plan_analysis.md), если меняется аудит;
   - [.codex/skills/medusa-master-repo/SKILL.md](../.codex/skills/medusa-master-repo/SKILL.md), если меняется навигация или current known reality.

---

## 8. Где смотреть статус разных типов

- Что делаем прямо сейчас: [current_work.md](./current_work.md)
- Куда идем и в каком порядке: [master_repo_plan_v2.md](./master_repo_plan_v2.md)
- Что реально уже подтверждено и какие есть разрывы: [plan_analysis.md](./plan_analysis.md)
- Как агенту быстро войти в контекст: [.codex/skills/medusa-master-repo/SKILL.md](../.codex/skills/medusa-master-repo/SKILL.md)

---

## 9. Когда этот документ нужно обновлять

Этот файл нужно обновлять обязательно, если:

- изменилась активная фаза;
- начался новый конкретный рабочий трек;
- изменилась рабочая поверхность текущего этапа;
- был снят или добавлен блокер;
- поменялся ответ на вопрос `что делаем прямо сейчас`.

## 2026-04-27 follow-up: pickup windows retest, 5Post source, and Russian admin UX

Safe retest of the Delivery Hub Admin diagnostics confirmed the backend runtime is using the Yandex Other-day test host `https://b2b.taxi.tst.yandex.net/api/b2b/platform` and pickup-window path `/offers/info?last_mile_policy=self_pickup`. Admin lookup for Moscow pickup points returned `1079` total points and the first sampled `50` were named `5 Post (Пятерочка)` in the sanitized Yandex pickup-point `name` field. Therefore `5Post` is the Yandex-returned partner network / PVZ display name, not a provider switch in our UI, not warehouse/provider mapping, and not legacy delivery contamination.

Pickup windows were tested safely for sampled Moscow/near-Moscow PVZ ids, including short ids `0195749be6e...`, `0195749be91...`, `0195749be9b...`, `0195749bedc...`, `0195749bf06...`. One saved connection later returned provider `403`/access-block normalized to `DELIVERY_HUB_CREDENTIALS_INVALID`. The other saved connection reproduced the user's original error on `0195749be9b...`: provider status `400`, safe code/message `no_delivery_options / No delivery options for interval`. No raw token/auth/ciphertext/raw provider body was printed and no shipment create/cancel/status/retry call was executed. Because no valid window was found, Test quote was not run for this PVZ set: warehouse quotes are guarded to require a future interval returned by pickup windows.

Admin UX was updated to make this operator flow explicit in [`page.tsx`](../medusa-agency-boilerplate/src/admin/routes/settings/delivery/page.tsx) and [`page-state.ts`](../medusa-agency-boilerplate/src/admin/routes/settings/delivery/page-state.ts): Russian labels/help text for connection, write-only token, Test connection, Pickup point lookup, Pickup windows lookup, and Test quote; Yandex API host selector restored; local Test quote guard explains that quote needs a future interval from pickup windows; pickup windows query now requires destination PVZ id; the UI explains that `5 Post (Пятерочка)` is a Yandex-returned point/network name. Targeted admin page-state tests and Yandex request-shape tests pass, and typecheck passes.

Follow-up for the user's `platform_station_id`/warehouse hypothesis: Yandex Other-day docs distinguish `/offers/create` (offer variants / quote-like request, returns `offers[].offer_id`, separate confirmation) from `/request/create` (order creation). The documented test warehouse/source `platform_station_id` is `fbed3aa1-2cc6-4370-ab4d-59c5cc9bb924`; destination remains a PVZ `platform_station_id` from pickup-point lookup. A controlled sandbox diagnostic used only saved sealed credentials and did not print token/auth/raw provider body or call shipment/order/confirm endpoints. `/offers/create` with documented shape reached meaningful JSON validation (`items[0].place_barcode` missing), which is stronger evidence than `/offers/info no_delivery_options`; after adding matching item/place barcode, Yandex access-block `403` resumed, so no offer success was captured before provider blocking. Adapter/UI/docs were updated so `warehouse_to_pickup_point` Test quote uses documented `/offers/create` with warehouse source `platform_station`, destination PVZ `platform_station`, package `items/places`, billing info and `last_mile_policy=self_pickup`; pickup windows lookup is now optional helper rather than mandatory prerequisite.

### 2026-04-27 Delivery Admin/Yandex quote `502` follow-up

Latest safe runtime inspection showed `medusa-backend` was not stale for the `/offers/create` quote changes (`/health` returned `200 OK`; no restart needed). Safe event logs for the current user-visible `POST /admin/delivery/test-quote` `502` show the backend is calling Yandex sandbox `POST /offers/create` with source warehouse `platform_station`, destination PVZ `platform_station`, `items`, `places`, matching barcode linkage, `billing_info`, `recipient_info`, dimensions and `last_mile_policy=self_pickup`. Yandex returns provider `400` with safe code/message `no_delivery_options / No delivery options for interval`; the backend maps that provider rejection to Admin `502` with `DELIVERY_HUB_PROVIDER_ERROR`.

The optional `GET /admin/delivery/pickup-windows` diagnostic separately calls `POST /offers/info?last_mile_policy=self_pickup` and returns the same provider `400` safe reason for the selected warehouse/PVZ pair. Diagnosis for that selected pair is provider-side sandbox unavailability, not stale backend code and not a missing local `/offers/create` mapper field.

Admin UI diagnostics were improved so Test quote and pickup-window errors render safe provider status/category/code/message/path/operator hint/correlation chips instead of only the generic `Yandex Delivery request failed with status 400`. Targeted page-state coverage was updated for this safe error rendering helper. No token, auth header, ciphertext, raw provider body, shipment create/cancel/status/retry, confirmation, `/request/create`, or commit was performed.

Final same-day Yandex Other-day documentation/runtime follow-up found the exact catalog-filter issue behind the user question about `5Post`: `/pickup-points/list` without `operator_ids` may return Yandex Market, partners and 5Post; `5 Post (Пятерочка)` is a Yandex-catalog partner network (`operator_id=5post`), not a separate Delivery Hub provider. The correct sandbox lookup for Yandex Market PVZs is now `geo_id=213`, `operator_id=market_l4g`, `station_type=pickup_point`, optionally `is_yandex_branded=true`. Exact documented/sandbox destination PVZ `e1139f6d-e34f-47a9-a55f-31f032a861a6` resolves to `Пункт выдачи заказов Яндекс Маркета` in Moscow and returned 4 offers with the mapped test warehouse `fbed3aa1-2cc6-4370-ab4d-59c5cc9bb924`; three additional `market_l4g` candidates also returned 4 offers, one partner point returned 2 offers, while sampled 5Post points returned `no_delivery_options`.

Code/UX alignment was updated so Delivery Hub passes documented pickup-point filters through adapter/service/admin route, carries safe operator/network/brand/type fields in pickup point DTOs, defaults Admin lookup to `market_l4g` Yandex-branded Moscow PVZs, and labels result cards as `Провайдер: Yandex` plus separate `Сеть ПВЗ`. Targeted no-network tests passed (`4` suites / `115` tests), and backend `typecheck` passed.

### 2026-04-27 Delivery Admin/Yandex manual quote success baseline

Fresh manual Admin validation now confirms both mandatory first-tranche quote paths through the direct Yandex `/offers/create` adapter, with no runtime-code changes and no docs-run network checks:

- `warehouse_to_pickup_point`: success baseline from **Settings → Delivery → Test quote** with `quotes count=4`, currency `RUB`, first/UI price `181.9 rub`, pickup window required `no`, interval empty.
- `dropoff_point_to_pickup_point`: fresh screenshot confirms connection `yandexTestname · test · active`, destination PVZ/platform station id `e1139f6d-e34f-47a9-a55f-31f032a861a6`, origin dropoff point id `019d2a9da5877011a771b75e903f3039`, currency `RUB`, quantity `1`, weight `500` grams, declared price `2000`, correlation id `a4adab14-ff1c-40da-a2cd-bfa0726e3be7`, `quotes count=13`, first offer price `181.9 rub`, visible ETA examples `3–4`, `4–5`, `5–6`, `6–7`, pickup window required `no`, provider reference redacted.

This closes the manual quote smoke baseline for both supported Yandex first-tranche paths only at quote/offer stage. It does not change checkout, legacy delivery fulfillment, shipment execution, confirmation, status/cancel/retry semantics, env gates or runtime wiring. The subsequent storefront-neutral quote/selection smoke has now also passed over existing Delivery Hub neutral contracts without checkout cutover; next safe step is to keep this as regression evidence while planning any separate checkout cutover behind explicit approval.


## 2026-04-28T09:00:00Z — Delivery Hub read-only cutover candidate planner

Implemented the next controlled cutover-prep step as read-only candidate/evidence only. Backend now exposes `GET /store/delivery/cutover-candidate?cart_id=...` to derive a safe future Medusa shipping-option candidate from stored neutral Delivery Hub cart selection plus current shipping-option catalog snapshots. The planner returns only shopper/operator-safe summary fields, keeps `can_commit_shipping_method=false`, and reports `checkout_source_of_truth=unchanged`.

Behavior:
- Missing neutral selection returns `candidate_status=selection_missing` with blocked reasons and no candidate ids/amounts.
- Present neutral selection plus matching Delivery Hub managed shipping option returns `candidate_status=ready_for_review`, candidate shipping option id/name, amount/currency, and pickup point id.
- Present selection without a matching option returns `candidate_status=shipping_option_missing`.
- Planner does not call provider/Yandex, shipment lifecycle, cart mutation, or Medusa shipping-method commit.

Storefront Preview/Shadow UI now loads the candidate planner beside cutover preconditions at `delivery-hub-cutover-candidate-status` and labels it strictly “candidate only / no checkout commit”; unavailable planner fails safe.
