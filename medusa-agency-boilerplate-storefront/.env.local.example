# Canonical client-init contract: npm run client:init:contract
# Root-level canonical defaults for local development.
# Run `npm run bootstrap` from repository root to sync this file and replace the placeholder publishable key.
MEDUSA_BACKEND_URL=http://localhost:9000
MEDUSA_BACKEND_PORT=9000
NEXT_PUBLIC_MEDUSA_BACKEND_URL=http://localhost:9000
NEXT_PUBLIC_MEDUSA_BACKEND_PORT=9000

# This placeholder is intentionally invalid until root bootstrap writes the real key from backend seed output.
NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY=REPLACE_WITH_ROOT_BOOTSTRAP

# Storefront runtime baseline.
NEXT_PUBLIC_BASE_URL=http://localhost:8000
NEXT_PUBLIC_DEFAULT_REGION=ru

# Optional Phase 6 preset switch for sanctioned client scenarios.
NEXT_PUBLIC_STOREFRONT_PRESET=atelier

# YooKassa is the primary hosted checkout path but remains opt-in on the storefront.
NEXT_PUBLIC_YOOKASSA_ENABLED=false

# VK ID remains strictly optional and only enables account profile linking UI.
NEXT_PUBLIC_VK_ID_ENABLED=false

# Optional AI Assistant widget. Browser calls the public Medusa store proxy; no secret token is used here.
NEXT_PUBLIC_AI_ASSISTANT_WIDGET_ENABLED=false
NEXT_PUBLIC_AI_ASSISTANT_CHAT_ENDPOINT=/store/assistant/chat

# Stripe-compatible variables are optional and only used when a compatible provider is enabled.
NEXT_PUBLIC_STRIPE_KEY=
NEXT_PUBLIC_MEDUSA_PAYMENTS_PUBLISHABLE_KEY=
NEXT_PUBLIC_MEDUSA_PAYMENTS_ACCOUNT_ID=

# Optional storefront content layer.
PAYLOAD_ENABLED=false
PAYLOAD_CMS_URL=http://localhost:3100
PAYLOAD_CONTENT_PREVIEW_TOKEN=
PAYLOAD_PREVIEW_SECRET=
PAYLOAD_REVALIDATE_SECRET=

# Optional generic revalidate hook shared with adjacent tooling when explicitly enabled.
REVALIDATE_SECRET=
MEDUSA_CLOUD_S3_HOSTNAME=
MEDUSA_CLOUD_S3_PATHNAME=
