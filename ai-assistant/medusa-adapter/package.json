{
  "name": "@medusa-ai-assistant/medusa-adapter-template",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Copy-ready Medusa backend adapter templates for the standalone AI Assistant service.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check:static": "node --check tests/static-contract.test.mjs && node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
