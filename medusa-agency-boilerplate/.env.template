# Synced from the root-level orchestration contract.
# Preferred flow for clean clones:
# 1. copy root .env.example -> .env
# 2. run `npm run bootstrap` from repository root
# 3. root bootstrap writes deterministic local values here
STORE_CORS=http://localhost:8000,https://docs.medusajs.com
ADMIN_CORS=http://localhost:5173,http://localhost:9000,https://docs.medusajs.com
AUTH_CORS=http://localhost:5173,http://localhost:9000,http://localhost:8000,https://docs.medusajs.com
REDIS_URL=redis://localhost:6379
JWT_SECRET=CHANGE_ME
COOKIE_SECRET=CHANGE_ME
DATABASE_URL=postgresql://CHANGE_ME:CHANGE_ME@localhost:5433/medusa
DB_NAME=medusa-v2
